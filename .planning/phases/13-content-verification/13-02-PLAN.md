---
phase: 13-content-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/test/phase13-verification/page-verification.spec.ts
autonomous: true
requirements:
  - VERIFY-03
  - API-COMPAT-07
  - API-COMPAT-08

must_haves:
  truths:
    - "Page endpoints return all fields matching Go Page struct with correct types"
    - "Page created_at/updated_at are strings (not null) per CCP-1 resolution"
    - "Page CRUD operations return consistent field structure"
  artifacts:
    - path: "server/test/phase13-verification/page-verification.spec.ts"
      provides: "Page field-by-field verification tests (7 endpoints)"
      exports: ["describe Page Field Verification"]
  key_links:
    - from: "server/test/phase13-verification/page-verification.spec.ts"
      to: "_go-backend-archive/pkg/domain/model/page.go"
      via: "Go Page struct defines the authoritative field list for assertions"
      pattern: "Page"
---

<objective>
Page module field-by-field verification

Purpose: Verify all 7 page endpoints match Go backend Page struct field-by-field per D-293/D-294/D-295. Page endpoints are MEDIUM risk due to CCP-1 date nullability.
Output: page-verification.spec.ts with field-by-field tests for all 7 page endpoints
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-content-verification/13-CONTEXT.md
@.planning/phases/13-content-verification/13-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@server/test/helpers/api-compat-helpers.ts
@_go-backend-archive/pkg/domain/model/page.go
@frontend/src/types/page-management.ts
@server/src/page/page.controller.ts
@server/src/page/public-page.controller.ts
@server/src/page/page.service.ts
@server/src/page/dto/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Page field-by-field verification (7 endpoints)</name>
  <files>server/test/phase13-verification/page-verification.spec.ts</files>
  <read_first>
    _go-backend-archive/pkg/domain/model/page.go
    frontend/src/types/page-management.ts
    server/src/page/page.controller.ts
    server/src/page/public-page.controller.ts
    server/src/page/page.service.ts
    server/src/page/dto/page-response.dto.ts
    server/test/helpers/api-compat-helpers.ts
    server/test/api-compat/page-api-compat.spec.ts
  </read_first>
  <behavior>
    - Test 1: GET /api/pages returns PageListResponse with list items having all Page fields (id, created_at, updated_at, title, path, content_md, content_html, is_published, custom_js, custom_css, status, comment_count, view_count, cover_url, abbrlink, keywords, home_sort, pin_sort)
    - Test 2: GET /api/pages/:id returns Page with all fields and correct types
    - Test 3: POST /api/pages returns Page with all fields
    - Test 4: PUT /api/pages/:id returns Page with all fields
    - Test 5: GET /api/public/pages/:path returns Page with all fields
    - Test 6: DELETE /api/pages/:id returns void
    - Test 7: POST /api/pages/initialize returns void
  </behavior>
  <action>
    Create page-verification.spec.ts per D-298. Import helpers from ../helpers/api-compat-helpers per D-297.

    MEDIUM-risk page endpoints (per D-295, verify first):
    - #47 GET /api/pages: Read Go Page struct from _go-backend-archive/pkg/domain/model/page.go. Verify PageListResponse with list items having all Page fields: id (string, Sqids), created_at (string, NOT null per CCP-1), updated_at (string, NOT null), title (string), path (string), content_md (string), content_html (string), is_published (boolean), custom_js (string), custom_css (string), status (string), comment_count (number), view_count (number), cover_url (string), abbrlink (string), keywords (string), home_sort (number), pin_sort (number). Verify typeof for each field. Verify pagination has pageSize (camelCase) matching Go admin list.
    - #48 GET /api/pages/:id: Verify single Page with all fields and correct types.
    - #49 POST /api/pages: Create page with { title, path, content_md, content_html, is_published }, verify response has all Page fields. Per D-245, isPublished defaults to true, markdownContent/customJs/customCss default to empty string.
    - #50 PUT /api/pages/:id: Update page, verify response has all Page fields.
    - #53 GET /api/public/pages/:path: Verify public page returns Page with all fields.

    NONE-risk page endpoints:
    - #51 DELETE /api/pages/:id: Verify void response.
    - #52 POST /api/pages/initialize: Verify void response.

    For all date fields, assert they are strings (not null) per CCP-1 resolution. For id fields, assert typeof is string (Sqids encoded). For boolean fields (is_published), assert typeof is boolean. For number fields (view_count, comment_count, home_sort, pin_sort), assert typeof is number.
  </action>
  <verify>
    <automated>npx vitest run server/test/phase13-verification/page-verification.spec.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - page-verification.spec.ts exists with 7 endpoint tests
    - All Page fields verified: id, created_at, updated_at, title, path, content_md, content_html, is_published, custom_js, custom_css, status, comment_count, view_count, cover_url, abbrlink, keywords, home_sort, pin_sort
    - Date fields confirmed as strings (not null) per CCP-1
    - Boolean fields confirmed as boolean type
    - Number fields confirmed as number type
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → NestJS app | Test context runs NestJS in-memory, no network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Spoofing | Test JWT token | low | accept | Test token uses known seed |
| T-13-SC | Tampering | npm installs | high | mitigate | No new packages installed |
</threat_model>

<verification>
- Page tests: npx vitest run server/test/phase13-verification/page-verification.spec.ts passes
- Full suite: npx vitest run server/test/ --reporter=verbose passes (no regressions)
</verification>

<success_criteria>
- Page verification tests cover all 7 endpoints with field-by-field assertions
- All Page fields verified with correct types
- Date fields confirmed as strings (not null)
- No regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/13-content-verification/13-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `server/test/phase13-verification/page-verification.spec.ts` — describe("Page Field Verification"), test cases for 7 page endpoints
