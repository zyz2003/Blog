---
phase: 04-page-public-api
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - server/src/app.module.ts
  - server/test/page/page.controller.spec.ts
  - server/test/page/public-page.controller.spec.ts
autonomous: true
requirements: [PAGE-01, PUBLIC-01, VERSION-01]

must_haves:
  truths:
    - AppModule imports PageModule and VersionModule
    - NestJS starts on port 8091 with all Phase 04 routes registered
    - All Phase 04 unit tests pass
    - Page CRUD endpoints respond correctly
    - Public page endpoint returns published pages and 404 for unpublished
    - Version endpoints return correct format
    - /api/version/string bypasses global interceptor
  artifacts:
    - server/test/page/page.controller.spec.ts
    - server/test/page/public-page.controller.spec.ts
  key_links:
    - AppModule imports PageModule and VersionModule
    - All Phase 04 routes accessible after startup
    - Test suite covers all PAGE-01, PUBLIC-01, VERSION-01 requirements
---

<objective>
Wire PageModule and VersionModule into AppModule, create comprehensive unit tests for all Phase 04 services and controllers, and verify integration.

Purpose: Ensure all Phase 04 modules are properly registered in the application, all endpoints are accessible, and comprehensive test coverage validates API compatibility with the Go backend.

Output: Updated app.module.ts, 4 test files, integration verification
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-public-api/04-CONTEXT.md
@.planning/phases/03-article-category-tag/03-05-SUMMARY.md
@server/src/app.module.ts
@server/src/page/page.service.ts
@server/src/page/page.controller.ts
@server/src/page/public-page.controller.ts
@server/src/version/version.controller.ts
@server/test/helpers/article-fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire PageModule and VersionModule into AppModule and verify startup</name>
  <files>server/src/app.module.ts</files>
  <read_first>
    server/src/app.module.ts
    server/src/page/page.module.ts
    server/src/version/version.module.ts
  </read_first>
  <action>
    Edit server/src/app.module.ts to add VersionModule import and registration.

    The PageModule is already imported and listed in the imports array (from Phase 01 placeholder). Verify it is present. If the import path is correct (./page/page.module), no change needed for PageModule.

    Add VersionModule:
    - Add import statement: import { VersionModule } from './version/version.module';
    - Add VersionModule to the imports array in @Module decorator

    Verify the app starts correctly by running npm run dev briefly and checking that routes are registered. The app should start on port 8091 with all Phase 04 routes visible in the log output.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>
    - VersionModule imported and added to AppModule imports array
    - PageModule already present in AppModule imports
    - TypeScript compilation passes
    - App starts on port 8091 with Phase 04 routes registered
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PageController and PublicPageController tests, and verify full integration</name>
  <files>server/test/page/page.controller.spec.ts, server/test/page/public-page.controller.spec.ts</files>
  <read_first>
    server/src/page/page.service.ts
    server/src/page/page.controller.ts
    server/src/page/public-page.controller.ts
    server/test/helpers/page-fixtures.ts
    server/test/article/article.controller.spec.ts
  </read_first>
  <action>
    Create server/test/page/page.controller.spec.ts:
    - Test route registration using NestJS Test.createTestingModule
    - Test @Public() decorator is NOT present (admin routes require auth)
    - Test controller methods call service with correct params
    - Use page-fixtures.ts from Plan 04-01 for mock data

    Create server/test/page/public-page.controller.spec.ts:
    - Test @Public() decorator IS present at class level
    - Test getByPath prepends '/' to path param
    - Test unpublished page returns NotFoundException

    Run the full Phase 04 test suite to verify all tests pass:
    cd server && npx vitest run test/page/ test/version/ --reporter=verbose
  </action>
  <verify>
    <automated>cd server && npx vitest run test/page/ test/version/ --reporter=verbose</automated>
  </verify>
  <done>
    - page.controller.spec.ts covers route registration and auth guard behavior
    - public-page.controller.spec.ts covers @Public() decorator and unpublished page 404
    - All Phase 04 tests pass (page.repository, page.service, page.controller, public-page.controller, version.controller)
    - Full test suite passes (no regressions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment | No production trust boundaries; tests verify security controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | Test mock injection | low | accept | Test-only concern; mocks do not affect production |
</threat_model>

<verification>
- TypeScript compilation: cd server && npx tsc --noEmit
- All Phase 04 tests: cd server && npx vitest run test/page/ test/version/ --reporter=verbose
- Full test suite: cd server && npx vitest run --reporter=verbose
- AppModule has VersionModule: grep -c 'VersionModule' server/src/app.module.ts
</verification>

<success_criteria>
- AppModule imports both PageModule and VersionModule
- App starts on port 8091 with all Phase 04 routes
- All Phase 04 unit tests pass
- Page CRUD endpoints work correctly
- Public page endpoint returns published pages, 404 for unpublished
- Version endpoints return correct format
- /api/version/string bypasses global interceptor
- Full test suite passes (no regressions)
</success_criteria>

<output>
Create `.planning/phases/04-page-public-api/04-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Updated `AppModule` (server/src/app.module.ts) — adds VersionModule import
- `page.controller.spec.ts` (server/test/page/page.controller.spec.ts) — PageController unit tests
- `public-page.controller.spec.ts` (server/test/page/public-page.controller.spec.ts) — PublicPageController unit tests
