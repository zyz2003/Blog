---
phase: 16-ai-model-router-summary-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - frontend/src/lib/settings/setting-descriptors.ts
  - frontend/src/lib/settings/setting-keys.ts
  - frontend/src/app/admin/settings/_config/settings-nav.ts
  - frontend/src/app/admin/settings/_config/settings-forms.ts
  - frontend/src/components/admin/settings/AiSummaryForm.tsx
  - frontend/src/components/admin/settings/AiModelsForm.tsx
  - frontend/src/components/admin/settings/AiPlaceholderForm.tsx
autonomous: true
requirements:
  - AI-02A
  - AI-01

must_haves:
  truths:
    - Admin can manage multiple AI profiles (add/edit/delete, set default, toggle enabled, mark purposes) via AiModelsForm per D-335
    - Admin can configure summary-specific settings (system_prompt, gpt_name, select profile for summary) via upgraded AiSummaryForm per D-335
    - "AI 功能" top-level nav group exists with 4 sub-items (AI 模型, AI 摘要, AI 对话, AI 写作) per D-334
    - ai_profiles and ai_default_profile_id keys belong to ai-models category per D-336
    - ai_summary_system_prompt and ai_summary_gpt_name keys belong to ai-summary category per D-336
    - Legacy ai_summary_provider/api_url/api_key/model keys are NOT exposed in UI per D-336
    - AI 对话 and AI 写作 show placeholder "敬请期待" cards per D-335
  artifacts:
    - frontend/src/components/admin/settings/AiModelsForm.tsx
    - frontend/src/components/admin/settings/AiPlaceholderForm.tsx
    - Updated AiSummaryForm.tsx
    - Updated setting-descriptors.ts with new category IDs
    - Updated settings-nav.ts with AI 功能 group
    - Updated settings-forms.ts with new form registrations
  key_links:
    - AiModelsForm reads/writes ai_profiles JSON and ai_default_profile_id
    - AiSummaryForm reads/writes ai_summary_system_prompt, ai_summary_gpt_name, and summary profile selection
    - Three-file sync: setting-descriptors.ts + settings-nav.ts + settings-forms.ts all reference same category IDs

user_setup: []
---

<objective>
Upgrade the frontend AI settings from a single-profile form to a multi-profile management system. Create the "AI 功能" top-level navigation group with 4 sub-items. Build AiModelsForm for managing ai_profiles JSON array (add/edit/delete profiles, set default, toggle enabled, mark purposes). Upgrade AiSummaryForm to manage summary-specific settings (prompt, gpt_name, profile selection). Add placeholder forms for AI 对话 and AI 写作.

Purpose: Enable admin to configure multiple AI model profiles that can be shared across different AI features (summary, chat, writing). This is the admin UI for the ModelResolver backend built in Plan 01.

Output: Fully functional multi-profile AI settings management in the admin panel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-ai-model-router-summary-migration/16-CONTEXT.md
@.planning/phases/16-ai-model-router-summary-migration/16-RESEARCH.md
@frontend/src/components/admin/settings/AiSummaryForm.tsx
@frontend/src/lib/settings/setting-keys.ts
@frontend/src/lib/settings/setting-descriptors.ts
@frontend/src/app/admin/settings/_config/settings-nav.ts
@frontend/src/app/admin/settings/_config/settings-forms.ts
@frontend/src/components/admin/settings/PostSettingsForm.tsx
@frontend/src/components/admin/settings/SettingsSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Register new AI category IDs in settings three-file system</name>
  <files>
    frontend/src/lib/settings/setting-descriptors.ts,
    frontend/src/lib/settings/setting-keys.ts,
    frontend/src/app/admin/settings/_config/settings-nav.ts,
    frontend/src/app/admin/settings/_config/settings-forms.ts
  </files>
  <read_first>
    frontend/src/lib/settings/setting-descriptors.ts,
    frontend/src/lib/settings/setting-keys.ts,
    frontend/src/app/admin/settings/_config/settings-nav.ts,
    frontend/src/app/admin/settings/_config/settings-forms.ts
  </read_first>
  <action>
    Update the three-file settings registration system per D-334, D-335, D-336. All changes must be in the same commit to avoid runtime crash (Pitfall 7 in RESEARCH.md).

    1. In `setting-keys.ts`: Add two new key constants (they already exist at lines 485-486, verify they are present):
       - `KEY_AI_PROFILES = "ai_profiles"` (already exists at line 485)
       - `KEY_AI_DEFAULT_PROFILE_ID = "ai_default_profile_id"` (already exists at line 486)
       No new keys needed — these are already defined.

    2. In `setting-descriptors.ts`:
       a. Add 4 new values to the SettingCategoryId union type: `"ai-models"`, `"ai-summary"`, `"ai-chat"`, `"ai-writing"`.
       b. Remove `"advanced-ai-summary"` from the union type (replaced by ai-models + ai-summary per D-336).
       c. In the categoryDescriptors object:
          - Remove the `"advanced-ai-summary"` entry (lines 834-841).
          - Add `"ai-models"` entry with keys: `KEY_AI_PROFILES` (type "json"), `KEY_AI_DEFAULT_PROFILE_ID` (type "string") per D-336.
          - Add `"ai-summary"` entry with keys: `KEY_AI_SUMMARY_PROFILE_ID` (type "string"), `KEY_AI_SUMMARY_SYSTEM_PROMPT` (type "string"), `KEY_AI_SUMMARY_GPT_NAME` (type "string") per D-336.
          - Add `"ai-chat"` entry with empty array (placeholder per D-335).
          - Add `"ai-writing"` entry with empty array (placeholder per D-335).
       d. Do NOT include legacy keys (ai_summary_provider, ai_summary_api_url, ai_summary_api_key, ai_summary_model) in any category — they are internal-only per D-336.

    3. In `settings-nav.ts`:
       a. Add a new top-level CategorySection with id "ai", label "AI 功能", icon Bot (already imported) per D-334.
       b. Children: 4 SubSections — { id: "ai-models", label: "AI 模型", icon: Bot }, { id: "ai-summary", label: "AI 摘要", icon: FileText }, { id: "ai-chat", label: "AI 对话", icon: MessageCircle }, { id: "ai-writing", label: "AI 写作", icon: PenLine } per D-334.
       c. Import PenLine from lucide-react (add to existing imports).
       d. Remove the "advanced-ai-summary" SubSection from the "advanced" group (lines 120-124).
       e. Place the new "ai" group after "advanced" in the settingsCategories array.

    4. In `settings-forms.ts`:
       a. Remove the `"advanced-ai-summary"` entry.
       b. Add `"ai-models"` entry: `lazy(() => import("@/components/admin/settings/AiModelsForm").then(m => ({ default: m.AiModelsForm }))) as LazyForm`.
       c. Add `"ai-summary"` entry: `lazy(() => import("@/components/admin/settings/AiSummaryForm").then(m => ({ default: m.AiSummaryForm }))) as LazyForm`.
       d. Add `"ai-chat"` entry: `lazy(() => import("@/components/admin/settings/AiPlaceholderForm").then(m => ({ default: m.AiPlaceholderForm }))) as LazyForm`.
       e. Add `"ai-writing"` entry: `lazy(() => import("@/components/admin/settings/AiPlaceholderForm").then(m => ({ default: m.AiPlaceholderForm }))) as LazyForm`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20; echo "tsc-exit: ${PIPESTATUS[0]}"</automated>
  </verify>
  <done>
    - SettingCategoryId union includes ai-models, ai-summary, ai-chat, ai-writing; excludes advanced-ai-summary
    - settings-nav.ts has "AI 功能" top-level group with 4 children
    - settings-forms.ts registers AiModelsForm, AiSummaryForm, AiPlaceholderForm for the 4 new categories
    - No TypeScript errors in the three settings files
    - Legacy ai_summary_provider/api_url/api_key/model keys are NOT in any category descriptor
  </done>
  <reversibility rating="costly">Changing nav structure affects 3 files simultaneously; reverting requires coordinated rollback of all three</reversibility>
</task>

<task type="auto">
  <name>Build AiModelsForm, upgrade AiSummaryForm, create AiPlaceholderForm</name>
  <files>
    frontend/src/components/admin/settings/AiModelsForm.tsx,
    frontend/src/components/admin/settings/AiSummaryForm.tsx,
    frontend/src/components/admin/settings/AiPlaceholderForm.tsx
  </files>
  <read_first>
    frontend/src/components/admin/settings/AiSummaryForm.tsx,
    frontend/src/components/admin/settings/PostSettingsForm.tsx,
    frontend/src/components/admin/settings/SettingsSection.tsx,
    frontend/src/lib/settings/setting-keys.ts
  </read_first>
  <action>
    1. Create `frontend/src/components/admin/settings/AiModelsForm.tsx` per D-335:
       - "use client" directive.
       - Props: `{ values: Record<string, string>; onChange: (key: string, value: string) => void; loading?: boolean }` — same pattern as existing forms.
       - Parse `values[KEY_AI_PROFILES]` as JSON to get AiProfile[] (with try/catch, default to empty array).
       - Display each profile as a card with: name (editable input), provider (select: openai/deepseek/custom), api_url (input), model (input), api_key (password input), enabled (checkbox/switch), purposes (checkboxes: summary, chat, writing — per D-333).
       - Each card has "设为默认" button per D-332 — sets KEY_AI_DEFAULT_PROFILE_ID to that profile's id.
       - "添加模型" button at bottom — generates a new profile with nanoid-style id (e.g. `p_${Date.now()}`), adds to array, writes back as JSON via onChange(KEY_AI_PROFILES, JSON.stringify(profiles)).
       - "删除" button on each card — removes profile from array, writes back.
       - Provider preset auto-fill: when provider changes to "openai", auto-fill api_url with "https://api.openai.com/v1" and model with "gpt-4o-mini" (same pattern as current AiSummaryForm). Same for "deepseek".
       - When saving, if no default exists and there are enabled profiles, auto-set the first enabled as default per D-332.
       - Use SettingsSection + SettingsFieldGroup containers for layout consistency.
       - Show Spinner when loading.

    2. Upgrade `frontend/src/components/admin/settings/AiSummaryForm.tsx` per D-335:
       - Remove the provider/api_url/api_key/model fields (these are now in AiModelsForm per D-336).
       - Keep the "前台展示" section with gpt_name (KEY_AI_SUMMARY_GPT_NAME).
       - Keep the "提示词配置" section with system_prompt (KEY_AI_SUMMARY_SYSTEM_PROMPT).
       - Add a "摘要模型" section at the top: a FormSelect that lists all enabled profiles from ai_profiles JSON (parse values[KEY_AI_PROFILES]), allowing the admin to pick which profile to use for summary generation. The selected profile id is stored as KEY_AI_SUMMARY_PROFILE_ID (summary-specific key, separate from the global KEY_AI_DEFAULT_PROFILE_ID). This avoids dual-writer conflict when chat/writing features arrive in Phase 18/19.
       - If no profiles are configured, show a message: "请先在「AI 模型」中添加模型配置".
       - Remove KEY_AI_SUMMARY_PROVIDER, KEY_AI_SUMMARY_API_URL, KEY_AI_SUMMARY_API_KEY, KEY_AI_SUMMARY_MODEL imports — these are no longer in this form per D-336.

    3. Create `frontend/src/components/admin/settings/AiPlaceholderForm.tsx` per D-335:
       - Simple component showing a centered placeholder card with icon and text "敬请期待" (Coming Soon).
       - Accept same props interface as other forms for consistency.
       - Used for both ai-chat and ai-writing categories.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10; echo "tsc-exit: ${PIPESTATUS[0]}"</automated>
  </verify>
  <done>
    - AiModelsForm manages ai_profiles JSON array: add/edit/delete profiles, set default, toggle enabled, mark purposes
    - AiSummaryForm shows profile selector + system_prompt + gpt_name (no provider/api_url/api_key/model fields)
    - AiPlaceholderForm shows "敬请期待" placeholder
    - All three components compile without TypeScript errors
    - Provider preset auto-fill works in AiModelsForm (openai/deepseek)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> Settings API | Admin saves ai_profiles JSON containing API keys |
| Settings API -> Settings DB | API keys stored as plain text in settings table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Information Disclosure | AiModelsForm API key field | medium | mitigate | API key input uses type="password"; existing pattern from AiSummaryForm; keys not in PUBLIC_SETTING_KEYS |
| T-16-06 | Tampering | ai_profiles JSON validation | low | accept | Admin-only form; backend resolveProfiles has try/catch for malformed JSON; Zod validation in future phase |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
- Admin settings page loads with "AI 功能" nav group showing 4 sub-items
- Clicking "AI 模型" shows AiModelsForm with add/edit/delete profile functionality
- Clicking "AI 摘要" shows upgraded AiSummaryForm with profile selector + prompt + gpt_name
- Clicking "AI 对话" or "AI 写作" shows placeholder card
- Old "高级功能 > AI 摘要" nav item no longer exists
</verification>

<success_criteria>
- "AI 功能" top-level nav group with 4 sub-items exists per D-334
- AiModelsForm manages multi-profile ai_profiles JSON per D-335
- AiSummaryForm upgraded to profile selector + prompt + gpt_name per D-335
- Legacy ai_summary_* provider keys not exposed in UI per D-336
- Placeholder forms for AI 对话 and AI 写作 per D-335
- Three-file settings registration in sync (descriptors + nav + forms)
- No TypeScript compilation errors
</success_criteria>

<output>
Create `.planning/phases/16-ai-model-router-summary-migration/16-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Components
- `AiModelsForm` — in `frontend/src/components/admin/settings/AiModelsForm.tsx`
- `AiPlaceholderForm` — in `frontend/src/components/admin/settings/AiPlaceholderForm.tsx`
- `AiSummaryForm` (upgraded) — in `frontend/src/components/admin/settings/AiSummaryForm.tsx`

### Category IDs Added
- `"ai-models"` — manages ai_profiles + ai_default_profile_id
- `"ai-summary"` — manages ai_summary_system_prompt + ai_summary_gpt_name
- `"ai-chat"` — placeholder
- `"ai-writing"` — placeholder

### Category ID Removed
- `"advanced-ai-summary"` — replaced by ai-models + ai-summary
