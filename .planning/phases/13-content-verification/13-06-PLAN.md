---
phase: 13-content-verification
plan: 06
type: execute
wave: 4
depends_on: ['13-01', '13-02', '13-03', '13-04', '13-05']
files_modified:
  - server/test/phase13-verification/ # potentially: fix failures found during regression
autonomous: true
requirements:
  - VERIFY-03
  - API-COMPAT-07
  - API-COMPAT-08

must_haves:
  truths:
    - "Full phase13-verification test suite passes with zero failures"
    - "Existing api-compat test suite still passes (no regressions from file.service.ts fixes)"
    - "All 7 verification test files exist in server/test/phase13-verification/"
  artifacts:
    - path: "server/test/phase13-verification/"
      provides: "Complete verification test suite (7 files, ~50 endpoints)"
      exports: []
  key_links:
    - from: "server/test/phase13-verification/"
      to: "server/test/api-compat/"
      via: "Phase 13 tests must not break existing api-compat tests"
      pattern: "vitest run"
---

<objective>
Full regression + Phase 13 test suite validation

Purpose: Run the complete phase13-verification test suite and the full existing test suite to confirm no regressions. Fix any issues found. This is the phase gate before Phase 14.
Output: Confirmed passing test suite with no regressions
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-content-verification/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression test + fix any failures</name>
  <files>server/test/phase13-verification/article-verification.spec.ts, server/test/phase13-verification/category-verification.spec.ts, server/test/phase13-verification/tag-verification.spec.ts, server/test/phase13-verification/page-verification.spec.ts, server/test/phase13-verification/file-verification.spec.ts, server/test/phase13-verification/comment-verification.spec.ts, server/test/phase13-verification/search-verification.spec.ts</files>
  <read_first>
    server/test/phase13-verification/article-verification.spec.ts
    server/test/phase13-verification/category-verification.spec.ts
    server/test/phase13-verification/tag-verification.spec.ts
    server/test/phase13-verification/page-verification.spec.ts
    server/test/phase13-verification/file-verification.spec.ts
    server/test/phase13-verification/comment-verification.spec.ts
    server/test/phase13-verification/search-verification.spec.ts
  </read_first>
  <acceptance_criteria>
    - npx vitest run server/test/phase13-verification/ passes with zero failures
    - npx vitest run server/test/api-compat/ passes with zero failures
    - npx vitest run server/test/ passes with zero failures
    - All 7 verification test files exist in server/test/phase13-verification/
    - Any test failures are fixed and re-verified
  </acceptance_criteria>
  <action>
    Run the full phase13-verification test suite: npx vitest run server/test/phase13-verification/ --reporter=verbose. Capture the output. If any tests fail, read the failing test file and the relevant NestJS source file to understand the mismatch, then fix either the test (if the assertion was wrong) or the NestJS code (if the implementation is wrong per Go DTO comparison).

    Run the existing api-compat test suite: npx vitest run server/test/api-compat/ --reporter=verbose. This confirms the file.service.ts changes from Plan 03 do not break existing tests.

    Run the full test suite: npx vitest run server/test/ --reporter=verbose. This confirms no regressions across all tests.

    If file.service.ts changes broke existing api-compat tests, fix the api-compat tests to match the corrected format (page_size instead of pageSize, etc.). The phase13-verification tests are the source of truth for correct behavior.

    Document the final test counts: how many phase13-verification tests, how many api-compat tests, total pass/fail counts.
  </action>
  <verify>
    <automated>npx vitest run server/test/ --reporter=verbose 2>&1 | grep -E "(Tests|passed|failed)" | tail -5</automated>
  </verify>
  <done>
    - Full phase13-verification suite passes (7 test files)
    - Full api-compat suite passes (no regressions)
    - Full test suite passes with zero failures
    - Test counts documented in SUMMARY
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → All test suites | Regression test touches all test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Tampering | Test fixes | low | accept | Test fixes are verified by re-running the suite |
| T-13-SC | Tampering | npm installs | high | mitigate | No new packages installed |
</threat_model>

<verification>
- Phase 13 suite: npx vitest run server/test/phase13-verification/ passes
- API compat suite: npx vitest run server/test/api-compat/ passes
- Full suite: npx vitest run server/test/ passes
</verification>

<success_criteria>
- All phase13-verification tests pass
- No regressions in api-compat tests
- Full test suite green
- Phase 13 verification complete and ready for Phase 14
</success_criteria>

<output>
Create `.planning/phases/13-content-verification/13-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Updated test files in `server/test/phase13-verification/` (fixes if any failures found during regression)
