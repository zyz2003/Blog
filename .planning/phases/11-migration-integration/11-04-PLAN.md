# Plan 11-04: Stats, Links, Album, SEO, Notification Module API Compat Tests

**Phase:** 11-Migration & Integration
**Depends on:** 11-02 (test infrastructure and helpers)
**Estimated files:** 9 new files

## Must-Haves

| Truth | Artifact | Key Link |
|-------|----------|----------|
| Stats/links/album/SEO/notification endpoints return correct response shapes | 9 test files in server/test/api-compat/ | Each test file asserts { code, data, message } wrapper and module-specific field names/types |

## Objective

Create API compatibility test files for statistics, links, albums, album categories, RSS, sitemap, music, notifications, and subscribers.

## Tasks

### Task 1: Statistics & links module test files (2 files)

**Files:**
- `server/test/api-compat/statistics-api-compat.spec.ts`
- `server/test/api-compat/link-api-compat.spec.ts`

**Endpoints covered:**

Statistics (7):
- GET /api/public/statistics/basic — Basic stats
- POST /api/public/statistics/visit — Record visit
- GET /api/statistics/analytics — Visitor analytics (JWT+Admin)
- GET /api/statistics/top-pages — Top pages (JWT+Admin)
- GET /api/statistics/trend — Visitor trend (JWT+Admin)
- GET /api/statistics/summary — Summary (JWT+Admin)
- GET /api/statistics/visitor-logs — Visitor logs (JWT+Admin)

Links (25):
- POST /api/public/links — Apply link (RateLimit)
- GET /api/public/links — Public list
- GET /api/public/links/random — Random links
- GET /api/public/links/applications — All applications
- GET /api/public/links/check-exists — Check exists
- GET /api/public/link-categories — Public categories
- POST /api/links — Admin create (JWT+Admin)
- GET /api/links — Admin list (JWT+Admin)
- DELETE /api/links/batch-delete — Batch delete (JWT+Admin)
- PUT /api/links/:id — Admin update (JWT+Admin)
- DELETE /api/links/:id — Admin delete (JWT+Admin)
- PUT /api/links/:id/review — Review (JWT+Admin)
- POST /api/links/import — Import (JWT+Admin)
- GET /api/links/export — Export (JWT+Admin)
- POST /api/links/health-check — Health check (JWT+Admin)
- GET /api/links/health-check/status — Health check status (JWT+Admin)
- PUT /api/links/sort — Batch sort (JWT+Admin)
- GET /api/links/categories — List categories (JWT+Admin)
- POST /api/links/categories — Create category (JWT+Admin)
- PUT /api/links/categories/:id — Update category (JWT+Admin)
- DELETE /api/links/categories/:id — Delete category (JWT+Admin)
- GET /api/links/tags — List tags (JWT+Admin)
- POST /api/links/tags — Create tag (JWT+Admin)
- PUT /api/links/tags/:id — Update tag (JWT+Admin)
- DELETE /api/links/tags/:id — Delete tag (JWT+Admin)

**Key assertions:**
- Statistics basic has today/yesterday/total counts
- Visit recording returns immediately (fire-and-forget)
- Link list grouped by categories
- Link apply has rate limiting
- Health check returns status per link

**Verification:** `npx vitest run server/test/api-compat/statistics-api-compat.spec.ts server/test/api-compat/link-api-compat.spec.ts --reporter=verbose`

### Task 2: Album & SEO module test files (5 files)

**Files:**
- `server/test/api-compat/album-api-compat.spec.ts`
- `server/test/api-compat/album-category-api-compat.spec.ts`
- `server/test/api-compat/rss-api-compat.spec.ts`
- `server/test/api-compat/sitemap-api-compat.spec.ts`
- `server/test/api-compat/music-api-compat.spec.ts`

**Endpoints covered:**

Album (11):
- GET /api/albums/get — Admin list (JWT+Admin)
- POST /api/albums/add — Add (JWT+Admin)
- POST /api/albums/batch-import — Batch import (JWT+Admin)
- PUT /api/albums/update/:id — Update (JWT+Admin)
- DELETE /api/albums/delete/:id — Delete (JWT+Admin)
- DELETE /api/albums/batch-delete — Batch delete (JWT+Admin)
- POST /api/albums/export — Export (JWT+Admin)
- POST /api/albums/import — Import (JWT+Admin)
- GET /api/public/albums — Public albums
- GET /api/public/album-categories — Public categories
- PUT /api/public/stat/:id — Update stat

Album Category (5):
- POST /api/album-categories — Create (JWT+Admin)
- GET /api/album-categories — List (JWT+Admin)
- GET /api/album-categories/:id — Get (JWT+Admin)
- PUT /api/album-categories/:id — Update (JWT+Admin)
- DELETE /api/album-categories/:id — Delete (JWT+Admin)

RSS (3):
- GET /rss.xml — RSS feed
- GET /feed.xml — RSS feed (alternate)
- GET /atom.xml — Atom feed

Sitemap (2):
- GET /sitemap.xml — Sitemap
- GET /robots.txt — Robots

Music (2):
- GET /api/public/music/playlist — Playlist
- POST /api/public/music/song-resources — Song resources

**Key assertions:**
- Album has widthAndHeight computed field ("WxH" format)
- Album has aspectRatio computed via GCD
- Album list paginated with { list, total, pageNum, pageSize }
- Album category has only { id, name, description, displayOrder }
- RSS returns valid XML with Content-Type: application/xml or text/xml
- Sitemap returns valid XML
- robots.txt returns text/plain
- Music playlist has songs array with name/artist/url fields

**Verification:** `npx vitest run server/test/api-compat/album-api-compat.spec.ts server/test/api-compat/album-category-api-compat.spec.ts server/test/api-compat/rss-api-compat.spec.ts server/test/api-compat/sitemap-api-compat.spec.ts server/test/api-compat/music-api-compat.spec.ts --reporter=verbose`

### Task 3: Notification & subscriber module test files (2 files)

**Files:**
- `server/test/api-compat/notification-api-compat.spec.ts`
- `server/test/api-compat/subscriber-api-compat.spec.ts`

**Endpoints covered:**

Notification (4):
- GET /api/user/notification-settings — Get settings (JWT)
- PUT /api/user/notification-settings — Update settings (JWT)
- GET /api/user/notification-configs — Get configs (JWT)
- GET /api/notification/types — List types (JWT+Admin)

Subscriber (4):
- POST /api/public/subscribe — Subscribe (RateLimit)
- POST /api/public/subscribe/code — Send code (RateLimit)
- POST /api/public/unsubscribe — Unsubscribe
- GET /api/public/unsubscribe/:token — Unsubscribe by token

**Key assertions:**
- Notification settings has type-based enable/disable flags
- Notification types list has id, name, description
- Subscribe returns verification requirement
- Unsubscribe by token works

**Verification:** `npx vitest run server/test/api-compat/notification-api-compat.spec.ts server/test/api-compat/subscriber-api-compat.spec.ts --reporter=verbose`

## Files Changed

| File | Action | Description |
|------|--------|-------------|
| `server/test/api-compat/statistics-api-compat.spec.ts` | CREATE | Statistics tests (7 endpoints) |
| `server/test/api-compat/link-api-compat.spec.ts` | CREATE | Link tests (25 endpoints) |
| `server/test/api-compat/album-api-compat.spec.ts` | CREATE | Album tests (11 endpoints) |
| `server/test/api-compat/album-category-api-compat.spec.ts` | CREATE | Album category tests (5 endpoints) |
| `server/test/api-compat/rss-api-compat.spec.ts` | CREATE | RSS tests (3 endpoints) |
| `server/test/api-compat/sitemap-api-compat.spec.ts` | CREATE | Sitemap tests (2 endpoints) |
| `server/test/api-compat/music-api-compat.spec.ts` | CREATE | Music tests (2 endpoints) |
| `server/test/api-compat/notification-api-compat.spec.ts` | CREATE | Notification tests (4 endpoints) |
| `server/test/api-compat/subscriber-api-compat.spec.ts` | CREATE | Subscriber tests (4 endpoints) |
