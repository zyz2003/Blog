---
phase: 03-article-category-tag
plan: 05
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - server/src/app.module.ts
  - server/src/common/constants/error-codes.ts
  - server/test/article/article.service.spec.ts
  - server/test/article/article.controller.spec.ts
  - server/test/post-category/post-category.service.spec.ts
  - server/test/post-tag/post-tag.service.spec.ts
  - server/test/article-history/article-history.service.spec.ts
  - server/test/helpers/article-fixtures.ts
autonomous: true
requirements: [ARTICLE-01, ARTICLE-02, ARTICLE-03, CATEGORY-01, TAG-01]

must_haves:
  truths:
    - AppModule imports all Phase 03 modules (ArticleModule, ArticleHistoryModule, PostCategoryModule, PostTagModule)
    - npm run dev starts NestJS on port 8091 with all Phase 03 routes registered
    - All article/category/tag/history endpoints respond correctly
    - Unit tests pass for article, category, tag, and history services
    - Integration test covers full article CRUD + public endpoints + history flow
  artifacts:
    - server/test/article/article.service.spec.ts
    - server/test/article/article.controller.spec.ts
    - server/test/post-category/post-category.service.spec.ts
    - server/test/post-tag/post-tag.service.spec.ts
    - server/test/article-history/article-history.service.spec.ts
    - server/test/helpers/article-fixtures.ts
  key_links:
    - AppModule correctly imports and wires all Phase 03 modules
    - Test fixtures provide consistent test data across all test files
    - Article module imports ArticleHistoryModule for auto-creation integration

---

<objective>
Wire Phase 03 modules into AppModule and create comprehensive test suite.

Purpose: Ensure all Phase 03 modules are registered in AppModule, the application starts successfully, and all endpoints are tested. This plan validates the complete article/category/tag/history feature set works end-to-end.

Output: Updated AppModule, unit tests for all services, and integration tests covering the full article lifecycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-article-category-tag/03-CONTEXT.md
@.planning/phases/03-article-category-tag/03-RESEARCH.md
@server/src/app.module.ts
@server/src/article/article.module.ts
@server/src/article-history/article-history.module.ts
@server/src/post-category/post-category.module.ts
@server/src/post-tag/post-tag.module.ts
@server/src/common/constants/error-codes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 03 modules into AppModule and verify startup</name>
  <files>
    server/src/app.module.ts,
    server/src/common/constants/error-codes.ts
  </files>
  <read_first>
    server/src/app.module.ts,
    server/src/article/article.module.ts,
    server/src/article-history/article-history.module.ts,
    server/src/post-category/post-category.module.ts,
    server/src/post-tag/post-tag.module.ts,
    server/src/common/constants/error-codes.ts,
    server/src/main.ts
  </read_first>
  <acceptance_criteria>
    - AppModule imports array includes ArticleModule, ArticleHistoryModule, PostCategoryModule, PostTagModule
    - npm run dev starts without module resolution errors
    - All Phase 03 routes are registered (visible in NestJS startup logs)
    - No circular dependency errors
  </acceptance_criteria>
  <action>
    Update server/src/app.module.ts:
    - Import ArticleModule from './article/article.module'
    - Import ArticleHistoryModule from './article-history/article-history.module'
    - Import PostCategoryModule from './post-category/post-category.module'
    - Import PostTagModule from './post-tag/post-tag.module'
    - Add all four modules to the imports array

    Note: The existing app.module.ts already imports ArticleModule (placeholder). The import path is the same, so just ensure it remains. Add the three new modules (ArticleHistoryModule, PostCategoryModule, PostTagModule).

    Verify no circular dependencies:
    - ArticleModule imports ArticleHistoryModule (for auto-creation)
    - ArticleHistoryModule imports DatabaseModule only
    - PostCategoryModule imports DatabaseModule only
    - PostTagModule imports DatabaseModule only
    - No circular dependency chain exists

    After updating, verify application starts: cd server && npx nest start (or npm run dev) should start without errors.
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    AppModule imports all Phase 03 modules. Application starts successfully on port 8091. No circular dependency errors. All Phase 03 routes are registered.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test fixtures and unit tests for article, category, tag, and history services</name>
  <files>
    server/test/helpers/article-fixtures.ts,
    server/test/article/article.service.spec.ts,
    server/test/article/article.controller.spec.ts,
    server/test/post-category/post-category.service.spec.ts,
    server/test/post-tag/post-tag.service.spec.ts,
    server/test/article-history/article-history.service.spec.ts
  </files>
  <read_first>
    server/src/article/article.service.ts,
    server/src/article/article.controller.ts,
    server/src/article/article.repository.ts,
    server/src/post-category/post-category.service.ts,
    server/src/post-category/post-category.controller.ts,
    server/src/post-tag/post-tag.service.ts,
    server/src/post-tag/post-tag.controller.ts,
    server/src/article-history/article-history.service.ts,
    server/src/article-history/article-history.controller.ts,
    server/src/common/utils/sqids.util.ts,
    server/src/common/utils/time.util.ts,
    server/test/phase02-integration.spec.ts
  </read_first>
  <behavior>
    - Test fixtures provide mock article, category, tag, and history data
    - Article service tests cover: create, update, delete, get, list, toApiResponse, validateAbbrlink, calculatePostStats, diffIDs
    - Article controller tests cover: route registration, auth guards, Sqids ID decoding
    - PostCategory service tests cover: list, create, update, delete, duplicate name rejection
    - PostTag service tests cover: list, create, update, delete, duplicate name rejection
    - ArticleHistory service tests cover: createHistory, listHistory, getVersion, compareVersions, restoreVersion, getHistoryCount, old version cleanup
  </behavior>
  <action>
    Create test infrastructure and unit tests.

    **Test fixtures** (server/test/helpers/article-fixtures.ts):
    - Mock article DB row with all 30+ fields
    - Mock category and tag DB rows
    - Mock article history DB row
    - Mock CreateArticleDto and UpdateArticleDto
    - Mock DRIZZLE provider that returns a mock DB with chainable query builders
    - Helper functions: createMockArticle(), createMockCategory(), createMockTag(), createMockHistory()

    **Article service tests** (server/test/article/article.service.spec.ts):
    - describe('ArticleService')
    - Test toApiResponse: verify all 30+ fields map correctly, Sqids encoding works, dates formatted, null handling for optional fields
    - Test create: verifies category/tag ID decoding, wordCount/readingTime calculation, repository call, history creation, count sync
    - Test update: verifies diffIDs calculation, count sync for added/removed categories/tags
    - Test delete: verifies soft delete and count decrement
    - Test list: verifies pagination and filter parameters
    - Test validateAbbrlink: verifies reserved path rejection, slash rejection, uniqueness check
    - Test calculatePostStats: Chinese character counting, English word counting, reading time calculation
    - Test diffIDs: added IDs, removed IDs, empty sets

    **Article controller tests** (server/test/article/article.controller.spec.ts):
    - describe('ArticleController')
    - Test route decorators: POST, GET, PUT, DELETE on correct paths
    - Test that @Public() is NOT on admin endpoints
    - Test that @Public() IS on public article endpoints (PublicArticleController)

    **PostCategory service tests** (server/test/post-category/post-category.service.spec.ts):
    - describe('PostCategoryService')
    - Test list: returns all non-deleted categories with Sqids IDs
    - Test create: verifies name uniqueness, slug generation
    - Test update: verifies Sqids ID decoding, field update
    - Test delete: verifies soft delete

    **PostTag service tests** (server/test/post-tag/post-tag.service.spec.ts):
    - describe('PostTagService')
    - Same pattern as PostCategory tests but for tags

    **ArticleHistory service tests** (server/test/article-history/article-history.service.spec.ts):
    - describe('ArticleHistoryService')
    - Test createHistory: verifies version increment, snapshot fields, old version cleanup
    - Test listHistory: verifies pagination and Sqids article ID decoding
    - Test getVersion: verifies specific version retrieval
    - Test compareVersions: verifies both versions returned
    - Test restoreVersion: verifies version data returned without modifying article
    - Test getHistoryCount: verifies count returned

    Follow the established test patterns from server/test/phase02-integration.spec.ts for module setup and mock injection.
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    All unit tests pass. Article service, controller, category service, tag service, and history service have comprehensive test coverage. Test fixtures provide consistent mock data.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run schema push and integration verification</name>
  <files>
    server/src/database/schemas/article-post-category-pivot.schema.ts,
    server/src/database/schemas/article-post-tag-pivot.schema.ts
  </files>
  <read_first>
    server/src/database/schemas/index.ts,
    server/drizzle.config.ts
  </read_first>
  <acceptance_criteria>
    - npx drizzle-kit push succeeds with all Phase 03 tables
    - npm run dev starts NestJS on port 8091
    - All Phase 03 routes respond (can be verified via curl or test)
  </acceptance_criteria>
  <action>
    Run final schema push and integration verification:

    1. Run npx drizzle-kit push to ensure all schema changes (junction tables from Plan 01) are applied
    2. Verify the application starts: npm run dev should start without errors
    3. Quick smoke test the endpoints (if application starts successfully, the routes are registered):
       - GET /api/post-categories should return { code: 200, data: [], message: "获取列表成功" }
       - GET /api/post-tags should return { code: 200, data: [], message: "获取列表成功" }
       - GET /api/public/articles should return { code: 200, data: { list: [], total: 0, page: 1, pageSize: 10 }, message: "获取列表成功" }
       - GET /api/articles should return 401 (requires auth)

    If any endpoint returns unexpected response, debug and fix before completing this plan.
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx drizzle-kit push 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -5 && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    All Phase 03 schemas are pushed to SQLite. Application starts successfully. All endpoints respond correctly. Test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment -> Application | Tests mock DB and external services; no real trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Tampering | Test fixtures leaking into production | low | accept | Test files are in test/ directory, not included in production build |
| T-03-SC | Tampering | npm installs (vitest) | high | mitigate | vitest already installed from Phase 01/02, no new packages |
</threat_model>

<verification>
- TypeScript compilation passes
- drizzle-kit push succeeds
- All unit tests pass: npx vitest run
- Application starts on port 8091
- Category/Tag CRUD endpoints respond
- Public article endpoints respond
- Admin article endpoints require auth
- History endpoints require auth
</verification>

<success_criteria>
- AppModule imports all Phase 03 modules
- Application starts successfully on port 8091
- All Phase 03 routes are registered and responding
- Unit tests pass for article, category, tag, and history services
- Integration smoke tests confirm endpoint functionality
- drizzle-kit push succeeds
</success_criteria>

<output>
Create `.planning/phases/03-article-category-tag/03-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Updated `AppModule` with Phase 03 module imports
- `server/test/helpers/article-fixtures.ts` — shared test fixtures
- `server/test/article/article.service.spec.ts` — article service unit tests
- `server/test/article/article.controller.spec.ts` — article controller unit tests
- `server/test/post-category/post-category.service.spec.ts` — category service unit tests
- `server/test/post-tag/post-tag.service.spec.ts` — tag service unit tests
- `server/test/article-history/article-history.service.spec.ts` — history service unit tests
