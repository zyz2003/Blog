---
phase: 14-features-verification
plan: 07
type: execute
wave: 4
depends_on:
  - 14-05
  - 14-06
files_modified:
  - server/test/phase14-verification/schedule-verification.spec.ts
  - server/test/phase14-verification/regression.spec.ts
autonomous: true
requirements:
  - CRON-01

must_haves:
  truths:
    - "All 7 cron jobs are registered with correct cron expressions matching Go schedule"
    - "4 dispatch-based jobs can be triggered programmatically"
    - "ScheduleService.runJob() executes jobs without errors"
    - "No startup log spam from ScheduleService (D-264)"
    - "Full Phase 14 regression test suite passes"
    - "All Phase 13 tests still pass (no regressions)"
  artifacts:
    - path: "server/test/phase14-verification/schedule-verification.spec.ts"
      provides: "Verification of schedule/cron jobs"
      contains: "describe.*Schedule.*verification"
    - path: "server/test/phase14-verification/regression.spec.ts"
      provides: "Full Phase 14 regression test validation"
      contains: "describe.*Phase 14.*Regression"
  key_links:
    - from: "server/src/schedule/schedule.service.ts"
      to: "server/src/schedule/jobs/*.ts"
      via: "ScheduleService orchestrates job execution"
      pattern: "runJob|executeJob"
---

<objective>
Verify Schedule/Cron jobs execute correctly and run full Phase 14 regression suite.

Per D-315: Verify 8+ job types execute correctly, no startup log spam (D-264).

Per RESEARCH: NestJS has 7 @Cron jobs + 4 dispatch-based jobs = 11 total. The 7 @Cron jobs match Go's cron schedule.

Purpose: Confirm all scheduled tasks work correctly and validate the entire Phase 14 test suite.
Output: Schedule verification tests + Phase 14 regression suite
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-features-verification/14-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@.planning/phases/13-content-verification/13-SUMMARY.md
@server/test/helpers/api-compat-helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify Schedule/Cron job registration and execution</name>
  <files>server/test/phase14-verification/schedule-verification.spec.ts</files>
  <read_first>
    - server/src/schedule/schedule.service.ts — NestJS ScheduleService (job orchestration, runJob method)
    - server/src/schedule/jobs/ — NestJS cron job implementations (all 7 @Cron + 4 dispatch-based jobs)
    - _go-backend-archive/pkg/handler/setting/handler.go — Go settings handler (backup schedule reference)
    - _go-backend-archive/internal/infra/router/router.go — Go router (cron schedule registration)
    - server/test/phase13-verification/article-verification.spec.ts — Phase 13 test pattern reference
    - server/test/helpers/api-compat-helpers.ts — Test helper functions
  </read_first>
  <behavior>
    - Test 1: ScheduleService is injectable and has runJob method
    - Test 2: ScheduledPublishJob can be executed (checks for due articles)
    - Test 3: StatisticsAggregationJob can be executed (aggregates visitor stats)
    - Test 4: SyncViewCountsJob can be executed (syncs article view counts)
    - Test 5: LinkHealthCheckJob can be executed (checks link health)
    - Test 6: ScheduledBackupJob can be executed (creates backup)
    - Test 7: No startup log spam (D-264) -- verify ScheduleService initializes cleanly
  </behavior>
  <action>
    Create `server/test/phase14-verification/schedule-verification.spec.ts`:

    **beforeAll:** createTestApp, get ScheduleService from app context.

    **Cron job verification approach:** Cannot wait for cron triggers in tests. Instead, call job methods directly via ScheduleService or inject individual job classes and call their execute/run methods.

    **7 @Cron jobs to verify (matching Go cron schedule):**
    1. ScheduledPublishJob (`* * * * *`, every minute): Inject and call execute(). Verify it checks for articles with scheduled_publish_time <= now. If no due articles, completes without error.
    2. StatisticsAggregationJob (`0 1 * * *`, 1 AM daily): Inject and call execute(). Verify it aggregates visitor stats without error.
    3. SyncViewCountsJob (`0 2 * * *`, 2 AM daily): Inject and call execute(). Verify it syncs view counts without error.
    4. CleanupAbandonedUploadsJob (`0 3 * * *`, 3 AM daily): Inject and call execute(). Verify it cleans up without error.
    5. LinkHealthCheckJob (`0 3 * * *`, 3 AM daily): Inject and call execute(). Verify it runs health check without error.
    6. ArticleHistoryCleanupJob (`30 3 * * *`, 3:30 AM daily): Inject and call execute(). Verify it cleans up history without error.
    7. ScheduledBackupJob (`0 4 * * *`, 4 AM daily): Inject and call execute(). Verify it creates backup without error.

    **4 dispatch-based jobs (triggered by service calls, not cron):**
    - ThumbnailGenerationJob: Triggered by file upload. Verify it is registered.
    - CommentNotificationJob: Triggered by comment creation. Verify it is registered.
    - LinkCleanupJob: Triggered by link operations. Verify it is registered.
    - CleanupOrphanedItemsJob: Triggered periodically. Verify it is registered.

    **Startup log spam verification (D-264):**
    - Verify ScheduleService initializes without excessive log output
    - The backfill is capped at 30 days per D-264
    - Cannot directly assert log output in tests, but verify the service initializes without throwing

    **Test structure:**
    - describe('Schedule/Cron Verification')
      - it('should have ScheduleService injectable')
      - it('should execute ScheduledPublishJob')
      - it('should execute StatisticsAggregationJob')
      - it('should execute SyncViewCountsJob')
      - it('should execute CleanupAbandonedUploadsJob')
      - it('should execute LinkHealthCheckJob')
      - it('should execute ArticleHistoryCleanupJob')
      - it('should execute ScheduledBackupJob')
      - it('should register all 11 job types')
      - it('should initialize without startup log spam per D-264')
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/schedule-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ScheduleService is injectable and has runJob method
    - ScheduledPublishJob execute() completes without error
    - StatisticsAggregationJob execute() completes without error
    - SyncViewCountsJob execute() completes without error
    - CleanupAbandonedUploadsJob execute() completes without error
    - LinkHealthCheckJob execute() completes without error
    - ArticleHistoryCleanupJob execute() completes without error
    - ScheduledBackupJob execute() completes without error
    - All 11 job types (7 @Cron + 4 dispatch-based) are registered
    - ScheduleService initializes cleanly (no startup log spam per D-264)
    - npx vitest run server/test/phase14-verification/schedule-verification.spec.ts exits 0
  </acceptance_criteria>
  <done>
    - All 7 @Cron jobs execute without errors
    - All 4 dispatch-based jobs are registered
    - ScheduleService initializes cleanly
    - No startup log spam per D-264
    - All schedule verification tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 14 regression test suite</name>
  <files>server/test/phase14-verification/regression.spec.ts</files>
  <read_first>
    - server/test/phase14-verification/link-verification.spec.ts — Link verification tests (from Plan 01)
    - server/test/phase14-verification/album-verification.spec.ts — Album verification tests (from Plan 02)
    - server/test/phase14-verification/storage-policy-verification.spec.ts — Storage-policy tests (from Plan 04)
    - server/test/phase14-verification/user-management-verification.spec.ts — User management tests (from Plan 04)
    - server/test/phase13-verification/ — Phase 13 test suite (cross-phase regression check)
    - .planning/phases/13-content-verification/13-SUMMARY.md — Phase 13 summary (baseline for regression)
  </read_first>
  <action>
    Create `server/test/phase14-verification/regression.spec.ts` as a meta-test that validates the entire Phase 14 test suite:

    **Regression test structure:**
    - Import and re-run a sampling of critical Phase 14 tests to confirm no regressions
    - Verify Phase 13 tests still pass (cross-phase regression check)
    - This is NOT a full re-run of all tests -- that would be too expensive
    - Instead, run a focused regression check on the fixed modules

    **Specific regression checks:**
    1. Link.id is still numeric after all other Phase 14 work
    2. Storage-policy dates are still ISO strings
    3. UserGroup.description is still empty string (not null)
    4. Album.fileHash is still present in response
    5. Phase 13 article tests still pass (no cross-phase regression)

    **Full suite run:**
    After creating the regression file, run the complete Phase 14 test suite:
    `cd server && npx vitest run test/phase14-verification/ --reporter=verbose --forceExit`

    Also verify Phase 13 tests are not broken:
    `cd server && npx vitest run test/phase13-verification/ --reporter=verbose --forceExit`

    And verify the existing api-compat tests are not broken:
    `cd server && npx vitest run test/api-compat/ --reporter=verbose --forceExit`

    **Test count expectation:**
    - Phase 14 verification: ~60-80 tests across 11 spec files
    - Phase 13 verification: 57 tests (should all still pass)
    - api-compat: 305+ tests (9 pre-existing failures acceptable per Phase 13 summary)

    **Create regression spec file with cross-cutting assertions:**
    - describe('Phase 14 Regression')
      - it('Link.id should be numeric in link responses')
      - it('Storage-policy dates should be ISO strings')
      - it('UserGroup.description should be empty string not null')
      - it('Album.fileHash should be present in response')
      - it('Phase 13 article tests should still pass')
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/ --forceExit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Link.id is still numeric in link responses (no regression from other Phase 14 work)
    - Storage-policy dates are still ISO strings (no regression)
    - UserGroup.description is still empty string not null (no regression)
    - Album.fileHash is still present in response (no regression)
    - npx vitest run server/test/phase14-verification/ --forceExit exits 0
    - npx vitest run server/test/phase13-verification/ --forceExit exits 0
    - Phase 14 test count is approximately 60-80 tests across 11 spec files
    - Phase 13 tests still pass (no cross-phase regressions)
  </acceptance_criteria>
  <done>
    - All Phase 14 verification tests pass (~60-80 tests)
    - Phase 13 tests still pass (no cross-phase regressions)
    - api-compat tests not broken by Phase 14 changes
    - Regression spec confirms all fixes are stable
  </done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NestJS internal | Cron jobs run with full system privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-11 | Denial of Service | Cron job execution blocking | low | accept | Jobs run sequentially, short duration for personal blog |
</threat_model>

<verification>
- Schedule tests pass: `cd server && npx vitest run test/phase14-verification/schedule-verification.spec.ts`
- Full Phase 14 suite passes: `cd server && npx vitest run test/phase14-verification/ --forceExit`
- Phase 13 still passes: `cd server && npx vitest run test/phase13-verification/ --forceExit`
</verification>

<success_criteria>
- All 7 @Cron jobs execute without errors
- All 4 dispatch-based jobs are registered
- No startup log spam per D-264
- Full Phase 14 regression suite passes (~60-80 tests)
- Phase 13 tests still pass (no cross-phase regressions)
- All fixes from Plans 01-04 are stable in regression
</success_criteria>

<output>
Create `.planning/phases/14-features-verification/14-07-SUMMARY.md` when done
</output>
