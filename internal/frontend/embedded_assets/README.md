# Embedded frontend assets placeholder

This directory is populated by `scripts/prepare-embedded-frontend.sh` before building release binaries.
The generated Next.js standalone files are intentionally ignored by git.