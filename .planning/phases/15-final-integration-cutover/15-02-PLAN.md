---
phase: 15-final-integration-cutover
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - server/test/phase15-verification/cross-module-integration.spec.ts
autonomous: true
requirements: [VERIFY-05, INTEGRATION-01]

must_haves:
  truths:
    - Full regression of Phase 13 + Phase 14 + api-compat test suites (approximately 561 tests) passes with zero failures
    - Cross-module integration tests verify multi-service request flows that individual module tests do not cover
    - Creating an article with categories/tags is visible in the public article list
    - Uploading a file and creating a direct link allows public access to the file
    - Posting a comment on an article makes it visible in the admin comment list
    - Creating a friend link makes it appear in the public link list
  artifacts:
    - server/test/phase15-verification/cross-module-integration.spec.ts
  key_links:
    - Cross-module tests use createTestApp from existing helpers, consistent with Phase 13-14 test patterns
    - Each cross-module test spans at least 2 different modules in a single request flow
---

<objective>
Run full regression suite and add cross-module integration tests.

Purpose: Per D-326, the full regression must pass (Phase 13 + Phase 14 + api-compat, approximately 561 tests). Per D-327, new cross-module integration tests go in server/test/phase15-verification/. These tests verify multi-service flows that individual module tests cannot catch.

Output: Full regression green; cross-module integration test file created and passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-final-integration-cutover/15-CONTEXT.md
@.planning/phases/15-final-integration-cutover/15-RESEARCH.md
@.planning/phases/15-final-integration-cutover/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression suite (Phase 13 + Phase 14 + api-compat)</name>
  <files></files>
  <read_first>
    server/vitest.config.ts
  </read_first>
  <action>
    Run the full regression test suite to confirm all 561 tests pass after the Wave 1 fixes. Delete data/anheyu.db before running to ensure a clean database state (the app auto-creates and seeds it on startup). Run the three test suites sequentially to avoid DB isolation issues:

    1. Delete test DB: `rm -f server/data/anheyu.db`
    2. Run Phase 13 tests: `cd server && npx vitest run test/phase13-verification/`
    3. Delete test DB again: `rm -f server/data/anheyu.db`
    4. Run Phase 14 tests: `cd server && npx vitest run test/phase14-verification/`
    5. Delete test DB again: `rm -f server/data/anheyu.db`
    6. Run api-compat tests: `cd server && npx vitest run test/api-compat/`

    If any test fails, investigate and fix before proceeding. Record the total test count and pass rate. If the DB delete approach causes issues (e.g., file locked), try running with a single invocation: `cd server && npx vitest run test/phase13-verification test/phase14-verification test/api-compat` — but note this may hit isolation failures. The per-suite approach with DB reset is more reliable per RESEARCH.md Pitfall 1.
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase13-verification test/phase14-verification test/api-compat</automated>
  </verify>
  <done>
    - All Phase 13 verification tests pass (approximately 57 tests)
    - All Phase 14 verification tests pass (approximately 190 tests)
    - All api-compat tests pass (approximately 314 tests)
    - Total: approximately 561 tests with zero failures
    - Test counts recorded for SUMMARY
  </done>
  <acceptance_criteria>
    - `cd server && npx vitest run test/phase13-verification/` exits with code 0
    - `cd server && npx vitest run test/phase14-verification/` exits with code 0
    - `cd server && npx vitest run test/api-compat/` exits with code 0
    - No test failures in any of the three suites
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add cross-module integration tests in phase15-verification/</name>
  <files>server/test/phase15-verification/cross-module-integration.spec.ts</files>
  <read_first>
    server/test/phase14-verification/regression.spec.ts
    server/test/helpers/api-compat-helpers.ts
    server/src/comment/comment-admin.controller.ts
    server/src/link/link.controller.ts
  </read_first>
  <behavior>
    - Test 1: Create article with category and tag, verify public article list includes it, verify public category list includes the category
    - Test 2: Upload a file, create a direct link from the file, verify public access to the direct link redirects or returns the file
    - Test 3: Post a comment on an article as a visitor, verify the comment appears in the admin comment list
    - Test 4: Create a friend link with APPROVED status, verify it appears in the public friend link list
  </behavior>
  <action>
    Create the directory server/test/phase15-verification/ and the file cross-module-integration.spec.ts.

    Import createTestApp, closeTestApp, assertSuccessResponse, assertPaginatedResponse, assertErrorResponse, TestContext from '../helpers/api-compat-helpers'. Import supertest.

    Use the same beforeAll/afterAll pattern as Phase 14 regression.spec.ts: createTestApp in beforeAll, closeTestApp in afterAll.

    Write 4 cross-module integration tests, each spanning at least 2 different service modules in a single request flow:

    **Test 1 — Article + Category + Public (3 modules):**
    - POST /api/post-categories to create a category (admin token)
    - POST /api/post-tags to create a tag (admin token)
    - POST /api/articles with categoryIds and tagIds referencing the created category and tag (admin token)
    - PUT /api/articles/:id with status: PUBLISHED to publish the article
    - GET /api/public/articles to verify the article appears in the public list (no auth)
    - GET /api/public/categories to verify the category appears in the public list (no auth)

    **Test 2 — File + Direct Link + Public (3 modules):**
    - POST /api/files/upload to upload a small test file (admin token, use .field() and .attach() for multipart)
    - POST /api/direct-links to create a direct link referencing the uploaded file (admin token)
    - GET /f/:publicId to verify public access to the direct link (no auth, expect redirect or file content)

    **Test 3 — Comment + Article + Admin (3 modules):**
    - POST /api/public/articles or reuse an existing published article
    - POST /api/comments to post a comment on the article as a visitor (no auth required for public comment endpoint, or use auth if admin-only)
    - GET /api/comments with admin token to verify the comment appears in the admin comment list
    - Verify the comment's articleId or article title matches the target article

    **Test 4 — Friend Link + Public (2 modules):**
    - POST /api/links to create a friend link with status APPROVED (admin token)
    - POST /api/link-categories to ensure a category exists (admin token, or reuse seeded category)
    - GET /api/public/links to verify the friend link appears in the public link list (no auth)

    Use ctx.ts (timestamp) for unique test data names. All assertions should use the shared assertSuccessResponse/assertPaginatedResponse helpers.
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase15-verification/</automated>
  </verify>
  <done>
    - server/test/phase15-verification/ directory created
    - cross-module-integration.spec.ts contains 4 tests covering cross-module flows
    - All 4 cross-module integration tests pass
    - Tests use existing helper patterns from api-compat-helpers.ts
  </done>
  <acceptance_criteria>
    - server/test/phase15-verification/cross-module-integration.spec.ts exists
    - File imports from '../helpers/api-compat-helpers' (createTestApp, closeTestApp, assertSuccessResponse, assertPaginatedResponse)
    - File has exactly 4 test cases (one per cross-module scenario)
    - `cd server && npx vitest run test/phase15-verification/` exits with code 0
    - All 4 tests pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → NestJS app | Test code sends HTTP requests to NestJS test app instance |
| Cross-module tests → Multiple services | Tests verify data flows across service boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Tampering | Test DB state leakage between suites | medium | mitigate | Delete data/anheyu.db between suite runs for isolation |
| T-15-04 | Denial of Service | Full regression consuming excessive resources | low | accept | Sequential suite execution prevents resource contention |
</threat_model>

<verification>
1. Run Phase 13 tests individually: `cd server && npx vitest run test/phase13-verification/`
2. Run Phase 14 tests individually: `cd server && npx vitest run test/phase14-verification/`
3. Run api-compat tests individually: `cd server && npx vitest run test/api-compat/`
4. Run Phase 15 cross-module tests: `cd server && npx vitest run test/phase15-verification/`
5. Combined: `cd server && npx vitest run test/phase13-verification test/phase14-verification test/api-compat test/phase15-verification`
</verification>

<success_criteria>
- All Phase 13 verification tests pass (approximately 57)
- All Phase 14 verification tests pass (approximately 190)
- All api-compat tests pass (approximately 314)
- 4 cross-module integration tests created and passing
- Total: approximately 565 tests with zero failures
</success_criteria>

<output>
Create `.planning/phases/15-final-integration-cutover/15-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Created: `server/test/phase15-verification/cross-module-integration.spec.ts` — 4 cross-module integration tests
