---
phase: 03-article-category-tag
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - server/src/article/article.module.ts
  - server/src/article/article.controller.ts
  - server/src/article/article.service.ts
  - server/src/article/article.repository.ts
  - server/src/article/dto/create-article.dto.ts
  - server/src/article/dto/update-article.dto.ts
  - server/src/article/dto/article-response.dto.ts
  - server/src/post-category/post-category.repository.ts
  - server/src/post-tag/post-tag.repository.ts
  - server/src/common/constants/error-codes.ts
  - server/package.json (add isomorphic-dompurify + @types/dompurify)
  - server/src/article/article.sanitize.ts (DOMPurify sanitization utility)
autonomous: true
requirements: [ARTICLE-01]

must_haves:
  truths:
    - POST /api/articles creates an article with title (required), status, content, categories, tags and returns ArticleResponse
    - PUT /api/articles/:id updates an article by Sqids ID and returns ArticleResponse
    - DELETE /api/articles/:id soft-deletes an article and syncs category/tag count fields
    - GET /api/articles returns paginated admin article list with status/category/tag filters
    - GET /api/articles/:id returns single article by Sqids ID for admin
    - Article response includes nested postCategories and postTags arrays with Sqids-encoded IDs
    - wordCount and readingTime auto-calculated on create/update from Markdown content
    - Category/tag count fields sync correctly on article CRUD (diffIDs logic)
    - content_html sanitized via isomorphic-dompurify before storage (D-70)
  artifacts:
    - server/src/article/article.module.ts
    - server/src/article/article.controller.ts
    - server/src/article/article.service.ts
    - server/src/article/article.repository.ts
    - server/src/article/dto/create-article.dto.ts
    - server/src/article/dto/update-article.dto.ts
    - server/src/article/dto/article-response.dto.ts
  key_links:
    - ArticleRepository junction table queries for category/tag associations
    - ArticleService.toApiResponse produces Go-compatible response shape
    - Category/tag count sync on create/update/delete

---

<objective>
Implement article admin CRUD with Go-compatible response shape and category/tag count sync.

Purpose: Per D-45, article response must exactly match Go backend ArticleResponse with 30+ fields including nested postCategories, postTags, owner. Per D-46, Create/Update DTOs are separate. Per D-58, category/tag count auto-syncs on article CRUD. Per D-66, wordCount/readingTime auto-calculated.

Output: Fully functional article admin CRUD endpoints with Go-compatible response format.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-article-category-tag/03-CONTEXT.md
@.planning/phases/03-article-category-tag/03-RESEARCH.md
@server/src/database/schemas/article.schema.ts
@server/src/database/schemas/article-post-category-pivot.schema.ts
@server/src/database/schemas/article-post-tag-pivot.schema.ts
@server/src/database/schemas/post-category.schema.ts
@server/src/database/schemas/post-tag.schema.ts
@server/src/database/schemas/user.schema.ts
@server/src/common/utils/sqids.util.ts
@server/src/common/utils/time.util.ts
@server/src/common/constants/error-codes.ts
@pkg/service/article/service.go
@pkg/handler/article/handler.go
@pkg/domain/model/article.go
@ent/schema/article.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create article DTOs and repository with junction table queries</name>
  <files>
    server/src/article/dto/create-article.dto.ts,
    server/src/article/dto/update-article.dto.ts,
    server/src/article/dto/article-response.dto.ts,
    server/src/article/article.repository.ts,
    server/src/common/constants/error-codes.ts
  </files>
  <read_first>
    server/src/database/schemas/article.schema.ts,
    server/src/database/schemas/article-post-category-pivot.schema.ts,
    server/src/database/schemas/article-post-tag-pivot.schema.ts,
    server/src/database/schemas/post-category.schema.ts,
    server/src/database/schemas/post-tag.schema.ts,
    server/src/database/schemas/user.schema.ts,
    server/src/common/constants/error-codes.ts,
    pkg/domain/model/article.go (lines 84-156 for CreateArticleRequest and UpdateArticleRequest),
    pkg/service/article/service.go (lines 526-543 for calculatePostStats, lines 208-272 for diffIDs)
  </read_first>
  <behavior>
    - CreateArticleDto accepts title (required), status (DRAFT/PUBLISHED/ARCHIVED/SCHEDULED per D-47), content_md, content_html, cover_url, post_tag_ids (array of Sqids strings), post_category_ids (array of Sqids strings), show_on_home, home_sort, pin_sort, top_img_url, summaries (array of strings), primary_color, is_primary_color_manual, abbrlink, copyright, is_reprint, copyright_author, copyright_author_href, copyright_url, keywords, scheduled_at, extra_config, is_doc, doc_series_id, doc_sort
    - UpdateArticleDto is PartialType of CreateArticleDto (all fields optional)
    - ArticleRepository.findByIdWithRelations returns article with joined categories and tags
    - ArticleRepository.createWithAssociations creates article + junction table entries in transaction
    - ArticleRepository.updateWithAssociations updates article + syncs junction table entries in transaction
    - ArticleRepository.softDelete sets deletedAt timestamp
    - calculatePostStats counts Chinese characters via regex and English words, returns wordCount and readingTime
    - diffIDs computes added/removed category/tag IDs for count sync
  </behavior>
  <action>
    Per D-46, create separate CreateArticleDto and UpdateArticleDto.

    **CreateArticleDto** (dto/create-article.dto.ts):
    - Match Go CreateArticleRequest JSON fields exactly (snake_case)
    - title: @IsString() @IsNotEmpty()
    - status: @IsOptional() @IsIn(['DRAFT', 'PUBLISHED', 'ARCHIVED', 'SCHEDULED']) default 'DRAFT' — include SCHEDULED per Go enum values and D-47 clarification that SCHEDULED must be accepted
    - content_md: @IsOptional() @IsString()
    - content_html: @IsOptional() @IsString()
    - cover_url: @IsOptional() @IsString()
    - post_tag_ids: @IsOptional() @IsArray() @IsString({ each: true })
    - post_category_ids: @IsOptional() @IsArray() @IsString({ each: true })
    - ip_location: @IsOptional() @IsString()
    - show_on_home: @IsOptional() @IsBoolean() default true
    - home_sort: @IsOptional() @IsInt() default 0
    - pin_sort: @IsOptional() @IsInt() default 0
    - top_img_url: @IsOptional() @IsString()
    - summaries: @IsOptional() @IsArray() @IsString({ each: true })
    - primary_color: @IsOptional() @IsString() default '#b4bfe2'
    - is_primary_color_manual: @IsOptional() @IsBoolean() default false
    - abbrlink: @IsOptional() @IsString()
    - copyright: @IsOptional() @IsBoolean() default true
    - is_reprint: @IsOptional() @IsBoolean() default false
    - copyright_author, copyright_author_href, copyright_url: @IsOptional() @IsString()
    - keywords: @IsOptional() @IsString()
    - scheduled_at: @IsOptional() @IsString()
    - extra_config: @IsOptional() — JSON object
    - is_doc: @IsOptional() @IsBoolean() default false
    - doc_series_id: @IsOptional() @IsString()
    - doc_sort: @IsOptional() @IsInt() default 0

    **UpdateArticleDto** (dto/update-article.dto.ts):
    - PartialType of CreateArticleDto (all fields optional pointers)
    - Do NOT use @IsNotEmpty on any field — updates can set fields to empty/null

    **ArticleResponseDto** (dto/article-response.dto.ts):
    - TypeScript interface matching Go ArticleResponse structure exactly (reference: pkg/domain/model/article.go lines 159-211)
    - All JSON keys in snake_case: id, created_at, updated_at, title, content_md, content_html, cover_url, status, view_count, word_count, reading_time, ip_location, primary_color, is_primary_color_manual, show_on_home, post_tags, post_categories, home_sort, pin_sort, top_img_url, summaries, abbrlink, copyright, is_reprint, copyright_author, copyright_author_href, copyright_url, keywords, scheduled_at, review_status, owner_id, owner_nickname, owner_avatar, is_takedown, extra_config, is_doc, doc_series_id, doc_sort

    **ArticleRepository** (article.repository.ts):
    - @Injectable with @Inject(DRIZZLE)
    - findById(dbId): select from articles where id=dbId and deletedAt isNull
    - findByIdWithRelations(dbId): select article + left join article_post_categories + left join article_post_tags + left join post_categories + left join post_tags. Return article with nested categories[] and tags[]
    - list(options: { page, pageSize, query?, status?, categoryName?, tagName? }): paginated select with filters. Join post_categories/post_tags when filtering by name
    - createWithAssociations(data, categoryDbIds, tagDbIds): Drizzle transaction — insert article, then batch insert into article_post_categories and article_post_tags junction tables
    - updateWithAssociations(dbId, data, categoryDbIds, tagDbIds): Drizzle transaction — update article, delete old junction entries, insert new junction entries
    - softDelete(dbId): update deletedAt = current timestamp where id = dbId
    - getArticleCategories(dbId): select from article_post_categories join post_categories for a given article
    - getArticleTags(dbId): select from article_post_tags join post_tags for a given article
    - All list queries must filter isNull(articles.deletedAt)

    **Helper function** (in article.repository.ts or separate utils file):
    - calculatePostStats(content: string): { wordCount, readingTime } — matches Go service.go lines 526-543. Count Chinese chars with regex range, count English words by splitting on whitespace. wordsPerMinute = 200. readingTime = Math.max(1, Math.ceil(wordCount / 200)) when wordCount > 0, else 0
    - diffIDs(oldIds: number[], newIds: number[]): { inc: number[], dec: number[] } — matches Go diffIDs. Set difference: inc = newIds not in oldSet, dec = oldIds not in newSet

    **Error codes** (server/src/common/constants/error-codes.ts):
    - Add: ARTICLE_NOT_FOUND, ABBRLINK_CONFLICT, ABBRLINK_INVALID, ARTICLE_CREATE_FAILED, ARTICLE_UPDATE_FAILED
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    Article DTOs match Go CreateArticleRequest/UpdateArticleRequest fields. ArticleRepository handles all queries including junction table operations in transactions. calculatePostStats and diffIDs utility functions exist. TypeScript compilation passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ArticleService with toApiResponse and CRUD logic</name>
  <files>
    server/src/article/article.service.ts,
    server/src/post-category/post-category.repository.ts,
    server/src/post-tag/post-tag.repository.ts
  </files>
  <read_first>
    server/src/article/article.repository.ts,
    server/src/article/dto/create-article.dto.ts,
    server/src/article/dto/update-article.dto.ts,
    server/src/article/dto/article-response.dto.ts,
    server/src/common/utils/sqids.util.ts,
    server/src/common/utils/time.util.ts,
    server/src/database/schemas/article.schema.ts,
    server/src/database/schemas/post-category.schema.ts,
    server/src/database/schemas/post-tag.schema.ts,
    server/src/post-category/post-category.repository.ts,
    server/src/post-tag/post-tag.repository.ts,
    pkg/service/article/service.go (lines 616-692 for ToAPIResponse, lines 729-755 for fillOwnerInfo)
  </read_first>
  <behavior>
    - toApiResponse maps article DB row to Go-compatible ArticleResponse with Sqids-encoded IDs, nested postCategories/postTags arrays, snake_case keys, null for missing optional fields (not undefined)
    - create() decodes Sqids IDs from post_category_ids/post_tag_ids, calculates wordCount/readingTime, sanitizes content_html via isomorphic-dompurify per D-70, validates abbrlink uniqueness, creates article with associations, creates version=1 history, syncs category/tag count+1
    - update() decodes Sqids IDs, sanitizes content_html via isomorphic-dompurify per D-70, recalculates wordCount/readingTime, validates abbrlink if changed, updates article with associations, creates new history version, syncs category/tag counts via diffIDs
    - delete() soft-deletes article, syncs category/tag count-1 for all associated categories/tags
    - get() returns single article by public ID with nested categories and tags
    - list() returns paginated articles with filters
    - validateAbbrlink checks: length limit, no slashes, allowed chars, reserved paths, uniqueness
  </behavior>
  <action>
    Per D-45, D-46, D-58, D-63, D-66 implement ArticleService.

    **ArticleService** (article.service.ts):
    - @Injectable with injected ArticleRepository, PostCategoryRepository (for count sync), PostTagRepository (for count sync)
    - toApiResponse(article, useAbbrlinkAsID: boolean, includeHTML: boolean) — matches Go service.go ToAPIResponse exactly:
      - responseID = useAbbrlinkAsID && article.abbrlink ? article.abbrlink : generatePublicID(article.id, EntityType.Article)
      - effectiveTopImgUrl = article.topImgUrl || article.coverUrl (matches Go lines 634-637)
      - All 30+ fields mapped from article DB row to snake_case response keys
      - postTags array: each tag mapped to { id: generatePublicID(tag.id, EntityType.PostTag), created_at, updated_at, name, slug, count }
      - postCategories array: each category mapped to { id: generatePublicID(cat.id, EntityType.PostCategory), created_at, updated_at, name, slug, description, count, is_series }
      - doc_series_id: encode via generatePublicID if non-null
      - Dates formatted via formatToChinaTime
      - Optional fields set to null (not undefined) when empty (per Pitfall 3 in RESEARCH.md)

    - create(dto, ownerId):
      1. Decode post_category_ids from Sqids to dbIds (validate EntityType.PostCategory)
      2. Decode post_tag_ids from Sqids to dbIds (validate EntityType.PostTag)
      3. Calculate wordCount/readingTime from content_md using calculatePostStats
      4. Sanitize content_html via DOMPurify.sanitize(dto.content_html) per D-70 — if content_html is provided, run it through isomorphic-dompurify before storage to strip XSS vectors
      5. Validate abbrlink uniqueness if provided (call validateAbbrlink)
      6. Create article via repository.createWithAssociations
      7. Create article history version=1 (call ArticleHistoryService or direct repo call — defer to Plan 04, for now create a placeholder method)
      8. Sync category count +1 for each associated category (call PostCategoryRepository.incrementCount)
      9. Sync tag count +1 for each associated tag (call PostTagRepository.incrementCount)
      10. Return toApiResponse(createdArticle, false, true)

    - update(publicId, dto, ownerId):
      1. Decode publicId to dbId (validate EntityType.Article)
      2. Fetch existing article with relations
      3. If post_category_ids provided: decode to dbIds, compute diffIDs with existing category dbIds
      4. If post_tag_ids provided: decode to dbIds, compute diffIDs with existing tag dbIds
      5. Recalculate wordCount/readingTime if content_md changed
      6. Sanitize content_html via DOMPurify.sanitize(dto.content_html) per D-70 — if content_html is provided in the update, sanitize before storage
      7. Validate abbrlink if changed
      8. Update article via repository.updateWithAssociations
      9. Create new article history version (placeholder — defer to Plan 04)
      10. Sync category/tag counts: inc categories/tags in diffIDs.inc, dec categories/tags in diffIDs.dec
      11. Return toApiResponse(updatedArticle, false, true)

    - delete(publicId):
      1. Decode publicId to dbId
      2. Fetch article with relations to get associated category/tag IDs
      3. Soft delete via repository.softDelete
      4. Sync category count -1 for each associated category
      5. Sync tag count -1 for each associated tag
      6. Return null (success message handled by interceptor)

    - get(publicId):
      1. Decode publicId to dbId
      2. Fetch article with relations
      3. Return toApiResponse(article, false, true)

    - list(options):
      1. Fetch paginated articles via repository.list
      2. Map each to toApiResponse(article, false, true)
      3. Return { list: articleResponses, total, page, pageSize } matching Go ArticleListResponse

    - validateAbbrlink(abbrlink, excludeArticleDbId?): matches Go validateAbbrlink:
      - Check length <= 255
      - Check no forward slashes
      - Check allowed chars (alphanumeric, hyphens, underscores)
      - Check not in reserved paths list: ['admin', 'api', 'login', 'register', 'posts', 'pages', 'public', 'rss', 'feed', 'atom', 'sitemap', 'robots', 'search', 'comments', 'albums', 'links', 'music', 'docs']
      - Check uniqueness in database (exclude current article if updating)

    **Count sync methods** (add to PostCategoryRepository and PostTagRepository in this task):
    - incrementCount(dbId): UPDATE post_categories SET count = count + 1 WHERE id = dbId
    - decrementCount(dbId): UPDATE post_categories SET count = count - 1 WHERE id = dbId AND count > 0
    - Same methods for PostTagRepository
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    ArticleService implements toApiResponse matching Go ArticleResponse shape exactly. CRUD operations handle Sqids ID decoding, category/tag association management, count sync, and wordCount/readingTime calculation. TypeScript compilation passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install isomorphic-dompurify and implement HTML sanitization utility (D-70)</name>
  <files>
    server/package.json,
    server/src/article/article.sanitize.ts
  </files>
  <read_first>
    server/src/article/article.service.ts,
    pkg/service/article/service.go (SanitizeHTML usage in Create/Update)
  </read_first>
  <behavior>
    - isomorphic-dompurify installed as npm dependency
    - article.sanitize.ts exports sanitizeHtml() function that wraps DOMPurify.sanitize()
    - ArticleService.create() and update() call sanitizeHtml() on content_html before storage
  </behavior>
  <action>
    Install packages: `cd D:/CodeDevelopment/project/Blog/server && npm install isomorphic-dompurify @types/dompurify`

    Create server/src/article/article.sanitize.ts:
    - Import DOMPurify from 'isomorphic-dompurify'
    - Export function sanitizeHtml(html: string): string — returns DOMPurify.sanitize(html, { ALLOWED_TAGS: Go's allowed tag list, ALLOWED_ATTR: Go's allowed attribute list })
    - If html is null/undefined/empty, return null (matches Go nil behavior per RESEARCH Pitfall 3)

    In article.service.ts, add import for sanitizeHtml from './article.sanitize'
    - In create() method: dto.contentHtml = sanitizeHtml(dto.contentHtml) before DB insert
    - In update() method: if dto.contentHtml provided, sanitizeHtml(dto.contentHtml) before DB update
  </action>
  <acceptance_criteria>
    - server/package.json contains "isomorphic-dompurify" in dependencies
    - server/package.json contains "@types/dompurify" in devDependencies
    - server/src/article/article.sanitize.ts contains export function sanitizeHtml(html: string): string
    - article.service.ts create() method calls sanitizeHtml(dto.contentHtml) before storage
    - article.service.ts update() method calls sanitizeHtml(dto.contentHtml) before storage when contentHtml is provided
  </acceptance_criteria>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    isomorphic-dompurify installed and sanitizeHtml utility created. ArticleService sanitizes content_html on Create/Update per D-70. TypeScript compilation passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement ArticleController and wire ArticleModule</name>
  <files>
    server/src/article/article.controller.ts,
    server/src/article/article.module.ts
  </files>
  <read_first>
    server/src/article/article.service.ts,
    server/src/auth/auth.controller.ts,
    server/src/common/decorators/public.decorator.ts,
    server/src/common/decorators/current-user.decorator.ts,
    server/src/common/guards/admin.guard.ts,
    pkg/handler/article/handler.go (lines 248-598 for CRUD handlers),
    internal/infra/router/router.go (lines 346-391 for route registration)
  </read_first>
  <behavior>
    - POST /api/articles creates article with JWT auth; ownerId extracted from @CurrentUser()
    - PUT /api/articles/:id updates article with JWT auth
    - DELETE /api/articles/:id soft-deletes with JWT auth
    - GET /api/articles returns paginated list with JWT auth; supports query, status, category, tag filters
    - GET /api/articles/:id returns single article with JWT auth
    - POST /api/articles/upload returns 501 (Phase 05 dependency)
    - POST /api/articles/primary-color returns default color (Phase 05 will add auto extract)
    - POST /api/articles/export returns 501 stub (complex, lower priority)
    - POST /api/articles/import returns 501 stub
    - DELETE /api/articles/batch returns 501 stub
  </behavior>
  <action>
    Per D-60, ArticleController handles both admin and public endpoints. Admin endpoints first (public endpoints in Plan 03).

    **ArticleController** (article.controller.ts):
    - @Controller('articles') — NestJS auto-prefixes to /api/articles via global prefix in main.ts
    - Inject ArticleService

    Admin endpoints (JWT auth from global guard):
    - @Post() create(@Body() dto: CreateArticleDto, @CurrentUser() user) — decode user.id to get ownerId, call service.create(dto, ownerDbId)
    - @Get() list(@Query() query: { page?, pageSize?, query?, status?, category?, tag? }) — call service.list(options)
    - @Get(':id') get(@Param('id') publicId) — call service.get(publicId)
    - @Put(':id') update(@Param('id') publicId, @Body() dto: UpdateArticleDto, @CurrentUser() user) — call service.update(publicId, dto, ownerDbId)
    - @Delete(':id') delete(@Param('id') publicId) — call service.delete(publicId)

    Stubs for Phase 05 dependencies:
    - @Post('upload') uploadImage() — throw new HttpException('功能暂未实现', HttpStatus.NOT_IMPLEMENTED) — matches D-44 pattern for avatar upload
    - @Post('primary-color') getPrimaryColor(@Body() body: { image_url: string }) — return { primary_color: '#b4bfe2' } (default color, per D-67 Phase 03 scope)

    Stubs for complex features:
    - @Post('export') exportArticles() — throw new HttpException('功能暂未实现', HttpStatus.NOT_IMPLEMENTED)
    - @Post('import') importArticles() — throw new HttpException('功能暂未实现', HttpStatus.NOT_IMPLEMENTED)
    - @Delete('batch') batchDelete() — throw new HttpException('功能暂未实现', HttpStatus.NOT_IMPLEMENTED)

    **ArticleModule** (article.module.ts):
    - @Module with imports: [DatabaseModule, PostCategoryModule, PostTagModule]
    - controllers: [ArticleController]
    - providers: [ArticleService, ArticleRepository]
    - exports: [ArticleService]

    Note: PostCategoryModule and PostTagModule must export PostCategoryRepository and PostTagRepository (or ArticleRepository accesses DB directly for count sync). Simpler approach: ArticleRepository directly queries post_categories and post_tags tables for count sync, avoiding circular module dependencies. This means ArticleModule only imports DatabaseModule.
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    ArticleController provides admin CRUD endpoints at /api/articles. ArticleModule is wired with DatabaseModule. Stubs return 501 for Phase 05 dependent features. TypeScript compilation passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/articles (POST/PUT/DELETE) | Untrusted input; requires JwtAuth |
| client -> /api/articles (GET) | Requires JwtAuth; admin article list includes drafts |
| ArticleService -> PostCategory/Tag count sync | Internal trust boundary; must be atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | POST /api/articles content_html XSS | critical | mitigate | HTML sanitization via isomorphic-dompurify per D-70 (installed in Task 3, applied in Task 2) |
| T-03-05 | Tampering | POST /api/articles abbrlink path traversal | high | mitigate | validateAbbrlink checks for slashes, reserved paths per Go logic |
| T-03-06 | Information Disclosure | GET /api/articles/:id IDOR | medium | mitigate | Global JwtAuthGuard ensures only authenticated users access admin endpoints |
| T-03-07 | Tampering | POST /api/articles mass assignment | medium | mitigate | DTO whitelist with class-validator, no extra fields accepted |
</threat_model>

<verification>
- TypeScript compilation passes
- Article CRUD endpoints respond correctly
- toApiResponse produces Go-compatible JSON with snake_case keys
- Category/tag count syncs on create/update/delete
- wordCount/readingTime auto-calculated
- abbrlink validation works
- Stubs return 501 for upload/export/import/batch-delete
</verification>

<success_criteria>
- POST /api/articles creates article with all fields, returns ArticleResponse with nested categories/tags
- PUT /api/articles/:id updates article and syncs category/tag counts
- DELETE /api/articles/:id soft-deletes and decrements category/tag counts
- GET /api/articles returns paginated list with filters
- GET /api/articles/:id returns single article
- Article IDs are Sqids-encoded in all responses
- All JSON keys use snake_case matching Go backend
- Optional fields are null (not missing) in responses
- wordCount/readingTime calculated from Markdown content
</success_criteria>

<output>
Create `.planning/phases/03-article-category-tag/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `ArticleModule` class (rewritten from placeholder)
- `ArticleController` class with endpoints: create, list, get, update, delete, uploadImage(501), getPrimaryColor, exportArticles(501), importArticles(501), batchDelete(501)
- `ArticleService` class with methods: create, update, delete, get, list, toApiResponse, validateAbbrlink
- `ArticleRepository` class with methods: findById, findByIdWithRelations, list, createWithAssociations, updateWithAssociations, softDelete, getArticleCategories, getArticleTags
- `CreateArticleDto` class with 25+ fields matching Go CreateArticleRequest
- `UpdateArticleDto` class (PartialType of CreateArticleDto)
- `ArticleResponseDto` TypeScript interface
- `calculatePostStats` helper function
- `diffIDs` helper function
- Error codes: ARTICLE_NOT_FOUND, ABBRLINK_CONFLICT, ABBRLINK_INVALID, ARTICLE_CREATE_FAILED, ARTICLE_UPDATE_FAILED
- PostCategoryRepository.incrementCount/decrementCount methods
- PostTagRepository.incrementCount/decrementCount methods
