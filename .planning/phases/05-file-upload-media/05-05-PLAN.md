---
phase: 05-file-upload-media
plan: 05
type: execute
wave: 4
depends_on: [05-03, 05-04]
files_modified:
  - server/package.json
  - server/src/app.module.ts
  - server/src/article/article.controller.ts
  - server/src/article/article.module.ts
  - server/src/common/constants/error-codes.ts
autonomous: true
requirements: [FILE-01, FILE-02, THUMB-01, STORAGE-01, LINK-DIRECT-01]

must_haves:
  truths:
    - sharp and @nestjs/serve-static installed per RESEARCH Standard Stack
    - AppModule imports all Phase 05 modules (StoragePolicyModule, FileModule, ThumbnailModule, DirectLinkModule)
    - ServeStaticModule configured for data/uploads at /uploads path per D-114
    - ArticleController.uploadImage no longer returns 501 per D-113
    - ArticleController.uploadImage delegates to StoragePolicyService and ThumbnailService
    - All Phase 05 error codes present in error-codes.ts
    - No circular dependency errors in module resolution
    - Default storage policies auto-created on startup
  artifacts:
    - server/src/app.module.ts
    - server/src/article/article.controller.ts
    - server/src/article/article.module.ts
  key_links:
    - AppModule imports StoragePolicyModule, FileModule, ThumbnailModule, DirectLinkModule, ServeStaticModule
    - ArticleController.uploadImage uses FileInterceptor('file') + StoragePolicyService + ThumbnailService
    - Circular dependency between FileModule and ThumbnailModule resolved via forwardRef()
---

<objective>
Wire all Phase 05 modules into AppModule, complete the ArticleController.uploadImage 501 stub from Phase 03, configure @nestjs/serve-static for file serving, install sharp and @nestjs/serve-static packages, and run integration verification.

Purpose: This final plan integrates all Phase 05 modules, resolves circular dependencies, completes the article image upload stub, and verifies the full file management subsystem works end-to-end.

Output: Updated app.module.ts, article.controller.ts, article.module.ts, package.json
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-file-upload-media/05-CONTEXT.md
@.planning/phases/05-file-upload-media/05-RESEARCH.md
@server/src/app.module.ts
@server/src/article/article.controller.ts
@server/src/article/article.module.ts
@server/src/storage-policy/storage-policy.module.ts
@server/src/file/file.module.ts
@server/src/thumbnail/thumbnail.module.ts
@server/src/direct-link/direct-link.module.ts
@server/src/common/constants/error-codes.ts
@pkg/handler/image/handler.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install packages, wire AppModule, configure static file serving, complete article upload stub</name>
  <files>server/package.json, server/src/app.module.ts, server/src/article/article.controller.ts, server/src/article/article.module.ts</files>
  <read_first>
    server/src/app.module.ts
    server/src/article/article.controller.ts
    server/src/article/article.module.ts
    server/src/storage-policy/storage-policy.module.ts
    server/src/file/file.module.ts
    server/src/thumbnail/thumbnail.module.ts
    server/src/direct-link/direct-link.module.ts
    pkg/handler/image/handler.go
  </read_first>
  <behavior>
    - sharp and @nestjs/serve-static in package.json dependencies
    - AppModule imports StoragePolicyModule, FileModule, ThumbnailModule, DirectLinkModule
    - ServeStaticModule configured for data/uploads at /uploads path per D-114
    - ArticleController.uploadImage uses FileInterceptor('file') per D-113
    - ArticleController.uploadImage delegates to StoragePolicyService.findByFlag('article_image')
    - ArticleController.uploadImage creates entity + file records and generates thumbnail
    - ArticleModule imports StoragePolicyModule and ThumbnailModule
    - No circular dependency errors in module resolution
  </behavior>
  <action>
    **Install packages** (per RESEARCH Standard Stack):
    ```bash
    cd server && npm install sharp @nestjs/serve-static
    ```
    Verify versions: sharp v0.35.x, @nestjs/serve-static latest.

    **Update AppModule** (server/src/app.module.ts):
    - Add imports: StoragePolicyModule, DirectLinkModule (FileModule and ThumbnailModule are already imported as placeholders — update them to resolve to real modules)
    - Add ServeStaticModule configuration for data/uploads directory per D-114:
      ```typescript
      ServeStaticModule.forRoot({
        rootPath: join(__dirname, '..', '..', 'data', 'uploads'),
        serveRoot: '/uploads',
        serveStaticOptions: {
          maxAge: 86400000, // 24h cache
        },
      })
      ```
    - Import ServeStaticModule from @nestjs/serve-static and join from 'path'

    **Configure static file serving** to make uploaded files accessible:
    - The data/uploads directory serves at /uploads path
    - Thumbnails at data/uploads/thumbnails/ are accessible via the ThumbnailController signed URL mechanism (GET /api/t/:signedToken), not direct static access
    - Uploaded files are served via FileController download endpoints (GET /api/file/download/:id) or DirectLinkController short-link (GET /api/f/:publicID/*filename)
    - Static serving provides an additional direct URL for files that need it (e.g., image src in articles)

    **Complete ArticleController.uploadImage** (server/src/article/article.controller.ts):
    Replace the 501 stub with a real implementation per D-113:
    - Add @UseInterceptors(FileInterceptor('file')) decorator (import from @nestjs/platform-express)
    - Add @UploadedFile() file: Express.Multer.File parameter
    - Inject StoragePolicyService and ThumbnailService (via module imports)
    - Implementation:
      1. Get default article_image policy from StoragePolicyService.findByFlag('article_image')
      2. If no policy found, throw 500 (default should have been created on init)
      3. Generate a unique filename: `{timestamp}-{originalname}` to avoid collisions
      4. Ensure target directory exists: policy.basePath + /articles/
      5. Write uploaded file buffer to target path using fs.writeFile
      6. Create entity record in entities table (type='image_content', source=targetPath, mimeType=file.mimetype, size=file.size, policyId=policy.id)
      7. Create file record in files table (ownerId from user, name=originalname, type=1, primaryEntityId=entity.id, parentId=findOrCreate articles directory)
      8. Generate thumbnail via ThumbnailService.generateThumbnail (wrap in try-catch per D-106)
      9. Return { file_id: generatePublicID(file.id, EntityType.File), name: file.name, size: file.size } matching Go backend UploadImage response per RESEARCH Section 1

    **Update ArticleModule** (server/src/article/article.module.ts):
    - Add imports: StoragePolicyModule, ThumbnailModule (for article image upload to resolve policies and generate thumbnails)
    - Add MulterModule configuration if not already present
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>
  <done>
    - sharp and @nestjs/serve-static installed and in package.json dependencies
    - AppModule imports all Phase 05 modules (StoragePolicyModule, FileModule, ThumbnailModule, DirectLinkModule)
    - ServeStaticModule configured for data/uploads at /uploads path per D-114
    - ArticleController.uploadImage no longer returns 501 — it uploads file, creates records, generates thumbnail per D-113
    - ArticleModule imports StoragePolicyModule and ThumbnailModule
    - TypeScript compilation passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration verification, error codes audit, and circular dependency resolution</name>
  <files>server/src/common/constants/error-codes.ts</files>
  <read_first>
    server/src/common/constants/error-codes.ts
    server/src/app.module.ts
    server/src/file/file.module.ts
    server/src/thumbnail/thumbnail.module.ts
  </read_first>
  <behavior>
    - All Phase 05 error codes present in error-codes.ts
    - TypeScript compilation passes with zero errors
    - npm run dev starts successfully on port 8091
    - Default storage policies auto-created on startup
    - No circular dependency errors in module resolution
    - ArticleController.uploadImage returns proper response (not 501)
  </behavior>
  <action>
    **Audit error-codes.ts** for completeness. Verify all error codes added across Plans 05-01 through 05-04 are present:
    - STORAGE_POLICY_FLAG_CONFLICT, STORAGE_DEFAULT_POLICY_INIT_FAILED (Plan 01)
    - UPLOAD_SESSION_NOT_FOUND, UPLOAD_SESSION_EXPIRED, UPLOAD_SESSION_INVALID_CHUNK, UPLOAD_SESSION_NOT_OWNER, UPLOAD_FILE_EXISTS, UPLOAD_URI_INVALID, UPLOAD_TEMP_DIR_CLEANUP_FAILED (Plan 02)
    - FILE_NOT_FOUND, FILE_NAME_EXISTS, FILE_PARENT_NOT_FOUND, FILE_DELETE_FAILED, FILE_COPY_FAILED, FILE_MOVE_FAILED, FOLDER_NOT_FOUND, FOLDER_NOT_EMPTY, SIGNED_URL_INVALID, SIGNED_URL_EXPIRED (Plan 03)
    - THUMBNAIL_GENERATION_FAILED, THUMBNAIL_NOT_FOUND, THUMBNAIL_SIGN_EXPIRED, THUMBNAIL_SIGN_INVALID (Plan 04)
    - Pre-existing codes that Phase 05 uses: STORAGE_NOT_FOUND, STORAGE_CONFLICT, INVALID_POLICY_TYPE, POLICY_NOT_FOUND, POLICY_CONFLICT, POLICY_SETTINGS_INVALID, POLICY_NAME_CONFLICT, POLICY_NOT_SUPPORT_AUTH, POLICY_USED_BY_FILES, INVALID_PUBLIC_ID, SIGNATURE_INVALID, LINK_EXPIRED (verify these exist or add if missing)

    **Circular dependency resolution:**
    The circular dependency between FileModule and ThumbnailModule must be resolved:
    - FileModule provides UploadService which needs ThumbnailService.generateThumbnail (for post-upload thumbnail generation per D-103)
    - ThumbnailModule provides ThumbnailService which needs FileService/FileRepository (for file/entity lookups)
    - Resolution: Use forwardRef() in both modules:
      - In FileModule: imports: [forwardRef(() => ThumbnailModule)]
      - In ThumbnailModule: imports: [forwardRef(() => FileModule)]
      - In UploadService: @Inject(forwardRef(() => ThumbnailService))
    - Verify this resolves without NestJS circular dependency warnings at startup

    **Integration verification steps** (manual, documented here for execution):
    1. cd server && npx tsc --noEmit — TypeScript compilation passes with zero errors
    2. npm run dev — NestJS starts on port 8091 without module resolution errors
    3. Verify StoragePolicyModule initializes default policies on startup (check DB for article_image, comment_image, user_avatar policies)
    4. Verify FileModule, ThumbnailModule, DirectLinkModule register without circular dependency errors
    5. Test basic flow: POST /api/policies returns list with default policies, PUT /api/file/upload creates upload session
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>
  <done>
    - All Phase 05 error codes present in error-codes.ts
    - TypeScript compilation passes with zero errors
    - npm run dev starts successfully on port 8091
    - Default storage policies auto-created on startup
    - No circular dependency errors in module resolution
    - ArticleController.uploadImage returns proper response (not 501)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Package installation crosses trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm installs (sharp, @nestjs/serve-static) | high | mitigate | Both packages are well-established with high download counts per RESEARCH Package Audit |
</threat_model>

<verification>
- TypeScript compilation: cd server && npx tsc --noEmit
- Package installed: grep -c '"sharp"' server/package.json
- Serve static: grep -c '"@nestjs/serve-static"' server/package.json
- Error codes count: grep -c 'UPLOAD_SESSION_NOT_FOUND\|THUMBNAIL_GENERATION_FAILED\|FILE_NOT_FOUND\|STORAGE_POLICY_FLAG_CONFLICT' server/src/common/constants/error-codes.ts
</verification>

<success_criteria>
- sharp and @nestjs/serve-static installed
- AppModule imports all Phase 05 modules
- ServeStaticModule configured for data/uploads
- ArticleController.uploadImage works (no longer 501)
- All error codes present in error-codes.ts
- cd server && npx tsc --noEmit passes
- npm run dev starts on port 8091
- Default storage policies created on startup
- No circular dependency errors
</success_criteria>

<output>
Create `.planning/phases/05-file-upload-media/05-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Updated `app.module.ts` — imports all Phase 05 modules + ServeStaticModule
- Updated `article.controller.ts` — uploadImage with FileInterceptor + StoragePolicyService + ThumbnailService
- Updated `article.module.ts` — imports StoragePolicyModule, ThumbnailModule
- Updated `package.json` — sharp, @nestjs/serve-static dependencies
- Verified error-codes.ts with all Phase 05 codes
