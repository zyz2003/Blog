---
phase: 02-auth-settings
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - server/src/app.module.ts
  - server/src/main.ts
  - server/test/phase02-integration.spec.ts
  - server/src/common/constants/error-codes.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, USER-01, SETTING-01, SETTING-02, API-COMPAT-03, API-COMPAT-06]

must_haves:
  truths:
    - AppModule imports all Phase 02 modules in correct order
    - NestJS app starts on port 8091 with all Phase 02 modules loaded
    - Sqids encoder initialized with seed from settings table at startup
    - SettingsService loads cache before any auth operation
    - All Phase 02 endpoints return Go-compatible response format
    - Integration tests verify end-to-end auth flow (login -> get user info -> refresh token)
    - New error codes added for auth-specific errors
  artifacts:
    - server/test/phase02-integration.spec.ts
    - Updated server/src/app.module.ts
    - Updated server/src/main.ts
  key_links:
    - AppModule startup sequence: SettingsService.onModuleInit() loads cache -> Sqids init with id_seed -> all modules ready
    - All feature modules registered and guards/interceptors working together
    - End-to-end auth flow verified by integration tests
---

<objective>
Wire all Phase 02 modules into AppModule, ensure startup sequence initializes SettingsService and Sqids before auth operations, and create integration tests verifying the complete auth flow.

Purpose: This plan ensures all Phase 02 modules work together correctly, the startup sequence is right, and the full auth flow (login -> user info -> refresh) is verified end-to-end.

Output: Fully wired AppModule with Phase 02 modules, startup initialization, and passing integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-settings/02-CONTEXT.md
@.planning/phases/02-auth-settings/02-RESEARCH.md
@.planning/phases/02-auth-settings/02-01-SUMMARY.md
@.planning/phases/02-auth-settings/02-02-SUMMARY.md
@.planning/phases/02-auth-settings/02-03-SUMMARY.md
@.planning/phases/02-auth-settings/02-04-SUMMARY.md
@server/src/app.module.ts
@server/src/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppModule wiring, startup initialization, and error codes</name>
  <files>server/src/app.module.ts, server/src/main.ts, server/src/common/constants/error-codes.ts</files>
  <behavior>
    - Test 1: AppModule imports SettingsModule, AuthModule, UserModule, CaptchaModule
    - Test 2: NestJS app starts without errors on port 8091
    - Test 3: SettingsService cache is loaded before any HTTP request
    - Test 4: Sqids encoder initialized with id_seed from settings table
    - Test 5: Auth endpoints return Go-compatible error messages
  </behavior>
  <action>
    Update error-codes.ts to add Phase 02 error messages:
    - LOGIN_FAILED: '邮箱或密码错误'
    - USER_NOT_ACTIVATED: '用户未激活'
    - USER_BANNED: '用户已被封禁'
    - OLD_PASSWORD_INCORRECT: '旧密码不正确'
    - CAPTCHA_REQUIRED: '验证码参数缺失'
    - CAPTCHA_EXPIRED: '验证码已过期'
    - CAPTCHA_INCORRECT: '验证码错误'
    - REFRESH_TOKEN_MISSING: '请求未携带Token，无权限访问'
    - USER_NOT_FOUND: '用户不存在'

    Update AppModule:
    - Import and add: UserModule, CaptchaModule (SettingsModule already imported, AuthModule already imported)
    - Import and add ThrottlerModule from @nestjs/throttler: ThrottlerModule.forRoot([{ name: 'default', ttl: 60000, limit: 100 }]) (global default: 100 requests per minute, override per-endpoint with @Throttle)
    - Add ThrottlerGuard as APP_GUARD before JwtAuthGuard (for rate limiting). NestJS supports multiple APP_GUARD providers; they execute in registration order. ThrottlerGuard should be listed before JwtAuthGuard so rate limiting runs before auth.

    Update main.ts to ensure startup initialization:
    - After app.listen(), the app bootstrap should trigger SettingsService.onModuleInit() automatically via NestJS lifecycle hooks
    - Ensure ValidationPipe is registered globally: app.useGlobalPipes(new ValidationPipe({ transform: true, whitelist: true }))
    - Add app.enableCors() if needed for frontend proxy

    Create an AppInitializer service that handles startup sequence:
    - OnModuleInit lifecycle hook
    - Get SettingsService from app reference
    - Call settingsService.get('id_seed') to get Sqids seed
    - Call initSqidsEncoderWithSeed(seed) to initialize Sqids encoder
    - This must run BEFORE any auth operation
    - Register AppInitializer as a provider in AppModule or create an InitModule

    Alternatively, add the Sqids initialization to the existing main.ts bootstrap:
    - After app.init(), get SettingsService from app.get(SettingsService)
    - Get id_seed from settingsService.get('id_seed')
    - Call initSqidsEncoderWithSeed(seed)
    - This is simpler and guaranteed to run before any HTTP request
  </action>
  <verify>
    <automated>cd server && npx vitest run --reporter=verbose phase02-integration</automated>
  </verify>
  <done>
    - AppModule imports all Phase 02 modules
    - NestJS app starts on port 8091 with all modules loaded
    - SettingsService cache loaded at startup via OnModuleInit
    - Sqids encoder initialized with id_seed from settings table
    - ValidationPipe registered globally
    - ThrottlerModule configured with per-endpoint limits
    - New error codes added for Phase 02
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for complete Phase 02 auth flow</name>
  <files>server/test/phase02-integration.spec.ts</files>
  <behavior>
    - Test 1: POST /api/auth/login with correct credentials returns { userInfo, roles, accessToken, refreshToken, expires }
    - Test 2: userInfo.id is public ID string, userInfo.userGroupID is raw database ID number
    - Test 3: GET /api/user/info with valid JWT returns user profile
    - Test 4: POST /api/auth/refresh-token with valid refresh token returns { accessToken, expires }
    - Test 5: Go-issued JWT token is accepted by NestJS JwtStrategy
    - Test 6: GET /api/public/site-config returns unflattened public settings
    - Test 7: GET /api/public/captcha/config returns provider config
    - Test 8: POST /api/settings/get-by-keys with admin user returns all keys
    - Test 9: POST /api/settings/get-by-keys with non-admin user returns only public keys
    - Test 10: GET /api/public/site-config/version returns { version: number }
    - Test 11: Admin endpoints protected by AdminGuard
    - Test 12: Login with wrong password returns 401
    - Test 13: Access protected endpoint without JWT returns 401
    - Test 14: bcryptjs.verify with Go-hashed password succeeds
  </behavior>
  <action>
    Create server/test/phase02-integration.spec.ts with end-to-end integration tests.

    The test file should:
    - Use Test.createTestingModule with AppModule to create a full application
    - Seed the database with test data: admin user (userGroupId=1), regular user, user groups, settings (including JWT_SECRET, id_seed, captcha.provider, APP_NAME, etc.)
    - Use the NestJS application's HTTP adapter or supertest to make actual HTTP requests

    Test setup:
    - Create test SQLite database in memory or temp file
    - Insert test data: admin user with bcryptjs.hash("password123"), user_group with id=1 (admin), settings entries (JWT_SECRET, id_seed, APP_NAME, captcha.provider, GRAVATAR_URL, etc.)
    - Initialize Sqids with test seed

    Test scenarios:
    1. Login flow: POST /api/auth/login with admin credentials, verify response structure
    2. User info: GET /api/user/info with JWT from login, verify userInfo structure and ID types
    3. Refresh token: POST /api/auth/refresh-token with refreshToken from login, verify new accessToken
    4. Go JWT compatibility: Create a JWT token using the same algorithm as Go (HS256, same payload structure) and verify NestJS accepts it
    5. Public config: GET /api/public/site-config, verify unflattened structure and _config_version
    6. Captcha config: GET /api/public/captcha/config, verify response
    7. Settings access control: POST /api/settings/get-by-keys as admin vs non-admin
    8. Config version: GET /api/public/site-config/version
    9. Error cases: wrong password, missing JWT, expired token, admin-only endpoints

    For Go JWT compatibility test (AUTH-03):
    - Use jsonwebtoken to sign a token with the same payload structure as Go:
      { user_id: generatePublicID(1, EntityType.User), user_group_id: generatePublicID(1, EntityType.UserGroup), permissions: [0,1,2,3], iss: "anheyu-app", iat: ..., exp: ..., nbf: ... }
    - Verify this token is accepted by the NestJS JwtStrategy

    For bcryptjs compatibility test:
    - Generate a hash using bcryptjs.hash("test123") and verify bcryptjs.compare("test123", hash) returns true
    - This verifies the algorithm matches Go's bcrypt (same DefaultCost=10)

    Cleanup after tests: close database connection, clean up temp files.
  </action>
  <verify>
    <automated>cd server && npx vitest run --reporter=verbose phase02-integration</automated>
  </verify>
  <done>
    - All 14 integration test scenarios pass
    - End-to-end auth flow (login -> user info -> refresh) verified
    - Go JWT compatibility verified (AUTH-03)
    - bcryptjs compatibility verified (AUTH-01)
    - Public config endpoints verified (SETTING-02)
    - Settings access control verified (SETTING-01)
    - Error cases return Go-compatible error messages
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test -> App | Test harness simulates client requests |
| Test database | Isolated test database, not production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-20 | Information Disclosure | Test credentials in test files | low | accept | Test-only credentials, not used in production; test database is isolated |
| T-02-21 | Tampering | ValidationPipe not enabled | medium | mitigate | Verify ValidationPipe is registered globally in main.ts integration test |

No new npm installs in this plan — all packages installed by prior plans.
</threat_model>

<verification>
- All Phase 02 modules loaded in AppModule
- NestJS app starts on port 8091 without errors
- SettingsService cache loaded at startup
- Sqids encoder initialized with id_seed
- All integration tests pass
- Go-compatible JWT tokens accepted
- bcryptjs password hashing compatible with Go
</verification>

<success_criteria>
- NestJS app starts with all Phase 02 modules loaded
- SettingsService cache initialized at startup
- Sqids encoder initialized with id_seed from settings
- ValidationPipe registered globally
- All 14 integration test scenarios pass
- End-to-end auth flow works: login -> user info -> refresh token
- Go-issued JWT tokens accepted by NestJS guards
- All Phase 02 endpoints return Go-compatible response format
</success_criteria>

<output>
Create `.planning/phases/02-auth-settings/02-05-SUMMARY.md` when done
</output>
