---
phase: 14-features-verification
plan: 04
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - server/src/storage-policy/storage-policy.service.ts
  - server/src/user/user.service.ts
  - server/test/phase14-verification/storage-policy-verification.spec.ts
  - server/test/phase14-verification/user-management-verification.spec.ts
autonomous: true
requirements:
  - STORAGE-POLICY-VERIFICATION  # Phase-scope module: no formal requirement ID in ROADMAP.md
  - USER-MANAGEMENT-VERIFICATION  # Phase-scope module: no formal requirement ID in ROADMAP.md

must_haves:
  truths:
    - "Storage-policy dates are ISO strings (not raw Date objects), matching Go time.Time serialization"
    - "Storage-policy endpoints return IDs matching Go handler format (Sqids string)"
    - "UserGroup.description returns empty string (not null), matching Go string zero value"
    - "User management endpoints return userGroupID matching Go AdminUserDTO format (Sqids string)"
    - "GetUserInfoResponse.userGroupID is raw number, matching Go inconsistency"
  artifacts:
    - path: "server/test/phase14-verification/storage-policy-verification.spec.ts"
      provides: "Field-by-field verification of 5 storage-policy endpoints"
      contains: "describe.*StoragePolicy.*verification"
    - path: "server/test/phase14-verification/user-management-verification.spec.ts"
      provides: "Field-by-field verification of 7 user management endpoints"
      contains: "describe.*UserManagement.*verification"
    - path: "server/src/storage-policy/storage-policy.service.ts"
      provides: "Date fields use toISODateString instead of raw Date"
      contains: "toISODateString"
    - path: "server/src/user/user.service.ts"
      provides: "UserGroup.description defaults to empty string"
      contains: "description ?? ''"
  key_links:
    - from: "server/src/storage-policy/storage-policy.service.ts"
      to: "server/src/common/utils/date.util.ts"
      via: "toISODateString for date serialization"
      pattern: "toISODateString"
    - from: "server/src/user/user.service.ts"
      to: "server/src/database/schemas/user-group.schema.ts"
      via: "UserGroup description nullability handling"
      pattern: "description"
---

<objective>
Fix Storage-policy date serialization bug and UserGroup.description nullability, then verify both modules field-by-field.

Per RESEARCH: Storage-policy service returns raw Date objects for created_at/updated_at instead of ISO strings. This is inconsistent with all other modules.

Per D-311: UserGroup.description -- Go returns "" (string zero value), NestJS may return null when DB has null. Fix: default to empty string.

Purpose: Fix two MEDIUM-risk mismatches and verify storage-policy + user management response structures.
Output: Fixed storage-policy.service.ts + user.service.ts + verification test suites
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-features-verification/14-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@server/test/helpers/api-compat-helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix storage-policy date serialization and UserGroup.description nullability</name>
  <files>server/src/storage-policy/storage-policy.service.ts, server/src/user/user.service.ts, server/test/phase14-verification/storage-policy-verification.spec.ts, server/test/phase14-verification/user-management-verification.spec.ts</files>
  <read_first>
    - server/src/storage-policy/storage-policy.service.ts — NestJS StoragePolicyService (date serialization, ID handling)
    - server/src/user/user.service.ts — NestJS UserService (UserGroup description handling, userGroupID type)
    - _go-backend-archive/pkg/handler/storage_policy/handler.go — Go storage policy handler
    - _go-backend-archive/pkg/handler/user/handler.go — Go user handler
    - _go-backend-archive/pkg/domain/model/ — Go domain model (StoragePolicyResponse, AdminUserDTO, UserGroup)
    - frontend/src/types/storage-policy.ts — Frontend storage policy type definitions
    - frontend/src/types/user-management.ts — Frontend user management type definitions
    - frontend/src/types/user-center.ts — Frontend user center type definitions
    - frontend/src/lib/api/storage-policy.ts — Frontend storage policy API calls
    - frontend/src/lib/api/user-management.ts — Frontend user management API calls
    - frontend/src/lib/api/user-center.ts — Frontend user center API calls
    - server/test/phase13-verification/article-verification.spec.ts — Phase 13 test pattern reference
    - server/test/helpers/api-compat-helpers.ts — Test helper functions
  </read_first>
  <behavior>
    - Test 1: GET /api/policies returns policies with created_at as ISO string (not Date object)
    - Test 2: GET /api/policies returns policies with updated_at as ISO string
    - Test 3: GET /api/policies/:id returns policy with Sqids string id
    - Test 4: GET /api/admin/user-groups returns groups with description as string (not null)
    - Test 5: GET /api/admin/users returns users with userGroupID as Sqids string
    - Test 6: User profile endpoint returns userGroupID as raw number (Go inconsistency)
  </behavior>
  <action>
    **Fix Storage-policy date serialization (per RESEARCH finding):**

    1. In `server/src/storage-policy/storage-policy.service.ts`, find where `created_at` and `updated_at` are set in the response DTO. The current code returns `policy.createdAt` and `policy.updatedAt` as raw Date objects. Change to `toISODateString(policy.createdAt)` and `toISODateString(policy.updatedAt)`. Import `toISODateString` from the common utils if not already imported.

    2. Verify the import path for `toISODateString` -- check other service files (e.g., article.service.ts, file.service.ts) for the correct import path.

    **Fix UserGroup.description nullability (per D-311):**

    3. In `server/src/user/user.service.ts`, find where UserGroup DTOs are constructed. Where `description: group.description` appears, change to `description: group.description ?? ''` to match Go's string zero value behavior. This applies to both the admin user-groups endpoint and the nested userGroup object in AdminUserDTO.

    **Create storage-policy verification test:**

    4. Create `server/test/phase14-verification/storage-policy-verification.spec.ts`:
       - beforeAll: createTestApp, seed storage policy test data
       - Go StoragePolicyResponse assertion: verify 15 fields -- id(string, Sqids), created_at(string, ISO), updated_at(string, ISO), name(string), type(string), flag(string), server(string), bucket_name(string), is_private(boolean), access_key(string, masked), secret_key(string, masked), max_size(number), base_path(string), virtual_path(string), settings(object)
       - Test endpoints #140-144 (excluding OneDrive 501 endpoints #145-146)
       - GET /api/policies: PolicyListResponse { list, total }
       - GET /api/policies/:id: StoragePolicyResponse with Sqids id and ISO date strings
       - POST /api/policies: void response
       - PUT /api/policies/:id: void response
       - DELETE /api/policies/:id: void response

    **Create user management verification test:**

    5. Create `server/test/phase14-verification/user-management-verification.spec.ts`:
       - beforeAll: createTestApp, seed user test data
       - Go AdminUserDTO assertion: verify 11 fields -- id(string, Sqids), created_at(string), updated_at(string), username(string), nickname(string), avatar(string), email(string), website(string|null), lastLoginAt(string|null), userGroupID(string, Sqids), userGroup(object), status(number)
       - Go UserGroup assertion: verify 3 fields -- id(string, Sqids), name(string), description(string, NOT null -- must be empty string)
       - Test endpoints #147-153
       - GET /api/admin/users: AdminListUsersResponse { users, total, page, size }
       - POST /api/admin/users: AdminUserDTO with Sqids userGroupID
       - PUT /api/admin/users/:id: void response
       - DELETE /api/admin/users/:id: void response
       - POST /api/admin/users/:id/reset-password: void response
       - PUT /api/admin/users/:id/status: void response
       - GET /api/admin/user-groups: UserGroup array with description as string (not null)

    **User center endpoints (#154-158):**
    - PUT /api/user/profile: void response
    - POST /api/user/update-password: void response
    - POST /api/user/avatar: verify avatar URL response structure
    - GET /api/user/notification-settings: verify SimpleUserNotificationSettingsResponse { allowCommentReplyNotification: boolean }
    - PUT /api/user/notification-settings: void response
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/storage-policy-verification.spec.ts test/phase14-verification/user-management-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - storage-policy.service.ts uses toISODateString for created_at and updated_at (not raw Date objects)
    - GET /api/policies returns policies with created_at as ISO string
    - GET /api/policies returns policies with updated_at as ISO string
    - GET /api/policies/:id returns policy with Sqids string id
    - user.service.ts uses group.description ?? '' for UserGroup description
    - GET /api/admin/user-groups returns groups with description as string (not null)
    - GET /api/admin/users returns users with userGroupID as Sqids string
    - npx vitest run server/test/phase14-verification/storage-policy-verification.spec.ts exits 0
    - npx vitest run server/test/phase14-verification/user-management-verification.spec.ts exits 0
  </acceptance_criteria>
  <done>
    - Storage-policy dates are ISO strings (not raw Date objects)
    - Storage-policy ID is Sqids string
    - UserGroup.description returns empty string (not null)
    - AdminUserDTO.userGroupID is Sqids string
    - All storage-policy and user management verification tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify user center and notification settings structures</name>
  <files>server/test/phase14-verification/user-management-verification.spec.ts</files>
  <read_first>
    - _go-backend-archive/pkg/handler/user/handler.go — Go user handler (user center endpoints)
    - _go-backend-archive/pkg/handler/notification/dto.go — Go notification DTO definitions
    - _go-backend-archive/pkg/handler/notification/handler.go — Go notification handler
    - _go-backend-archive/pkg/domain/model/ — Go domain model (GetUserInfoResponse, SimpleUserNotificationSettingsResponse)
    - frontend/src/types/user-center.ts — Frontend user center type definitions
    - frontend/src/lib/api/user-center.ts — Frontend user center API calls
    - server/test/phase13-verification/article-verification.spec.ts — Phase 13 test pattern reference
  </read_first>
  <action>
    Extend the user management verification test file with user center and notification settings tests:

    **User center endpoints (#154-158):**
    - PUT /api/user/profile: Update profile, verify void response (returns null data).
    - POST /api/user/update-password: Change password, verify void response.
    - POST /api/user/avatar: Upload avatar. Read Go handler to determine response structure. Go returns avatar URL string. Verify NestJS returns same structure.
    - GET /api/user/notification-settings: Verify SimpleUserNotificationSettingsResponse { allowCommentReplyNotification: boolean } per D-313.
    - PUT /api/user/notification-settings: Update settings, verify void response.

    **UserGroup.description edge case:**
    - Create a user group with null description in DB (if schema allows)
    - Verify GET /api/admin/user-groups returns description as "" (empty string), not null
    - This validates the `group.description ?? ''` fix

    **GetUserInfoResponse.userGroupID inconsistency:**
    - GET /api/user/profile or equivalent user info endpoint
    - Verify userGroupID is raw number (not Sqids string) -- this matches Go's GetUserInfoResponse.userGroupID: uint
    - This is a Go design inconsistency that NestJS replicates per D-311
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/user-management-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - PUT /api/user/profile returns void response (null data)
    - POST /api/user/update-password returns void response
    - POST /api/user/avatar returns avatar URL string matching Go handler format
    - GET /api/user/notification-settings returns { allowCommentReplyNotification: boolean }
    - PUT /api/user/notification-settings returns void response
    - GET /api/admin/user-groups returns description as "" (empty string) for null DB values
    - GetUserInfoResponse.userGroupID is raw number (not Sqids string)
    - npx vitest run server/test/phase14-verification/user-management-verification.spec.ts exits 0
  </acceptance_criteria>
  <done>
    - User center endpoints verified (profile, password, avatar, notification settings)
    - UserGroup.description returns "" for null DB values
    - GetUserInfoResponse.userGroupID is raw number (Go inconsistency replicated)
    - All user management and user center tests pass
  </done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | User management endpoints require admin auth |
| client -> API | Storage policy endpoints require admin auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Elevation of Privilege | User management admin endpoints | medium | mitigate | AuthGuard + AdminGuard enforced |
| T-14-06 | Information Disclosure | Storage policy access_key/secret_key | medium | mitigate | Keys masked in response per existing implementation |
</threat_model>

<verification>
- Storage-policy tests pass: `cd server && npx vitest run test/phase14-verification/storage-policy-verification.spec.ts`
- User management tests pass: `cd server && npx vitest run test/phase14-verification/user-management-verification.spec.ts`
- Date fix applied: `grep -n 'toISODateString' server/src/storage-policy/storage-policy.service.ts` returns match
- Description fix applied: `grep -n "description ?? ''" server/src/user/user.service.ts` returns match
</verification>

<success_criteria>
- Storage-policy dates are ISO strings (not raw Date objects)
- Storage-policy ID is Sqids string matching Go
- UserGroup.description returns empty string for null values
- AdminUserDTO.userGroupID is Sqids string
- GetUserInfoResponse.userGroupID is raw number (Go inconsistency)
- All storage-policy and user management verification tests pass
</success_criteria>

<output>
Create `.planning/phases/14-features-verification/14-04-SUMMARY.md` when done
</output>
