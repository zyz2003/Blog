---
phase: 07-statistics-links
plan: 05
type: execute
wave: 4
depends_on: ["07-03", "07-04"]
files_modified:
  - server/src/app.module.ts
autonomous: true
requirements:
  - STATS-01
  - STATS-02
  - LINK-FRIEND-01

must_haves:
  truths:
    - AppModule imports StatisticsModule and LinkModule (already present as placeholders, now functional)
    - drizzle-kit push succeeds with all Phase 07 schemas (visitor_logs, visitor_stats, url_stats, links, link_categories, link_tags, link_tag_pivot)
    - All Phase 07 endpoints return correct response format { code, data, message }
    - Statistics and Link modules work together without conflicts
  artifacts:
    - path: "server/src/app.module.ts"
      provides: "AppModule with StatisticsModule and LinkModule wired"
  key_links:
    - from: "server/src/app.module.ts"
      to: "server/src/statistics/statistics.module.ts"
      via: "StatisticsModule import"
      pattern: "StatisticsModule"
    - from: "server/src/app.module.ts"
      to: "server/src/link/link.module.ts"
      via: "LinkModule import"
      pattern: "LinkModule"
---

<objective>
Wire StatisticsModule and LinkModule into AppModule, verify schema push succeeds, and run integration tests to confirm all Phase 07 endpoints work correctly.

Purpose: This plan ensures the statistics and link modules are properly integrated into the application, all database schemas are pushed correctly, and the complete API surface works end-to-end.
Output: Updated AppModule, verified schema push, passing integration tests
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-statistics-links/07-CONTEXT.md
@.planning/phases/07-statistics-links/07-RESEARCH.md
@.planning/phases/07-statistics-links/07-03-SUMMARY.md
@.planning/phases/07-statistics-links/07-04-SUMMARY.md
@server/src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify AppModule wiring, schema push, and run integration tests</name>
  <files>server/src/app.module.ts</files>
  <read_first>
    server/src/app.module.ts
    server/src/statistics/statistics.module.ts
    server/src/link/link.module.ts
  </read_first>
  <action>
Verify and update AppModule wiring for Phase 07:

1. AppModule already imports StatisticsModule and LinkModule (placeholders from Phase 01). Verify these imports now point to the fully-implemented modules with all providers. No changes needed if imports are correct.

2. Verify module import order: StatisticsModule and LinkModule should be after WeatherModule (since StatisticsService depends on GeoIPService from WeatherModule). Current order in app.module.ts already has StatisticsModule and LinkModule after WeatherModule, so no reordering needed.

3. Run drizzle-kit push to verify all Phase 07 schemas are correctly defined and can be pushed to SQLite:
   - visitor_logs (already exists from Phase 01, verify no changes needed)
   - visitor_stats (already exists from Phase 01, verify no changes needed)
   - url_stats (already exists from Phase 01, verify no changes needed)
   - links (already exists from Phase 01, verify no changes needed)
   - link_categories (already exists from Phase 01, verify no changes needed)
   - link_tags (already exists from Phase 01, verify no changes needed)
   - link_tag_pivot (already exists from Phase 01, verify no changes needed)

4. Run TypeScript compilation to verify no type errors across all Phase 07 files:
   npx tsc --noEmit in server/ directory

5. Run all existing tests to verify no regressions:
   npm test in server/ directory

6. Run Phase 07 specific tests:
   - StatisticsRepository tests
   - StatisticsService tests
   - StatisticsController tests
   - UAParserService tests
   - VisitorDedupService tests
   - LinkRepository tests
   - LinkService tests
   - LinkController tests
   - LinkApplyRateLimiter tests

7. Verify NestJS application starts successfully:
   npm run dev (brief startup check, then stop)

8. Manual endpoint verification (if NestJS starts):
   - POST /api/public/statistics/visit with test payload
   - GET /api/public/statistics/basic
   - GET /api/statistics/analytics (with admin JWT)
   - GET /api/statistics/top-pages (with admin JWT)
   - GET /api/statistics/trend (with admin JWT)
   - GET /api/statistics/summary (with admin JWT)
   - GET /api/statistics/visitor-logs (with admin JWT)
   - POST /api/public/links with test apply payload
   - GET /api/public/links
   - GET /api/links (with admin JWT)

If any issues are found during verification, fix them in the relevant files from Plans 01-04.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AppModule correctly imports StatisticsModule and LinkModule
    - drizzle-kit push succeeds for all Phase 07 schemas
    - TypeScript compilation passes with no errors
    - All Phase 07 tests pass
    - NestJS application starts successfully on port 8091
    - All Phase 07 endpoints return correct { code, data, message } format
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal | Module wiring within NestJS application — no external trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-07-16 | Elevation of Privilege | Module import order could expose admin endpoints | low | mitigate | Verify @Public() decorator on public endpoints; global JwtAuthGuard protects all others |
</threat_model>

<verification>
- AppModule imports StatisticsModule and LinkModule
- drizzle-kit push succeeds
- TypeScript compilation passes
- All Phase 07 tests pass
- NestJS starts on port 8091
- Statistics endpoints return correct response format
- Link endpoints return correct response format
</verification>

<success_criteria>
- AppModule correctly wires StatisticsModule and LinkModule
- All Phase 07 schemas push to SQLite without errors
- TypeScript compilation passes with no errors
- All Phase 07 unit tests pass
- NestJS application starts and all endpoints respond correctly
- No regressions in existing Phase 01-06 functionality
</success_criteria>

<output>
Create `.planning/phases/07-statistics-links/07-05-SUMMARY.md` when done
</output>
