# Plan 11-05: Backup, Captcha, Weather, Proxy Tests + Integration Verification

**Phase:** 11-Migration & Integration
**Depends on:** 11-01 (migration tool), 11-02 (test infrastructure), 11-03, 11-04
**Estimated files:** 5 new test files + 2 updates

## Must-Haves

| Truth | Artifact | Key Link |
|-------|----------|----------|
| All remaining endpoints have compat tests | 4 test files in server/test/api-compat/ | Backup, captcha, weather, proxy endpoints verified |
| Full test suite passes end-to-end | vitest run output | All api-compat test files pass with 0 failures |
| Migration tool works on real data | scripts/migrate.ts execution | Migration completes with row count parity and critical value match |
| Project is production-ready | STATE.md + ROADMAP.md | Phase 11 marked complete, progress 100% |

## Objective

Complete the remaining API compatibility test files, run full integration verification, and mark the project as production-ready.

## Tasks

### Task 1: Backup & captcha module test files (2 files)

**Files:**
- `server/test/api-compat/backup-api-compat.spec.ts`
- `server/test/api-compat/captcha-api-compat.spec.ts`

**Endpoints covered:**

Backup (7):
- POST /api/config/backup/create — Create backup (JWT+Admin)
- GET /api/config/backup/list — List backups (JWT+Admin)
- POST /api/config/backup/restore — Restore backup (JWT+Admin)
- POST /api/config/backup/delete — Delete backup (JWT+Admin)
- POST /api/config/backup/clean — Clean old backups (JWT+Admin)
- GET /api/config/export — Export config (JWT+Admin)
- POST /api/config/import — Import config (JWT+Admin)

Captcha (2):
- GET /api/public/captcha/config — Captcha config
- GET /api/public/captcha/image — Captcha image (RateLimit)

**Key assertions:**
- Backup list is array of { id, name, created_at, size }
- Backup create returns success with backup info
- Captcha config has provider field
- Captcha image returns image data (Content-Type: image/*)

**Verification:** `npx vitest run server/test/api-compat/backup-api-compat.spec.ts server/test/api-compat/captcha-api-compat.spec.ts --reporter=verbose`

### Task 2: Weather & proxy test files (2 files)

**Files:**
- `server/test/api-compat/weather-api-compat.spec.ts`
- `server/test/api-compat/proxy-api-compat.spec.ts`

**Endpoints covered:**

Weather (1):
- GET /api/public/weather/ip-location — IP location for weather widget

Proxy (1):
- GET /api/proxy/download — Proxy download (RateLimit)

File Download (1):
- GET /needcache/download/:public_id — Signed download

**Key assertions:**
- Weather IP location returns location data or error
- Proxy download returns proper response shape
- Signed download returns file content or error

**Verification:** `npx vitest run server/test/api-compat/weather-api-compat.spec.ts server/test/api-compat/proxy-api-compat.spec.ts --reporter=verbose`

### Task 3: Run full API compat test suite

1. **Run all API compat tests**:
   ```bash
   npx vitest run server/test/api-compat/ --reporter=verbose
   ```
   - All tests must pass
   - No skipped tests (unless explicitly marked as known issue with comment)
   - Record total test count and pass rate

2. **Fix any failures**:
   - If tests fail due to response format mismatch: fix the NestJS controller/service
   - If tests fail due to missing endpoint: verify route registration in NestJS
   - If tests fail due to test infrastructure issue: fix the helpers

**Verification:** `npx vitest run server/test/api-compat/` exits with code 0 (all pass).

### Task 4: Run migration tool end-to-end

1. **Create test source database**:
   - Use the Go backend's SQLite format (with ISO8601 timestamps)
   - Insert sample data in key tables: user_groups, users, settings, articles, comments

2. **Run migration**:
   ```bash
   npx tsx scripts/migrate.ts --source test-source.db --target data/anheyu.db --verbose
   ```

3. **Verify migration results**:
   - Row counts match between source and target
   - id_seed value identical
   - JWT_SECRET value identical
   - Timestamps converted correctly (spot-check a few records)
   - FK integrity check passes

4. **Verify Sqids encoding consistency**:
   - After migration, start NestJS with the migrated database
   - Encode a known article ID and verify it matches the Go backend's encoding

**Verification:** Migration completes with exit code 0. Verification output shows all ✅.

### Task 5: Frontend integration smoke test + project completion

1. **Start NestJS backend**:
   ```bash
   cd server && npm run dev
   ```
   - Verify starts on port 8091
   - No startup errors

2. **Start Next.js frontend**:
   ```bash
   npm run dev
   ```
   - Verify frontend loads
   - Login works with migrated credentials
   - Article list loads

3. **Update STATE.md**:
   - Mark Phase 11 as Complete
   - Update progress to 100% (11/11 phases, all plans complete)
   - Record final metrics
   - Update last_updated date

4. **Update ROADMAP.md**:
   - Mark Phase 11 plans as complete with date
   - Fix "PostgreSQL connection" → "SQLite database" in success criteria (per D-300)
   - Fix "All 30 tables" → "All 33 tables"
   - Fix "P0 endpoints" → "All endpoints" (per D-306)
   - Update "Last updated" date

**Verification:** `npm run dev` starts both frontend and backend. STATE.md shows Phase 11 Complete. ROADMAP.md Phase 11 section has all plans checked.

## Files Changed

| File | Action | Description |
|------|--------|-------------|
| `server/test/api-compat/backup-api-compat.spec.ts` | CREATE | Backup tests (7 endpoints) |
| `server/test/api-compat/captcha-api-compat.spec.ts` | CREATE | Captcha tests (2 endpoints) |
| `server/test/api-compat/weather-api-compat.spec.ts` | CREATE | Weather tests (1 endpoint) |
| `server/test/api-compat/proxy-api-compat.spec.ts` | CREATE | Proxy/download tests (2 endpoints) |
| `.planning/STATE.md` | EDIT | Mark Phase 11 complete, progress 100% |
| `.planning/ROADMAP.md` | EDIT | Update Phase 11 status, fix success criteria wording |

## Completion Criteria

Phase 11 is complete when:
1. ✅ Migration CLI tool works end-to-end (Task 4)
2. ✅ All API compat tests pass (Task 3)
3. ✅ Frontend connects to new backend (Task 5)
4. ✅ STATE.md and ROADMAP.md updated (Task 5)
