---
phase: 03-article-category-tag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/database/schemas/article-post-category-pivot.schema.ts
  - server/src/database/schemas/article-post-tag-pivot.schema.ts
  - server/src/database/schemas/index.ts
  - server/src/post-category/post-category.module.ts
  - server/src/post-category/post-category.controller.ts
  - server/src/post-category/post-category.service.ts
  - server/src/post-category/post-category.repository.ts
  - server/src/post-category/dto/create-post-category.dto.ts
  - server/src/post-category/dto/update-post-category.dto.ts
  - server/src/post-tag/post-tag.module.ts
  - server/src/post-tag/post-tag.controller.ts
  - server/src/post-tag/post-tag.service.ts
  - server/src/post-tag/post-tag.repository.ts
  - server/src/post-tag/dto/create-post-tag.dto.ts
  - server/src/post-tag/dto/update-post-tag.dto.ts
  - server/src/common/constants/error-codes.ts
autonomous: true
requirements: [CATEGORY-01, TAG-01]

must_haves:
  truths:
    - GET /api/post-categories returns all categories with Sqids-encoded IDs and snake_case JSON keys
    - POST /api/post-categories creates a category and returns it with Sqids-encoded ID
    - PUT /api/post-categories/:id updates a category by Sqids public ID
    - DELETE /api/post-categories/:id soft-deletes a category by Sqids public ID
    - GET /api/post-tags returns all tags with Sqids-encoded IDs
    - POST /api/post-tags creates a tag and returns it with Sqids-encoded ID
    - PUT /api/post-tags/:id updates a tag by Sqids public ID
    - DELETE /api/post-tags/:id soft-deletes a tag by Sqids public ID
    - article_post_categories and article_post_tags junction tables exist in SQLite after drizzle-kit push
  artifacts:
    - server/src/database/schemas/article-post-category-pivot.schema.ts
    - server/src/database/schemas/article-post-tag-pivot.schema.ts
    - server/src/post-category/post-category.module.ts
    - server/src/post-category/post-category.controller.ts
    - server/src/post-category/post-category.service.ts
    - server/src/post-category/post-category.repository.ts
    - server/src/post-tag/post-tag.module.ts
    - server/src/post-tag/post-tag.controller.ts
    - server/src/post-tag/post-tag.service.ts
    - server/src/post-tag/post-tag.repository.ts
  key_links:
    - Junction table FK references point to correct schema exports (articles, postCategories, postTags)
    - PostCategoryModule and PostTagModule import DatabaseModule for DRIZZLE injection
    - Controller routes match Go backend paths exactly (/api/post-categories, /api/post-tags)

---

<objective>
Create junction table schemas and independent PostCategory/PostTag CRUD modules.

Purpose: Establish the M2M relationship schemas and category/tag management endpoints that articles depend on. Per D-57, both relationships use junction tables. Per D-59, PostCategoryModule and PostTagModule are independent modules with their own controller+service+repository.

Output: Two junction table schema files, two complete CRUD modules, article-related error codes, and schema barrel export update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-article-category-tag/03-CONTEXT.md
@.planning/phases/03-article-category-tag/03-RESEARCH.md
@server/src/database/schemas/article.schema.ts
@server/src/database/schemas/post-category.schema.ts
@server/src/database/schemas/post-tag.schema.ts
@server/src/database/schemas/link-tag-pivot.schema.ts
@server/src/database/schemas/index.ts
@server/src/database/database.module.ts
@server/src/common/utils/sqids.util.ts
@server/src/common/utils/time.util.ts
@server/src/common/constants/error-codes.ts
@pkg/handler/post_category/handler.go
@pkg/handler/post_tag/handler.go
@pkg/domain/model/article.go
@internal/infra/router/router.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create junction table schemas and update barrel export</name>
  <files>
    server/src/database/schemas/article-post-category-pivot.schema.ts,
    server/src/database/schemas/article-post-tag-pivot.schema.ts,
    server/src/database/schemas/index.ts
  </files>
  <read_first>
    server/src/database/schemas/link-tag-pivot.schema.ts,
    server/src/database/schemas/article.schema.ts,
    server/src/database/schemas/post-category.schema.ts,
    server/src/database/schemas/post-tag.schema.ts,
    server/src/database/schemas/index.ts
  </read_first>
  <behavior>
    - article_post_categories table has composite PK (article_id, post_category_id) with FK references and ON DELETE CASCADE
    - article_post_tags table has composite PK (article_id, post_tag_id) with FK references and ON DELETE CASCADE
    - Both schemas export table constants that are re-exported from index.ts
  </behavior>
  <action>
    Per D-57, create two junction table schema files following the link-tag-pivot.schema.ts pattern exactly.

    Create article-post-category-pivot.schema.ts:
    - Table name: "article_post_categories" (matches Go ent/migrate/schema.go ArticlePostCategoriesTable)
    - Columns: articleId (integer, notNull, references articles.id with onDelete cascade), postCategoryId (integer, notNull, references postCategories.id with onDelete cascade)
    - Composite primary key on (articleId, postCategoryId) using the table callback pattern from link-tag-pivot.schema.ts
    - Import articles from './article.schema' and postCategories from './post-category.schema'

    Create article-post-tag-pivot.schema.ts:
    - Table name: "article_post_tags" (matches Go ent/migrate/schema.go ArticlePostTagsTable)
    - Columns: articleId (integer, notNull, references articles.id with onDelete cascade), postTagId (integer, notNull, references postTags.id with onDelete cascade)
    - Composite primary key on (articleId, postTagId)
    - Import articles from './article.schema' and postTags from './post-tag.schema'

    Update server/src/database/schemas/index.ts:
    - Add export for article-post-category-pivot.schema.ts
    - Add export for article-post-tag-pivot.schema.ts
    - Place after the existing post-tag.schema.ts export line
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    article_post_categories and article_post_tags junction table schemas exist with correct FK references, composite PKs, and are re-exported from index.ts. TypeScript compilation passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PostCategory and PostTag CRUD modules</name>
  <files>
    server/src/post-category/post-category.module.ts,
    server/src/post-category/post-category.controller.ts,
    server/src/post-category/post-category.service.ts,
    server/src/post-category/post-category.repository.ts,
    server/src/post-category/dto/create-post-category.dto.ts,
    server/src/post-category/dto/update-post-category.dto.ts,
    server/src/post-tag/post-tag.module.ts,
    server/src/post-tag/post-tag.controller.ts,
    server/src/post-tag/post-tag.service.ts,
    server/src/post-tag/post-tag.repository.ts,
    server/src/post-tag/dto/create-post-tag.dto.ts,
    server/src/post-tag/dto/update-post-tag.dto.ts,
    server/src/common/constants/error-codes.ts
  </files>
  <read_first>
    server/src/auth/auth.controller.ts,
    server/src/auth/auth.service.ts,
    server/src/settings/settings.service.ts,
    server/src/common/utils/sqids.util.ts,
    server/src/common/utils/time.util.ts,
    server/src/common/decorators/public.decorator.ts,
    server/src/common/guards/admin.guard.ts,
    server/src/common/constants/error-codes.ts,
    server/src/database/database.module.ts,
    pkg/handler/post_category/handler.go,
    pkg/handler/post_tag/handler.go,
    internal/infra/router/router.go
  </read_first>
  <behavior>
    - GET /api/post-categories returns array of PostCategoryResponse with Sqids-encoded id, snake_case fields, no auth required
    - POST /api/post-categories creates category with name (required), slug, description, is_series, sort_order; returns PostCategoryResponse; requires JwtAuth + AdminAuth
    - PUT /api/post-categories/:id decodes Sqids ID, updates fields, returns PostCategoryResponse; requires JwtAuth + AdminAuth
    - DELETE /api/post-categories/:id soft-deletes by setting deletedAt; returns null with success message; requires JwtAuth + AdminAuth
    - GET /api/post-tags returns array of PostTagResponse with Sqids-encoded id, snake_case fields; uses @Public() per Go router JWTAuthOptional pattern
    - POST /api/post-tags creates tag with name (required), slug; returns PostTagResponse; requires JwtAuth + AdminAuth
    - PUT /api/post-tags/:id decodes Sqids ID, updates fields, returns PostTagResponse; requires JwtAuth + AdminAuth
    - DELETE /api/post-tags/:id soft-deletes; returns null with success message; requires JwtAuth + AdminAuth
    - Duplicate category/tag name returns 409 CONFLICT
    - Category/tag not found returns 404 with appropriate error code
  </behavior>
  <action>
    Per D-59 and D-62, create independent PostCategoryModule and PostTagModule.

    **PostCategoryModule** (server/src/post-category/):
    - post-category.module.ts: @Module with controllers [PostCategoryController], providers [PostCategoryService, PostCategoryRepository], imports [DatabaseModule]
    - post-category.controller.ts: @Controller('post-categories') with 4 endpoints matching Go routes:
      - @Public() @Get() list() — no auth, returns PostCategoryService.list()
      - @Post() create(@Body() dto) — JwtAuth + AdminAuth (implicit from global guard), returns PostCategoryService.create()
      - @Put(':id') update(@Param('id') publicId, @Body() dto) — JwtAuth + AdminAuth, decodes Sqids ID in controller, passes dbId to service
      - @Delete(':id') delete(@Param('id') publicId) — JwtAuth + AdminAuth, decodes Sqids ID in controller
    - post-category.service.ts: @Injectable with methods list(), create(dto), update(dbId, dto), delete(dbId). Uses PostCategoryRepository. toApiResponse method encodes id via generatePublicID(dbId, EntityType.PostCategory), formats dates via formatToChinaTime, maps to snake_case JSON keys matching Go PostCategoryResponse: id, created_at, updated_at, name, slug, description, count, is_series
    - post-category.repository.ts: @Injectable with @Inject(DRIZZLE). Methods: findAll() (where deletedAt isNull), findById(dbId), findByName(name), create(data), update(dbId, data), softDelete(dbId). All queries filter deletedAt IS NULL
    - dto/create-post-category.dto.ts: name (IsString, IsNotEmpty), slug (IsOptional, IsString), description (IsOptional, IsString), is_series (IsOptional, IsBoolean), sort_order (IsOptional, IsInt)
    - dto/update-post-category.dto.ts: PartialType of CreatePostCategoryDto

    **PostTagModule** (server/src/post-tag/):
    - Same structure as PostCategoryModule but with PostTag schema (no description, is_series, sort_order fields)
    - Controller: @Controller('post-tags'). Per Go router line 294, GET /api/post-tags uses JWTAuthOptional. In NestJS with global JwtAuthGuard, use @Public() on list() since optional JWT context is not needed for tag listing
    - toApiResponse: id, created_at, updated_at, name, slug, count (matching Go PostTagResponse)
    - dto/create-post-tag.dto.ts: name (IsString, IsNotEmpty), slug (IsOptional, IsString)
    - dto/update-post-tag.dto.ts: PartialType of CreatePostTagDto

    **Error codes** (server/src/common/constants/error-codes.ts):
    - Add: CATEGORY_NOT_FOUND, CATEGORY_NAME_EXISTS, TAG_NOT_FOUND, TAG_NAME_EXISTS

    **Key patterns to follow** (from auth.controller.ts / auth.service.ts):
    - Controller decodes Sqids public ID to dbId before calling service (per D-63)
    - Service returns raw data objects, ResponseInterceptor wraps as { code, data, message }
    - Repository uses @Inject(DRIZZLE) and Drizzle query builders
    - All queries must include isNull(deletedAt) filter for soft-deleted records
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    PostCategoryModule and PostTagModule with full CRUD endpoints. Category and tag IDs are Sqids-encoded in responses. JSON keys use snake_case matching Go PostCategoryResponse/PostTagResponse. Soft delete works. Duplicate names return 409. All queries filter soft-deleted records. TypeScript compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Schema push and verify junction tables exist in SQLite</name>
  <files>
    server/src/database/schemas/article-post-category-pivot.schema.ts,
    server/src/database/schemas/article-post-tag-pivot.schema.ts
  </files>
  <read_first>
    server/src/database/schemas/index.ts,
    server/drizzle.config.ts
  </read_first>
  <action>
    Run npx drizzle-kit push to create the article_post_categories and article_post_tags junction tables in the SQLite database. This is required per the schema_push_requirement. Verify the tables exist by checking the database file.

    After push, verify that:
    - article_post_categories table exists with columns article_id and post_category_id
    - article_post_tags table exists with columns article_id and post_tag_id
    - Foreign key constraints are in place
    - Composite primary keys are set

    If drizzle-kit push requires interactive prompts, use the non-TTY workaround (pipe yes or use --force flag if available).
  </action>
  <verify>
    <automated>cd D:/CodeDevelopment/project/Blog/server && npx drizzle-kit push 2>&1 | tail -10</automated>
  </verify>
  <done>
    Junction tables article_post_categories and article_post_tags exist in SQLite with correct columns, FK constraints, and composite PKs. drizzle-kit push succeeds without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/post-categories (POST/PUT/DELETE) | Untrusted input crosses here; requires JwtAuth + AdminAuth |
| client -> /api/post-tags (POST/PUT/DELETE) | Untrusted input crosses here; requires JwtAuth + AdminAuth |
| client -> /api/post-categories (GET) | Public endpoint, no auth required |
| client -> /api/post-tags (GET) | Public endpoint with optional auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | POST /api/post-categories name field | medium | mitigate | class-validator IsString + IsNotEmpty, unique constraint in DB |
| T-03-02 | Information Disclosure | GET /api/post-categories | low | accept | Public data by design, no sensitive info exposed |
| T-03-03 | Tampering | PUT /api/post-categories/:id ID manipulation | medium | mitigate | Sqids decode validates entityType = PostCategory, not found returns 404 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan; all already installed from Phase 01/02 |
</threat_model>

<verification>
- TypeScript compilation passes: cd server && npx tsc --noEmit
- Junction tables exist after schema push: cd server && npx drizzle-kit push
- Category CRUD works: POST/GET/PUT/DELETE /api/post-categories
- Tag CRUD works: POST/GET/PUT/DELETE /api/post-tags
- Sqids encoding works: category/tag IDs in responses are encoded
- Soft delete works: deleted categories/tags don't appear in GET list
- Duplicate name rejection: POST with existing name returns 409
</verification>

<success_criteria>
- Junction table schemas exist and are exported from index.ts
- PostCategoryModule provides 4 CRUD endpoints at /api/post-categories
- PostTagModule provides 4 CRUD endpoints at /api/post-tags
- All IDs are Sqids-encoded in responses
- All JSON keys use snake_case matching Go backend
- Soft-deleted records are filtered from list queries
- TypeScript compilation passes
- drizzle-kit push succeeds
</success_criteria>

<output>
Create `.planning/phases/03-article-category-tag/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `articlePostCategories` table constant (article-post-category-pivot.schema.ts)
- `articlePostTags` table constant (article-post-tag-pivot.schema.ts)
- `PostCategoryModule` class
- `PostCategoryController` class with endpoints: list, create, update, delete
- `PostCategoryService` class with methods: list, create, update, delete, toApiResponse
- `PostCategoryRepository` class with methods: findAll, findById, findByName, create, update, softDelete
- `CreatePostCategoryDto` class
- `UpdatePostCategoryDto` class
- `PostTagModule` class
- `PostTagController` class with endpoints: list, create, update, delete
- `PostTagService` class with methods: list, create, update, delete, toApiResponse
- `PostTagRepository` class with methods: findAll, findById, findByName, create, update, softDelete
- `CreatePostTagDto` class
- `UpdatePostTagDto` class
- Error codes: CATEGORY_NOT_FOUND, CATEGORY_NAME_EXISTS, TAG_NOT_FOUND, TAG_NAME_EXISTS
