---
phase: 14-features-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/album/album.service.ts
  - server/src/album/dto/album-response.dto.ts
  - server/test/phase14-verification/album-verification.spec.ts
autonomous: true
requirements:
  - ALBUM-01

must_haves:
  truths:
    - "Album response fields use camelCase matching Go Album struct (imageUrl, bigImageUrl, downloadUrl, etc.)"
    - "Album.id is raw DB int (number), matching Go Album.id: uint"
    - "Album.fileHash is present in response, matching Go Album.fileHash"
    - "Album dates (created_at, updated_at, published_at) use snake_case, matching Go"
    - "AlbumCategory uses camelCase displayOrder, matching Go"
  artifacts:
    - path: "server/test/phase14-verification/album-verification.spec.ts"
      provides: "Field-by-field verification of all 15 album endpoints"
      contains: "describe.*Album.*verification"
    - path: "server/src/album/album.service.ts"
      provides: "toResponseDTO includes fileHash field"
      contains: "fileHash"
  key_links:
    - from: "server/src/album/album.service.ts"
      to: "server/src/album/dto/album-response.dto.ts"
      via: "toResponseDTO populates DTO with camelCase fields"
      pattern: "toResponseDTO"
---

<objective>
Verify Album module field-by-field against Go Album struct and add missing fileHash field.

Per D-304: Go Album uses camelCase JSON tags (imageUrl, bigImageUrl, etc.), NestJS toResponseDTO also uses camelCase -- consistent.

Per D-306: Album.id is raw DB int in both Go and NestJS -- consistent.

Research found Album.fileHash is MISSING from NestJS toResponseDTO but present in Go Album struct. Need to add it.

Purpose: Ensure Album module response structure exactly matches Go Album struct, including the missing fileHash field.
Output: Fixed album.service.ts + comprehensive album verification test suite
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-features-verification/14-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@server/test/helpers/api-compat-helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add missing fileHash to Album response and verify all Album endpoints</name>
  <files>server/src/album/album.service.ts, server/src/album/dto/album-response.dto.ts, server/test/phase14-verification/album-verification.spec.ts</files>
  <read_first>
    - server/src/album/album.service.ts — NestJS AlbumService (toResponseDTO method)
    - server/src/album/dto/album-response.dto.ts — NestJS Album DTO (field definitions)
    - _go-backend-archive/pkg/domain/model/album.go — Go Album struct (camelCase JSON tags, fileHash field)
    - _go-backend-archive/pkg/handler/album/handler.go — Go album handler (CRUD, import/export)
    - _go-backend-archive/pkg/handler/album_category/handler.go — Go album category handler
    - frontend/src/types/album.ts — Frontend Album type definitions
    - frontend/src/lib/api/album.ts — Frontend album admin API calls
    - frontend/src/lib/api/album-public.ts — Frontend album public API calls
    - server/test/phase13-verification/article-verification.spec.ts — Phase 13 test pattern reference
    - server/test/helpers/api-compat-helpers.ts — Test helper functions
  </read_first>
  <behavior>
    - Test 1: GET /api/albums/get returns albums with fileHash field (string or null)
    - Test 2: GET /api/albums/get returns albums with all camelCase fields: imageUrl, bigImageUrl, downloadUrl, viewCount, downloadCount, fileSize, aspectRatio, displayOrder, categoryId
    - Test 3: GET /api/albums/get returns albums with snake_case date fields: created_at, updated_at, published_at
    - Test 4: GET /api/albums/get returns albums with numeric id (not Sqids string)
    - Test 5: GET /api/album-categories returns categories with camelCase displayOrder
    - Test 6: GET /api/public/albums returns public albums with same field structure
    - Test 7: POST /api/albums/batch-import returns correct result structure
  </behavior>
  <action>
    **Add missing fileHash field (per RESEARCH finding):**

    1. In `server/src/album/dto/album-response.dto.ts`, verify `fileHash` field exists. If missing, add `fileHash: string | null` property.

    2. In `server/src/album/album.service.ts`, find the `toResponseDTO` method. Verify it includes `fileHash: album.fileHash ?? null` (or equivalent). If missing, add it. The Go Album struct has `fileHash` as a string field.

    3. In `server/src/album/album.service.ts`, verify `toResponseDTO` does NOT include `widthAndHeight` -- this is an extra field not in Go Album. If present, it is LOW risk (extra field ignored by frontend per D-304), but document its presence in the test file as a known deviation.

    **Create verification test file:**

    4. Create `server/test/phase14-verification/album-verification.spec.ts` following Phase 13 pattern:
       - Import from `../../test/helpers/api-compat-helpers`
       - Use `vitest` (describe/it/expect/beforeAll/afterAll)
       - beforeAll: createTestApp, seed album test data (categories, albums)
       - Test all 15 endpoints from 12-RISK-MARKING.md #119-133

    **Go Album field-by-field assertion function:**
    - assertAlbumResponseFields(data): verify 22 fields matching Go Album struct:
      - id(number), created_at(string), updated_at(string), imageUrl(string), bigImageUrl(string), downloadUrl(string), thumbParam(string), bigParam(string), tags(string), viewCount(number), downloadCount(number), width(number), height(number), fileSize(number), format(string), aspectRatio(string), fileHash(string|null), displayOrder(number), categoryId(number|null), title(string), description(string), location(string), published_at(string|null)
      - Note: Go uses camelCase for these fields, NestJS must match per D-304
      - Note: created_at/updated_at/published_at use snake_case per D-305

    **Go AlbumCategoryDTO assertion function:**
    - assertAlbumCategoryFields(data): verify 4 fields -- id(number), name(string), description(string), displayOrder(number)
      - Note: displayOrder is camelCase matching Go per D-304

    **Test groups:**
    - Admin album CRUD: GET /api/albums/get, POST /api/albums/add, PUT /api/albums/update/:id, DELETE /api/albums/delete/:id, DELETE /api/albums/batch-delete
    - Album categories: GET /api/album-categories, POST /api/album-categories (returns 201 per D-244), PUT /api/album-categories/:id, DELETE /api/album-categories/:id
    - Import/Export: POST /api/albums/batch-import, POST /api/albums/import, POST /api/albums/export
    - Public: GET /api/public/albums, GET /api/public/album-categories, PUT /api/public/stat/:id
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/album-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - album.service.ts toResponseDTO includes fileHash field
    - album-response.dto.ts has fileHash: string | null property
    - GET /api/albums/get returns albums with fileHash (string or null)
    - GET /api/albums/get returns albums with camelCase fields: imageUrl, bigImageUrl, downloadUrl, viewCount, downloadCount, fileSize, aspectRatio, displayOrder, categoryId
    - GET /api/albums/get returns albums with snake_case dates: created_at, updated_at, published_at
    - GET /api/albums/get returns albums with numeric id (not Sqids string)
    - GET /api/album-categories returns categories with camelCase displayOrder
    - npx vitest run server/test/phase14-verification/album-verification.spec.ts exits 0
  </acceptance_criteria>
  <done>
    - Album.fileHash is present in toResponseDTO output
    - All camelCase fields (imageUrl, bigImageUrl, downloadUrl, viewCount, etc.) verified
    - All snake_case date fields (created_at, updated_at, published_at) verified
    - Album.id is raw DB int (number)
    - AlbumCategory.displayOrder is camelCase
    - All 15 album endpoint verification tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify album import/export result structures and public album display</name>
  <files>server/test/phase14-verification/album-verification.spec.ts</files>
  <read_first>
    - _go-backend-archive/pkg/handler/album/handler.go — Go album handler (import/export, public endpoints)
    - _go-backend-archive/pkg/domain/model/album.go — Go Album struct (field definitions for import/export)
    - frontend/src/types/album.ts — Frontend Album type definitions
    - frontend/src/lib/api/album-public.ts — Frontend album public API calls
    - server/test/phase13-verification/article-verification.spec.ts — Phase 13 test pattern reference
  </read_first>
  <action>
    Extend the album verification test file with detailed import/export and public endpoint tests:

    **Import result structures (endpoints #128-129):**
    - POST /api/albums/batch-import: Verify BatchImportAlbumsResult structure. Read Go handler to determine exact field names. Common pattern: { total, success, failed, skipped } or similar. Assert each field type.
    - POST /api/albums/import: Verify ImportAlbumsResult structure. May differ from batch-import. Assert field names and types match Go handler.

    **Export (endpoint #130):**
    - POST /api/albums/export: Verify returns blob (application/octet-stream or similar). Assert Content-Type header.

    **Public album endpoints (endpoints #131-133):**
    - GET /api/public/albums: Verify PublicAlbumListData structure. Each album must have all camelCase fields matching Go Album struct. Verify pagination structure { list, total, page, pageSize } or equivalent.
    - GET /api/public/album-categories: Verify array of AlbumCategoryDTO with camelCase displayOrder.
    - PUT /api/public/stat/:id: Verify void response (increments view count).

    **Edge cases:**
    - Album with null categoryId (uncategorized album)
    - Album with null published_at (unpublished)
    - Album with null fileHash (if DB allows)
    - Album stat increment for nonexistent ID succeeds silently per D-248
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/album-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/albums/batch-import returns result with total, success, failed, skipped fields matching Go handler
    - POST /api/albums/export returns Content-Type application/octet-stream (or equivalent blob)
    - GET /api/public/albums returns albums with all camelCase fields and numeric id
    - GET /api/public/album-categories returns array with camelCase displayOrder
    - PUT /api/public/stat/:id succeeds (void response, increments view count)
    - Album with null categoryId returns categoryId: null
    - Album with null published_at returns published_at: null
    - npx vitest run server/test/phase14-verification/album-verification.spec.ts exits 0
  </acceptance_criteria>
  <done>
    - Import/export result structures match Go handler responses
    - Public album endpoints return correct field structures
    - Edge cases (null fields, stat increment) verified
    - All album verification tests pass
  </done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Album import data is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | Album import data | low | accept | Import validated by DTO class-validator |
</threat_model>

<verification>
- All album verification tests pass: `cd server && npx vitest run test/phase14-verification/album-verification.spec.ts`
- fileHash present in album response: `grep -n 'fileHash' server/src/album/album.service.ts` returns match
- fileHash in DTO: `grep -n 'fileHash' server/src/album/dto/album-response.dto.ts` returns match
</verification>

<success_criteria>
- Album.fileHash present in response matching Go Album struct
- All camelCase fields verified (imageUrl, bigImageUrl, downloadUrl, viewCount, etc.)
- All snake_case date fields verified (created_at, updated_at, published_at)
- Album.id is raw DB int
- All 15 album endpoint verification tests pass
</success_criteria>

<output>
Create `.planning/phases/14-features-verification/14-02-SUMMARY.md` when done
</output>
