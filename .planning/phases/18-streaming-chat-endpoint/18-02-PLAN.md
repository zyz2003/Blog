---
phase: 18-streaming-chat-endpoint
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/chat/ChatWidget.tsx
  - frontend/src/components/chat/ChatWindow.tsx
  - frontend/src/components/chat/MessageList.tsx
  - frontend/src/components/chat/ToolResultCard.tsx
  - frontend/src/components/chat/ChatInput.tsx
  - frontend/src/components/chat/index.ts
  - frontend/src/app/layout.tsx
autonomous: true
requirements: [AI-05F]
tags: [ai, frontend, chat, useChat, streaming]

must_haves:
  truths:
    - Right-bottom floating button is visible on all frontend pages; clicking opens chat window
    - Chat window shows streaming tokens arriving incrementally (typewriter effect)
    - Tool calls display "正在搜索文章..." loading state during execution
    - Tool results render as clickable article link cards (title + snippet + url)
    - useChat with DefaultChatTransport sends messages to /api/ai/chat
    - conversationId is stored in localStorage for history recovery (D-361)
    - Desktop: 380x580 fixed window; mobile: fullscreen (D-366)
  artifacts:
    - frontend/src/components/chat/ChatWidget.tsx
    - frontend/src/components/chat/ChatWindow.tsx
    - frontend/src/components/chat/MessageList.tsx
    - frontend/src/components/chat/ToolResultCard.tsx
    - frontend/src/components/chat/ChatInput.tsx
    - frontend/src/components/chat/index.ts
  key_links:
    - ChatWidget (floating button) → ChatWindow (useChat) → POST /api/ai/chat
    - ChatWindow → localStorage (conversationId read/write)
    - MessageList → ToolResultCard (when part.type === 'tool-result')
    - useChat + DefaultChatTransport → /api/ai/chat (Next.js rewrite proxies to :8091)
---

<objective>
Build the frontend chat widget that consumes the streaming chat endpoint from Plan 18-01.

Purpose: Visitors can chat with the AI assistant directly from the frontend. The chat
widget provides a floating button, streaming token display, and tool result rendering.

Output: Working chat widget component installed in the frontend layout.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-streaming-chat-endpoint/18-CONTEXT.md
@frontend/src/components/post/ArticleLeadSummary.tsx
@frontend/src/app/layout.tsx
@frontend/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install AI SDK dependencies + ChatWidget + ChatWindow with useChat</name>
  <files>
    frontend/package.json,
    frontend/package-lock.json,
    frontend/src/components/chat/ChatWidget.tsx,
    frontend/src/components/chat/ChatWindow.tsx,
    frontend/src/components/chat/index.ts
  </files>
  <read_first>
    - frontend/package.json (current deps — need to add ai@7 + @ai-sdk/react@7)
    - frontend/src/components/post/ArticleLeadSummary.tsx (typewriter pattern — reference for streaming token display)
    - frontend/src/app/layout.tsx (root layout — ChatWidget mounts here)
  </read_first>
  <behavior>
    - ChatWidget renders a fixed-position floating button (bottom-right, circular, AI icon)
    - Clicking the floating button toggles ChatWindow visibility
    - ChatWindow uses useChat with DefaultChatTransport({ api: '/api/ai/chat', body: { conversationId } })
    - useChat returns messages, input, handleInputChange, handleSubmit, isLoading, error
    - On mount, ChatWindow reads conversationId from localStorage; if absent, creates a new one (UUID or empty string — backend auto-creates if not provided per D-361)
    - On successful chat response, the returned conversationId (from body or inferred) is saved to localStorage
    - Messages display as a scrollable list with user/assistant styling
    - Streaming tokens display incrementally as they arrive (no custom typewriter — useChat auto-appends streaming text)
    - Loading indicator shows while isLoading is true
    - Error state displays error message from useChat
    - Desktop: 380x580 fixed window anchored bottom-right; mobile (max-width: 640px): fullscreen overlay
  </behavior>
  <action>
    Install frontend dependencies: `cd frontend && npm install ai@7 @ai-sdk/react@7`. Per D-368, these versions match the backend ai@7.0.37.

    Create `frontend/src/components/chat/ChatWidget.tsx` — a 'use client' component that renders a floating circular button (fixed bottom-right, z-50) with a Bot icon from lucide-react. On click, toggles an internal `isOpen` state. When open, renders ChatWindow component. The button should be ~56px diameter with a subtle shadow and theme-appropriate background (use CSS variables or a neutral color). When ChatWindow is open, the button shows an X icon instead. Export ChatWidget as default.

    Create `frontend/src/components/chat/ChatWindow.tsx` — a 'use client' component that receives `onClose` prop. Uses `useChat` from `@ai-sdk/react` with `transport: new DefaultChatTransport({ api: '/api/ai/chat', body: { conversationId } })`. Import `DefaultChatTransport` from `ai`, `useChat` from `@ai-sdk/react`, `UIMessage` from `ai`.

    ChatWindow state management: (1) `conversationId` state initialized from `localStorage.getItem('chat_conversation_id')` or empty string. (2) On first successful message exchange, if the backend created a new conversation, save the conversationId to localStorage (the backend response doesn't directly return conversationId — instead, generate a UUID on frontend for new conversations and send it as body.conversationId so the backend can use or create with that ID). Per D-361: localStorage stores conversationId for history recovery on page refresh.

    Layout: fixed container, bottom-right on desktop (380x580 per D-366), fullscreen on mobile (below 640px breakpoint). Background white/dark depending on theme. Header with "AI 助手" title + close button. Message list area (scrollable, flex-1). Input area at bottom with text input + send button.

    The `handleSubmit` from useChat is wired to a form onSubmit. `handleInputChange` from useChat is wired to the text input's onChange. `isLoading` shows a typing indicator (e.g., "AI 正在思考..." with animated dots).

    Messages rendering: iterate `messages` array from useChat. For each message, render based on `role`: user messages right-aligned with user bubble, assistant messages left-aligned with assistant bubble. For assistant messages, iterate `message.parts` array. For `part.type === 'text'`, render the text content. For `part.type === 'tool-call'`, render a loading state "正在搜索文章..." (per D-367). For `part.type === 'tool-result'`, render a ToolResultCard component (imported from ./ToolResultCard). Fallback: if `parts` is empty/undefined but `content` exists, render content as text.

    The `sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithToolCalls` should be set on useChat so tool results are automatically submitted back to the model for follow-up (per AI SDK 7 multi-step tool calling pattern). Import `lastAssistantMessageIsCompleteWithToolCalls` from `ai`.

    Create `frontend/src/components/chat/index.ts` — re-exports ChatWidget as named export.

    Update `frontend/src/app/layout.tsx`: import ChatWidget from `@/components/chat` and add `<ChatWidget />` as a sibling after `{children}` inside the `<Providers>` wrapper, before the custom footer HTML. This makes the chat widget available on all pages.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | grep -E "(ChatWidget|ChatWindow|chat/index)" | head -20; test $? -eq 0 || test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json has ai@7 and @ai-sdk/react@7 in dependencies
    - frontend/src/components/chat/ChatWidget.tsx is a 'use client' component with floating button + ChatWindow toggle
    - frontend/src/components/chat/ChatWindow.tsx uses useChat from @ai-sdk/react with DefaultChatTransport pointing to /api/ai/chat
    - useChat has sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithToolCalls for multi-step tools
    - conversationId read from / written to localStorage (per D-361)
    - ChatWindow desktop layout: 380x580 fixed bottom-right (per D-366)
    - ChatWindow mobile layout: fullscreen below 640px (per D-366)
    - Streaming tokens display incrementally via useChat's automatic message appending
    - Tool-call parts show loading state; tool-result parts render ToolResultCard
    - ChatWidget rendered in root layout.tsx inside Providers
    - TypeScript compiles with no errors in chat component files
  </acceptance_criteria>
  <done>
    Frontend chat widget installed: floating button on all pages, ChatWindow with useChat streaming, localStorage conversationId, responsive layout. TypeScript compiles clean.
  </done>
  <precondition>Plan 18-01 completed — POST /api/ai/chat endpoint must be available at /api/ai/chat (proxied via Next.js rewrites to :8091)</precondition>
  <reversibility rating="costly">Adding ai@7 + @ai-sdk/react@7 to frontend is reversible (npm uninstall), but the ChatWidget components are new code. Layout.tsx change is a single line addition. D-366 layout changes (CSS + responsive breakpoints) would need CSS adjustments to revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: MessageList + ToolResultCard + ChatInput sub-components</name>
  <files>
    frontend/src/components/chat/MessageList.tsx,
    frontend/src/components/chat/ToolResultCard.tsx,
    frontend/src/components/chat/ChatInput.tsx
  </files>
  <read_first>
    - frontend/src/components/chat/ChatWindow.tsx (the parent that consumes these components — read after Task 1 creates it)
    - frontend/src/components/post/ArticleLeadSummary.tsx (typewriter cursor animation reference — reuse the blinking cursor CSS pattern)
  </read_first>
  <behavior>
    - MessageList receives messages: UIMessage[] and renders them in a scrollable container
    - User messages styled right-aligned with distinct background
    - Assistant messages styled left-aligned; iterate parts array for text/tool-call/tool-result rendering
    - Text parts render inline with streaming cursor animation when the message is still loading
    - Tool-call parts render "正在搜索文章..." with a spinner or animated dots (per D-367)
    - Tool-result parts with articles array render as ToolResultCard list
    - ToolResultCard renders: title (clickable link), snippet text, and URL
    - ChatInput receives input, handleInputChange, handleSubmit, isLoading from useChat
    - ChatInput has a textarea/input + send button; disabled when isLoading
    - Enter key submits (Shift+Enter for newline if textarea)
    - Auto-scroll to bottom when new messages arrive
  </behavior>
  <action>
    Create `frontend/src/components/chat/MessageList.tsx` — a 'use client' component that receives `messages: UIMessage[]` and `isLoading: boolean` props. Use a `useRef` for scroll container with `useEffect` to auto-scroll on message changes. Import `UIMessage` from `ai`. For each message: (a) If `message.role === 'user'`, render a right-aligned bubble with `message.content` (extract text from parts or use content directly). (b) If `message.role === 'assistant'`, render left-aligned bubble. Iterate `message.parts` (or fall back to content if parts is empty): for `part.type === 'text'`, render the text with a blinking cursor animation if this is the last assistant message and `isLoading` is true (use a CSS animation `@keyframes blink` with `|` cursor, similar to ArticleLeadSummary's cursor). For `part.type === 'tool-call'`, render a div with "正在搜索文章..." text and an animated spinner (simple CSS rotate animation on a small circle). For `part.type === 'tool-result'`, render `<ToolResultCard result={part.result} />`.

    Create `frontend/src/components/chat/ToolResultCard.tsx` — receives `result: unknown` prop. Cast result to `{ articles?: { title: string; snippet: string; url: string }[] }` (the shape returned by search_articles tool). If `articles` array exists, map each article to a card with: clickable title as an `<a>` tag with `href` pointing to the article URL, snippet text below, and a subtle border/shadow. If no articles array (e.g., get_article result), render a single card with title, truncated content, and URL link. Style: compact card with light border, rounded corners, padding 8-12px.

    Create `frontend/src/components/chat/ChatInput.tsx` — receives `input: string`, `handleInputChange: (e) => void`, `handleSubmit: (e) => void`, `isLoading: boolean` props. Render a form with `onSubmit={handleSubmit}`. Inside: a text `<input>` (or `<textarea>` with rows=1 that auto-grows) with `value={input}`, `onChange={handleInputChange}`, `placeholder="输入消息..."`, `disabled={isLoading}`. A submit button (Send icon from lucide-react or "发送" text) disabled when isLoading or input is empty. On Enter key (without Shift), call handleSubmit. The input area should have a subtle top border and padding.

    All chat component styles should use Tailwind CSS classes (consistent with the existing frontend codebase). Use neutral colors that work in both light and dark themes (the frontend uses next-themes). For dark mode, use `dark:` prefix classes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | grep -E "(MessageList|ToolResultCard|ChatInput)" | head -20; test $? -eq 0 || test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - MessageList renders UIMessage[] with user/assistant message differentiation
    - MessageList iterates parts array: text → inline text, tool-call → loading state, tool-result → ToolResultCard
    - Last assistant message shows blinking cursor while isLoading (per D-367 typewriter)
    - Tool-call parts show "正在搜索文章..." with animated indicator (per D-367)
    - ToolResultCard renders article link cards with title + snippet + url (per D-367)
    - ChatInput has text input + submit button, disabled during loading
    - ChatInput submits on Enter key
    - Auto-scroll to bottom on new messages
    - All components use Tailwind CSS with dark mode support
    - TypeScript compiles clean
  </acceptance_criteria>
  <done>
    MessageList + ToolResultCard + ChatInput sub-components complete. Chat widget renders messages with streaming text, tool loading states, and article link cards. TypeScript compiles clean.
  </done>
  <reversibility rating="reversible">All new files. Removing them and the ChatWindow references reverts to pre-chat state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/ai/chat (via Next.js rewrite) | User-supplied chat input crosses here |
| localStorage → conversationId | Client-side storage, user can tamper |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06 | Tampering | localStorage conversationId | low | accept | conversationId is opaque to user; tampering only affects their own session history lookup |
| T-18-07 | Information Disclosure | Tool result in chat UI | low | accept | Tool results are from public articles; no private data exposed |
| T-18-SC | Tampering | npm installs (ai@7, @ai-sdk/react@7) | medium | mitigate | Both are Vercel official packages with high download counts; verify on npmjs.com before install |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes with zero errors in chat components
- ChatWidget floating button visible on page load
- Clicking button opens ChatWindow at 380x580 on desktop
- Typing a message and pressing Enter sends to /api/ai/chat
- Streaming tokens appear incrementally in the chat window
- Tool calls show loading state, results show as article link cards
- On mobile viewport, chat window opens fullscreen
</verification>

<success_criteria>
- Floating chat button on all frontend pages (D-366)
- Chat window 380x580 desktop, fullscreen mobile (D-366)
- useChat + DefaultChatTransport wired to /api/ai/chat (D-368)
- Streaming token display (D-367)
- Tool loading state + article link cards (D-367)
- localStorage conversationId persistence (D-361)
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/18-streaming-chat-endpoint/18-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Purpose | Consumed By |
|----------|---------|-------------|
| `frontend/src/components/chat/ChatWidget.tsx` | Floating button + chat window toggle | layout.tsx |
| `frontend/src/components/chat/ChatWindow.tsx` | Main chat window with useChat | ChatWidget |
| `frontend/src/components/chat/MessageList.tsx` | Renders UIMessage[] with parts | ChatWindow |
| `frontend/src/components/chat/ToolResultCard.tsx` | Renders article search results | MessageList |
| `frontend/src/components/chat/ChatInput.tsx` | Text input + submit button | ChatWindow |
| `frontend/src/components/chat/index.ts` | Re-exports | layout.tsx |
