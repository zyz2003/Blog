---
phase: 01-infrastructure
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
  - 01-02
files_modified:
  - server/src/app.module.ts
  - server/vitest.config.ts
  - server/test/app.e2e-spec.ts
  - server/test/database.spec.ts
  - server/test/schemas.spec.ts
  - server/test/response-interceptor.spec.ts
  - server/test/guards.spec.ts
  - server/test/sqids.spec.ts
  - server/data/.gitkeep
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - API-COMPAT-02
  - API-COMPAT-04
  - API-COMPAT-05

must_haves:
  truths:
    - "npm run dev starts NestJS on port 8091"
    - "SQLite WAL mode enabled, busy_timeout set to 5000ms"
    - "All 30 Drizzle schema files exist and drizzle-kit push creates the database"
    - "Global response interceptor wraps all responses as { code, data, message }"
    - "Sqids encode/decode round-trips correctly with Go-compatible seed"
    - "JWT guard and Admin guard are functional"
  artifacts:
    - path: "server/test/app.e2e-spec.ts"
      provides: "Bootstrap and port verification test"
      contains: "8091"
    - path: "server/test/database.spec.ts"
      provides: "SQLite connection + PRAGMA verification test"
      contains: "WAL"
    - path: "server/test/schemas.spec.ts"
      provides: "Schema count and structure verification test"
      contains: "30"
    - path: "server/data/anheyu.db"
      provides: "SQLite database file with all 30 tables"
      contains: ""
    - path: "server/vitest.config.ts"
      provides: "Vitest configuration for NestJS"
      contains: "vitest"
  key_links:
    - from: "server/src/app.module.ts"
      to: "server/src/common/common.module.ts"
      via: "Global providers: APP_GUARD, APP_INTERCEPTOR, APP_FILTER"
      pattern: "APP_GUARD|APP_INTERCEPTOR"
    - from: "server/src/app.module.ts"
      to: "server/src/database/database.module.ts"
      via: "DatabaseModule imported for DI"
      pattern: "DatabaseModule"
---

<objective>
Wire all infrastructure together in AppModule, push schemas to SQLite, and create integration tests to verify the entire Phase 01 infrastructure.

Purpose: This is the integration plan that connects everything built in Plans 01-04 and proves it works end-to-end. Schema push must succeed, all PRAGMAs must be set, and all tests must pass.
Output: Fully verified Phase 01 infrastructure -- NestJS app on port 8091 with Drizzle+SQLite, response format, guards, Sqids, all 30 tables.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AppModule with global providers and create Vitest configuration</name>
  <files>
    server/src/app.module.ts
    server/vitest.config.ts
    server/data/.gitkeep
  </files>
  <read_first>
    .planning/phases/01-infrastructure/01-CONTEXT.md
    server/src/app.module.ts
    server/src/common/common.module.ts
    server/src/database/database.module.ts
  </read_first>
  <behavior>
    - AppModule imports DatabaseModule and CommonModule
    - AppModule registers APP_GUARD (JwtAuthGuard), APP_INTERCEPTOR (ResponseInterceptor), and APP_FILTER (HttpExceptionFilter) as global providers
    - Vitest configured for NestJS ESM/TypeScript
    - data/.gitkeep exists so data/ is tracked by git
  </behavior>
  <action>
    Update server/src/app.module.ts: Import ConfigModule.forRoot({ isGlobal: true, validationSchema, load: [configuration] }), DatabaseModule, CommonModule, and all 18 feature modules. Add global providers: { provide: APP_GUARD, useClass: JwtAuthGuard }, { provide: APP_INTERCEPTOR, useClass: ResponseInterceptor }, { provide: APP_FILTER, useClass: HttpExceptionFilter }. Import APP_GUARD, APP_INTERCEPTOR, APP_FILTER from @nestjs/core. Import the guard/interceptor/filter classes from common. Per D-08, D-10.

    Create server/vitest.config.ts: Define configuration with test: { environment: 'node', globals: true, include: ['test/**/*.spec.ts', 'test/**/*.test.ts', 'src/**/*.spec.ts'], coverage: { provider: 'v8' } }. Ensure TypeScript path resolution works with aliases.

    Create server/data/.gitkeep as empty file so the data/ directory is tracked by git (but *.db files are in .gitignore).
  </action>
  <verify>
    <automated>cd "D:/CodeDevelopment/project/Blog/anheyu-app" && grep -c "APP_GUARD" server/src/app.module.ts | grep -E "^[1-9]" && grep -c "APP_INTERCEPTOR" server/src/app.module.ts | grep -E "^[1-9]" && grep -c "APP_FILTER" server/src/app.module.ts | grep -E "^[1-9]" && echo "WIRING_OK"</automated>
  </verify>
  <done>
    - AppModule registers JwtAuthGuard as APP_GUARD (global auth)
    - AppModule registers ResponseInterceptor as APP_INTERCEPTOR (global response format)
    - AppModule registers HttpExceptionFilter as APP_FILTER (global error format)
    - AppModule imports DatabaseModule and CommonModule
    - Vitest configuration file exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests and push schema to SQLite</name>
  <files>
    server/test/app.e2e-spec.ts
    server/test/database.spec.ts
    server/test/schemas.spec.ts
    server/test/response-interceptor.spec.ts
    server/test/guards.spec.ts
    server/test/sqids.spec.ts
  </files>
  <read_first>
    .planning/phases/01-infrastructure/01-RESEARCH.md
    server/src/common/interceptors/response.interceptor.ts
    server/src/common/filters/http-exception.filter.ts
    server/src/common/guards/jwt-auth.guard.ts
    server/src/common/guards/jwt-auth-optional.guard.ts
    server/src/common/guards/admin.guard.ts
    server/src/common/utils/sqids.util.ts
    server/src/database/schemas/index.ts
    server/src/database/database.service.ts
  </read_first>
  <behavior>
    - All tests pass with npx vitest run --reporter=verbose
    - App bootstrap test verifies port 8091
    - Database test verifies WAL mode and busy_timeout
    - Schema test verifies 30 tables exist after push
    - Response interceptor test verifies { code, data, message } format
    - Guards test verifies JwtAuthGuard, @Public(), AdminGuard behavior
    - Sqids test verifies encode/decode round-trip and Go-compatible shuffle
  </behavior>
  <action>
    Create server/test/app.e2e-spec.ts: Test that NestJS application bootstraps successfully and listens on port 8091. Use createTestingApp or createNestApplication. Verify app.listen resolves and the URL contains :8091. Per INFRA-01, INFRA-04.

    Create server/test/database.spec.ts: Test DatabaseService. (1) Verify data/ directory is created. (2) Verify SQLite connection works. (3) After connection, execute "PRAGMA journal_mode" and assert result is "wal". (4) Execute "PRAGMA busy_timeout" and assert result is 5000. (5) Execute "PRAGMA foreign_keys" and assert result is 1. Per INFRA-02, INFRA-03.

    Create server/test/schemas.spec.ts: Test schema definitions. (1) Import all schemas from schemas/index. (2) Count the number of exported table objects -- must be 30. (3) Verify each table has an 'id' column. (4) Verify tables with SoftDeleteMixin have 'deletedAt' property. Per INFRA-06.

    Create server/test/response-interceptor.spec.ts: Test ResponseInterceptor. (1) Create mock ExecutionContext with statusCode 200. (2) Create mock CallHandler returning Observable of { test: 'data' }. (3) Call interceptor.intercept(). (4) Assert result is { code: 200, message: 'success', data: { test: 'data' } }. (5) Test with statusCode 201 -- assert code: 201. Per API-COMPAT-02.

    Create server/test/guards.spec.ts: Test guard behavior. (1) Test JwtAuthGuard with @Public() -- should return true. (2) Test JwtAuthGuard without @Public() and no token -- should throw UnauthorizedException. (3) Test JwtAuthOptionalGuard with no token -- should return true (guest). (4) Test JwtAuthOptionalGuard with invalid token -- should throw UnauthorizedException with message matching Go's "Token已过期". (5) Test AdminGuard with user_group_id decoding to dbID=1, entityType=UserGroup -- should return true. (6) Test AdminGuard with user_group_id decoding to dbID=2 -- should throw ForbiddenException. Per API-COMPAT-04.

    Create server/test/sqids.spec.ts: Test Sqids encoder/decoder. (1) Call initSqidsEncoderWithSeed with known test seed. (2) Call generatePublicID(1, EntityType.Article) -- verify returns a string. (3) Call decodePublicID with result -- verify returns { dbID: 1, entityType: EntityType.Article }. (4) Test round-trip with various dbIDs and entityTypes. (5) Test with empty seed -- should use default alphabet. (6) Test shuffleAlphabet determinism: same seed produces same alphabet. (7) Test minLength=4: encoded IDs should have at least 4 characters. Per D-05, D-13.

    After all tests are created, run npx drizzle-kit push from server/ directory to push all schemas to SQLite database. This is the [BLOCKING] schema push task. Verify that data/anheyu.db is created and contains all 30 tables. Per INFRA-05, INFRA-06.
  </action>
  <verify>
    <automated>cd "D:/CodeDevelopment/project/Blog/anheyu-app/server" && npx vitest run --reporter=verbose 2>&1 | tail -20 && echo "TEST_RUN_COMPLETE"</automated>
  </verify>
  <done>
    - All integration tests pass
    - NestJS app starts on port 8091
    - SQLite WAL mode verified
    - SQLite busy_timeout=5000 verified
    - All 30 schema tables created in data/anheyu.db
    - Response interceptor wraps data as { code, message, data }
    - Guards behave matching Go backend
    - Sqids encode/decode round-trips correctly
    - drizzle-kit push succeeds without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> NestJS API | Untrusted input crosses at all /api/* endpoints |
| NestJS -> SQLite | Parameterized queries prevent SQL injection |
| .env -> AppModule | Secrets loaded at boot, must not leak in errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Information Disclosure | Error messages leaking .env values | medium | mitigate | HttpExceptionFilter never includes env values in error messages |
| T-01-11 | Denial of Service | SQLite busy_timeout exhaustion | medium | accept | Single-user blog; 5000ms timeout sufficient; serialize writes at app layer |
| T-01-SC | Tampering | npm install supply chain | high | mitigate | All packages Approved in audit table; no [SUS] or [SLOP] packages |
</threat_model>

<verification>
- npm run dev starts NestJS on port 8091
- npx drizzle-kit push creates all 30 tables in data/anheyu.db
- npx vitest run passes all tests
- SQLite PRAGMA journal_mode returns "wal"
- SQLite PRAGMA busy_timeout returns 5000
- Response format matches Go: { code, data, message }
- Error format matches Go: { code, message, data: null }
- Guards and Sqids tested with Go-compatible behavior
</verification>

<success_criteria>
- npm run dev starts NestJS app on port 8091
- drizzle-kit push creates SQLite database with all 30 tables
- WAL mode and busy_timeout=5000 verified
- Global response interceptor wraps all responses as { code, data, message }
- Sqids encode/decode round-trips with Go-compatible shuffle
- JWT and Admin guards functional (even without auth module)
- All integration tests pass
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure/01-05-SUMMARY.md` when done
</output>
