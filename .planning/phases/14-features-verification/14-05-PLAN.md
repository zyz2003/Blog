---
phase: 14-features-verification
plan: 05
type: execute
wave: 3
depends_on:
  - 14-03
  - 14-04
files_modified:
  - server/test/phase14-verification/music-verification.spec.ts
  - server/test/phase14-verification/notification-verification.spec.ts
  - server/test/phase14-verification/backup-verification.spec.ts
autonomous: true
requirements:
  - MUSIC-01
  - NOTIF-01
  - SUBSCRIBER-01

must_haves:
  truths:
    - "Music playlist returns { songs: Song[], total: number } matching Go gin.H structure"
    - "Each Song has id, neteaseId, name, artist, url, pic, lrc fields"
    - "Notification settings return allowCommentReplyNotification boolean"
    - "NotificationTypeDTO has all 9 fields matching Go struct"
    - "Subscriber subscribe/unsubscribe return void responses"
    - "Backup CRUD returns correct BackupInfo structure"
  artifacts:
    - path: "server/test/phase14-verification/music-verification.spec.ts"
      provides: "Verification of music playlist endpoint"
      contains: "describe.*Music.*verification"
    - path: "server/test/phase14-verification/notification-verification.spec.ts"
      provides: "Verification of notification and subscriber endpoints"
      contains: "describe.*Notification.*verification"
    - path: "server/test/phase14-verification/backup-verification.spec.ts"
      provides: "Verification of backup CRUD endpoints"
      contains: "describe.*Backup.*verification"
  key_links:
    - from: "server/src/music/music.controller.ts"
      to: "server/src/music/music.service.ts"
      via: "Controller must wrap Song[] as { songs, total }"
      pattern: "songs.*total"
---

<objective>
Verify Music, Notification/Subscriber, and Backup modules -- all LOW risk, confirm response structures match Go.

Per D-312: Music playlist response uses Go gin.H{ songs, total } structure. NestJS controller must wrap Song[] correctly.

Per D-313: Notification settings, avatar upload, backup CRUD are LOW risk -- confirm structure.

Purpose: Complete verification of all LOW-risk auxiliary feature modules.
Output: Three verification test suites for music, notification, and backup
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-features-verification/14-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@server/test/helpers/api-compat-helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify Music playlist and Notification/Subscriber endpoints</name>
  <files>server/test/phase14-verification/music-verification.spec.ts, server/test/phase14-verification/notification-verification.spec.ts</files>
  <read_first>
    - server/src/music/music.controller.ts — NestJS MusicController (playlist method, wrapping logic)
    - server/src/music/music.service.ts — NestJS MusicService
    - _go-backend-archive/pkg/handler/music/handler.go — Go music handler (gin.H{ songs, total } structure)
    - _go-backend-archive/pkg/handler/notification/handler.go — Go notification handler
    - _go-backend-archive/pkg/handler/notification/dto.go — Go notification DTO definitions
    - _go-backend-archive/pkg/handler/subscriber/handler.go — Go subscriber handler
    - frontend/src/types/music.ts — Frontend music type definitions
    - frontend/src/lib/api/music.ts — Frontend music API calls
    - frontend/src/hooks/use-music-api.ts — Frontend music API hook
    - server/test/phase13-verification/article-verification.spec.ts — Phase 13 test pattern reference
    - server/test/helpers/api-compat-helpers.ts — Test helper functions
  </read_first>
  <behavior>
    - Test 1: GET /api/public/music/playlist returns { songs: Song[], total: number }
    - Test 2: Each Song has id(string), neteaseId(string), name(string), artist(string), url(string), pic(string), lrc(string)
    - Test 3: GET /api/user/notification-settings returns { allowCommentReplyNotification: boolean }
    - Test 4: NotificationTypeDTO has id(number), code(string), name(string), description(string), category(string), isActive(boolean), defaultEnabled(boolean), supportedChannels(string[]), createdAt(string), updatedAt(string)
    - Test 5: Subscriber subscribe returns void
    - Test 6: Subscriber unsubscribe returns void
  </behavior>
  <action>
    **Create music verification test:**

    1. Create `server/test/phase14-verification/music-verification.spec.ts`:
       - beforeAll: createTestApp
       - GET /api/public/music/playlist: Verify response is wrapped in { code, data: { songs, total } } where songs is an array and total is a number. This matches Go's `gin.H{ "songs": songs, "total": len(songs) }` per D-312. If music.controller.ts does not wrap Song[] as { songs, total }, add the wrapping in the controller's playlist method.
       - Each Song object: verify fields id(string), neteaseId(string), name(string), artist(string), url(string), pic(string), lrc(string) per Go Song struct.
       - Note: Music endpoint depends on external API (metings.qjqq.cn). If the external API is unavailable in test, the test should handle the error gracefully (verify error response format, not crash).

    **Create notification verification test:**

    2. Create `server/test/phase14-verification/notification-verification.spec.ts`:
       - beforeAll: createTestApp, seed notification type data
       - GET /api/user/notification-settings: Verify SimpleUserNotificationSettingsResponse { allowCommentReplyNotification: boolean } per D-313.
       - PUT /api/user/notification-settings: Update settings, verify void response.
       - NotificationTypeDTO assertion (if admin endpoint exists): verify 9 fields -- id(number), code(string), name(string), description(string), category(string), isActive(boolean), defaultEnabled(boolean), supportedChannels(string[]), createdAt(string), updatedAt(string) per Go NotificationTypeDTO.
       - UserNotificationConfigDTO assertion (if endpoint exists): verify 10 fields -- id(number), userId(number), notificationTypeId(number), isEnabled(boolean), enabledChannels(string[]), notificationEmail(string|null), customSettings(object|null), notificationType(object|null), createdAt(string), updatedAt(string) per Go UserNotificationConfigDTO.

    **Subscriber endpoints:**
    - POST subscribe: Verify void response (Go returns void).
    - POST unsubscribe: Verify void response.
    - POST unsubscribe-by-token: Verify void response (if endpoint exists).
    - POST send-verification-code: Verify void response (if endpoint exists).
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/music-verification.spec.ts test/phase14-verification/notification-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/public/music/playlist returns { code, data: { songs: Song[], total: number } }
    - Each Song has id, neteaseId, name, artist, url, pic, lrc fields
    - GET /api/user/notification-settings returns { allowCommentReplyNotification: boolean }
    - NotificationTypeDTO has 9 fields: id, code, name, description, category, isActive, defaultEnabled, supportedChannels, createdAt, updatedAt
    - Subscriber subscribe returns void response
    - Subscriber unsubscribe returns void response
    - npx vitest run server/test/phase14-verification/music-verification.spec.ts exits 0
    - npx vitest run server/test/phase14-verification/notification-verification.spec.ts exits 0
  </acceptance_criteria>
  <done>
    - Music playlist returns { songs, total } structure matching Go
    - Song fields verified (7 fields)
    - Notification settings structure verified
    - Subscriber endpoints return void responses
    - All music and notification tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify Backup CRUD endpoints</name>
  <files>server/test/phase14-verification/backup-verification.spec.ts</files>
  <read_first>
    - server/src/backup/backup.controller.ts — NestJS BackupController
    - server/src/backup/backup.service.ts — NestJS BackupService
    - server/src/backup/dto/ — NestJS Backup DTOs
    - _go-backend-archive/pkg/handler/setting/handler.go — Go settings/backup handler
    - _go-backend-archive/pkg/handler/setting/dto/dto.go — Go backup DTO definitions
    - frontend/src/lib/api/config.ts — Frontend config/backup API calls
    - server/test/phase13-verification/article-verification.spec.ts — Phase 13 test pattern reference
    - server/test/helpers/api-compat-helpers.ts — Test helper functions
  </read_first>
  <behavior>
    - Test 1: GET /api/config/backup/list returns array of BackupInfo objects
    - Test 2: BackupInfo has filename(string), size(number), created_at(string), description(string), is_auto(boolean)
    - Test 3: POST /api/config/backup/create creates backup and returns BackupInfo
    - Test 4: POST /api/config/backup/restore restores from backup (void response)
    - Test 5: POST /api/config/backup/delete deletes backup (void response)
    - Test 6: POST /api/config/backup/clean removes old backups (void response)
  </behavior>
  <action>
    Create `server/test/phase14-verification/backup-verification.spec.ts`:

    **beforeAll:** createTestApp

    **Go BackupInfo assertion function:**
    - assertBackupInfo(data): verify 5 fields -- filename(string), size(number), created_at(string, ISO format), description(string), is_auto(boolean)

    **Test groups for 5 endpoints (#17-21):**
    - GET /api/config/backup/list: Verify array of BackupInfo objects. Each has 5 fields with correct types. created_at must be ISO string (not raw Date object).
    - POST /api/config/backup/create: Create a backup, verify response is BackupInfo with all 5 fields. Verify filename contains timestamp per D-239 (local time format).
    - POST /api/config/backup/restore: Restore from a backup filename, verify void response.
    - POST /api/config/backup/delete: Delete a backup, verify void response.
    - POST /api/config/backup/clean: Clean old backups, verify void response.

    **Edge cases:**
    - Backup with empty description
    - Backup list when no backups exist (empty array)
    - Restore from nonexistent backup (should return appropriate error)
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/backup-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/config/backup/list returns array of BackupInfo objects
    - BackupInfo has filename(string), size(number), created_at(string, ISO), description(string), is_auto(boolean)
    - POST /api/config/backup/create returns BackupInfo with all 5 fields
    - POST /api/config/backup/restore returns void response
    - POST /api/config/backup/delete returns void response
    - POST /api/config/backup/clean returns void response
    - Backup created_at is ISO string (not raw Date object)
    - npx vitest run server/test/phase14-verification/backup-verification.spec.ts exits 0
  </acceptance_criteria>
  <done>
    - BackupInfo structure matches Go (5 fields)
    - Backup CRUD operations verified
    - created_at is ISO string in backup responses
    - All backup verification tests pass
  </done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Backup endpoints require admin auth |
| external API -> NestJS | Music playlist fetched from metings.qjqq.cn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | Backup filename path traversal | medium | mitigate | validateBackupFilename() sanitizes input per existing implementation |
| T-14-08 | Spoofing | Music external API response | low | accept | External API response used as-is, SSL bypass per D-210 |
</threat_model>

<verification>
- Music tests pass: `cd server && npx vitest run test/phase14-verification/music-verification.spec.ts`
- Notification tests pass: `cd server && npx vitest run test/phase14-verification/notification-verification.spec.ts`
- Backup tests pass: `cd server && npx vitest run test/phase14-verification/backup-verification.spec.ts`
</verification>

<success_criteria>
- Music playlist returns { songs, total } matching Go gin.H structure
- Song fields verified (7 fields per Go Song struct)
- Notification settings structure verified
- Subscriber endpoints return void responses
- BackupInfo structure matches Go (5 fields)
- All music, notification, and backup verification tests pass
</success_criteria>

<output>
Create `.planning/phases/14-features-verification/14-05-SUMMARY.md` when done
</output>
