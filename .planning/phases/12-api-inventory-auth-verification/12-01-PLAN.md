---
phase: 12-api-inventory-auth-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md
autonomous: true
requirements:
  - REQ-12-01
must_haves:
  truths:
    - Every apiClient call in frontend/src/lib/api/ 23 files is recorded in the inventory table
    - Every non-apiClient API call (direct fetch, axios, proxy) is captured in supplementary scan
    - Each endpoint row has method, path, frontend file, request type name, response type name, Go handler path
    - Endpoints are grouped by module matching frontend API file structure
    - Every endpoint's NestJS implementation status is accurately documented (IMPLEMENTED/501/MISSING)
  artifacts:
    - .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md
  key_links:
    - Inventory table completeness (188 rows) verified by row count
    - Cross-reference accuracy verified by matching NestJS controller routes
---

<objective>
Build the complete frontend API endpoint inventory as a Markdown table, grouped by module, with cross-reference to NestJS controllers and Go handler paths.

Purpose: The inventory is the single source of truth for all subsequent verification phases (13-15). Every frontend API call must be documented so no endpoint is missed during compatibility verification.
Output: 12-API-INVENTORY.md containing the full endpoint table, supplementary scan results, and cross-reference status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-api-inventory-auth-verification/12-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build main API endpoint inventory table</name>
  <files>.planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md</files>
  <action>
Per D-270, D-271, D-272, D-273: Create 12-API-INVENTORY.md with the complete frontend API endpoint inventory.

**Primary data source: frontend source code.** Scan each file in `frontend/src/lib/api/` to extract API calls. Use RESEARCH.md Section 1 as cross-reference validation — if the scan finds endpoints not in RESEARCH.md, add them; if RESEARCH.md lists endpoints not found in scan, investigate and document the discrepancy.

Extract and format the scanned data into a Markdown document with the following structure:

1. Document header: Phase 12 API Inventory, date, total endpoint count
2. Summary table: module name, endpoint count, frontend file, NestJS implementation status
3. Per-module detail tables with columns: #, Method, Path, Frontend Method, Request Type, Response Type, Go Handler Path, NestJS Status

NestJS Status values: IMPLEMENTED, 501 NOT_IMPLEMENTED, MISSING (no NestJS route found)

Module grouping order (matching frontend API files):
- Auth (auth.ts) -- 9 endpoints
- Settings (settings.ts, site-config.ts) -- 5 endpoints
- Config/Backup (config.ts) -- 7 endpoints
- Article Public (article.ts) -- 12 endpoints
- Article Admin (post-management.ts) -- 13 endpoints
- Page (page-management.ts) -- 7 endpoints
- File Manager (file-manager.ts) -- 24 endpoints
- Comment Public (comment.ts) -- 8 endpoints
- Comment Admin (comment-management.ts) -- 8 endpoints
- Friends/Links (friends.ts) -- 25 endpoints
- Album (album.ts, album-public.ts) -- 15 endpoints
- Doc Series (doc-series.ts) -- 5 endpoints
- Music (music.ts) -- 1 endpoint
- Storage Policy (storage-policy.ts) -- 7 endpoints
- User Management (user-management.ts) -- 7 endpoints
- User Center (user-center.ts) -- 5 endpoints
- Statistics/Admin (admin.ts) -- 6 endpoints
- Theme Mall (theme-mall.ts) -- 20 endpoints
- Changelog (changelog.ts) -- 1 external endpoint

For NestJS Status, cross-reference against the NestJS controllers. Read each controller file in server/src/ to verify which routes exist. Mark endpoints as:
- IMPLEMENTED if a matching controller route exists
- 501 NOT_IMPLEMENTED if the route exists but throws 501
- MISSING if no matching route found in any NestJS controller

Known MISSING endpoints from prior research: config/export, config/import, proxy/download (D-250, D-251).
Known 501 endpoints: auth register/activate/forgot-password/reset-password/check-email, settings/test-email.

IMPORTANT: The Go router at _go-backend-archive/internal/infra/router/router.go shows that register, activate, forgot-password, reset-password, and check-email ARE implemented in Go with real handlers. The NestJS 501 behavior does NOT match Go for these 5 endpoints. Mark these as "501 NOT_IMPLEMENTED (Go has implementation -- compatibility gap)" in the NestJS Status column.
  </action>
  <verify>
    <automated>cd "d:/CodeDevelopment/project/Blog" && test -f .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md && grep -c "^|" .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md | awk '{if ($1 > 180) print "PASS: " $1 " table rows found"; else print "FAIL: only " $1 " rows, expected 180+"}'</automated>
  </verify>
  <done>12-API-INVENTORY.md exists with 188+ endpoint rows across 19 module tables, each row has Method/Path/Frontend Method/Request Type/Response Type/Go Handler/NestJS Status columns. Summary table shows per-module counts. 5 auth endpoints marked as compatibility gap (Go implemented, NestJS 501).</done>
</task>

<task type="auto">
  <name>Task 2: Supplementary scan for non-apiClient API calls</name>
  <files>.planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md</files>
  <action>
Per D-271: Append a "Supplementary Scan" section to 12-API-INVENTORY.md documenting non-apiClient API calls found in the frontend codebase.

Scan these locations for direct fetch/axios calls or other HTTP requests NOT going through apiClient:
1. frontend/src/hooks/queries/ -- 13 React Query hook files
2. frontend/src/hooks/use-music-api.ts
3. frontend/src/hooks/use-settings.ts
4. frontend/src/providers/visit-statistics-tracker.tsx
5. frontend/src/lib/proxy-backend.ts

Use grep patterns to find:
- `fetch(` calls
- `axios.` calls
- direct `http://` or `https://` URLs in source
- any import from a non-apiClient HTTP library

For each finding, record: source file, line, call type (fetch/axios/other), target URL or path, whether it's already covered in the main inventory (e.g., visit-statistics-tracker uses statisticsApi which is already in admin.ts).

The RESEARCH.md Section 1 already identified 3 supplementary items (#186-188). Verify these and check for any additional ones.

Append results as a "## Supplementary Scan" section with a table: #, Source File, Call Type, Target, Already in Main Inventory (Y/N), Notes.

If no new endpoints are found beyond the 3 already known, state that explicitly.
  </action>
  <verify>
    <automated>cd "d:/CodeDevelopment/project/Blog" && grep -q "Supplementary Scan" .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md && echo "PASS: Supplementary Scan section found" || echo "FAIL: Supplementary Scan section missing"</automated>
  </verify>
  <done>Supplementary Scan section appended to 12-API-INVENTORY.md with all non-apiClient API calls documented. Each finding has source file, call type, target URL, and whether already covered in main inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-reference inventory against NestJS controllers and produce gap summary</name>
  <files>.planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md</files>
  <action>
Append a "## Cross-Reference Gap Summary" section to 12-API-INVENTORY.md.

For each endpoint in the inventory, verify the NestJS Status column is accurate by reading the NestJS controller source files. Specifically:

1. Read all controller files in server/src/ (use glob pattern server/src/**/*.controller.ts)
2. For each controller, extract route decorators (@Get, @Post, @Put, @Delete) and their path arguments
3. Combine with the controller's @Controller() prefix to build full route paths
4. Match against inventory paths

Produce a gap summary table with three categories:
- **IMPLEMENTED**: Frontend endpoint has matching NestJS route
- **501 NOT_IMPLEMENTED**: NestJS route exists but returns 501
- **MISSING**: No NestJS route found for this frontend endpoint

For MISSING endpoints, note whether Go also lacks the endpoint (frontend-only definition) or Go has it (compatibility gap requiring implementation).

Also note: The 5 auth endpoints (register, activate, forgot-password, reset-password, check-email) are a special case -- NestJS returns 501 but Go has full implementations. These are compatibility gaps, not "matching 501 behavior."

Count totals: IMPLEMENTED count, 501 count, MISSING count, total.
  </action>
  <verify>
    <automated>cd "d:/CodeDevelopment/project/Blog" && grep -q "Cross-Reference Gap Summary" .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md && grep -q "IMPLEMENTED" .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md && grep -q "MISSING" .planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md && echo "PASS: Gap summary section found with IMPLEMENTED and MISSING categories" || echo "FAIL: Gap summary section incomplete"</automated>
  </verify>
  <done>Cross-Reference Gap Summary section appended with categorized endpoint counts. Each MISSING endpoint annotated with whether Go has it (compatibility gap) or not (frontend-only). 5 auth endpoints flagged as compatibility gaps (Go implemented, NestJS 501).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend source → Inventory | Static scan of source files; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Inventory document | low | accept | Inventory is a planning artifact, not deployed code |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan (documentation only) |
</threat_model>

<verification>
- 12-API-INVENTORY.md exists with 188+ endpoint rows
- Supplementary Scan section documents all non-apiClient calls
- Cross-Reference Gap Summary categorizes all endpoints (IMPLEMENTED/501/MISSING)
- 5 auth endpoints flagged as compatibility gaps
</verification>

<success_criteria>
- Complete inventory of all 188 frontend API endpoints in Markdown table format
- Every endpoint has Method, Path, Frontend File, Request Type, Response Type, Go Handler, NestJS Status
- Supplementary scan found no additional endpoints beyond the 3 known
- Cross-reference gap summary shows IMPLEMENTED/501/MISSING counts
- 5 auth endpoints where Go has implementation but NestJS returns 501 are flagged as compatibility gaps
</success_criteria>

<output>
Create `.planning/phases/12-api-inventory-auth-verification/12-01-SUMMARY.md` when done
</output>
