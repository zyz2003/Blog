---
phase: 04-page-public-api
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - server/src/page/page.controller.ts
  - server/src/page/public-page.controller.ts
  - server/src/page/dto/create-page.dto.ts
  - server/src/page/dto/update-page.dto.ts
  - server/src/page/page.module.ts
autonomous: true
requirements: [PAGE-01, PUBLIC-01]

must_haves:
  truths:
    - POST /api/pages creates a page (admin only) per D-77
    - GET /api/pages returns { pages, total, page, size } (admin only) per D-73
    - GET /api/pages/:id returns single page by numeric ID (admin only) per D-71
    - PUT /api/pages/:id updates page with optional fields (admin only) per D-78
    - DELETE /api/pages/:id soft-deletes page (admin only) per D-81
    - POST /api/pages/initialize creates default pages (admin only) per D-82
    - GET /api/public/pages/*path returns published page by path (public) per D-74/D-75
    - Public endpoint returns 404 for unpublished pages per D-75
    - All admin endpoints require JWT + Admin guard (global guard)
    - Public endpoints skip auth via @Public() decorator
  artifacts:
    - server/src/page/page.controller.ts
    - server/src/page/public-page.controller.ts
    - server/src/page/dto/create-page.dto.ts
    - server/src/page/dto/update-page.dto.ts
    - server/src/page/page.module.ts
  key_links:
    - PageController delegates to PageService for all business logic
    - PublicPageController calls PageService.getByPath and checks isPublished
    - Page IDs parsed as integers directly, no Sqids per D-71
    - Page list query params use page_size (underscore format) per D-73
---

<objective>
Implement PageController (admin CRUD), PublicPageController (public path access), DTOs, and wire PageModule with all providers and controllers.

Purpose: Connect the PageService (from Plan 01) to HTTP endpoints matching the Go backend API contract. Admin endpoints at /api/pages require JWT+Admin guard (global). Public endpoint at /api/public/pages/*path uses @Public() to skip auth and only returns published pages.

Output: page.controller.ts, public-page.controller.ts, DTOs, updated page.module.ts
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-public-api/04-CONTEXT.md
@.planning/phases/04-page-public-api/04-RESEARCH.md
@server/src/page/page.service.ts
@server/src/page/page.repository.ts
@server/src/article/article.controller.ts
@server/src/article/public-article.controller.ts
@pkg/handler/page/page.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DTOs and PageController for admin CRUD endpoints</name>
  <files>server/src/page/dto/create-page.dto.ts, server/src/page/dto/update-page.dto.ts, server/src/page/page.controller.ts</files>
  <read_first>
    server/src/page/page.service.ts
    server/src/article/article.controller.ts
    server/src/post-category/dto/create-post-category.dto.ts
    server/src/post-category/dto/update-post-category.dto.ts
    pkg/handler/page/page.go
    pkg/domain/model/page.go
  </read_first>
  <action>
    Create server/src/page/dto/create-page.dto.ts per D-77:
    - Class CreatePageDto with class-validator decorators
    - Required fields: title (IsString, IsNotEmpty), path (IsString, IsNotEmpty), content (IsString, IsNotEmpty) — matching Go binding:"required"
    - Optional fields: markdown_content (IsString, IsOptional), custom_js (IsString, IsOptional), custom_css (IsString, IsOptional), description (IsString, IsOptional), is_published (IsBoolean, IsOptional), show_comment (IsBoolean, IsOptional), sort (IsInt, IsOptional)
    - Use IsOptional from class-validator for all optional fields

    Create server/src/page/dto/update-page.dto.ts per D-78:
    - Class UpdatePageDto — all fields optional (pointer semantics matching Go *string/*bool/*int)
    - Do NOT use PartialType (per established pattern from Phase 03 — @nestjs/mapped-types not installed)
    - Each field is optional with IsOptional decorator: title, path, content, markdown_content, custom_js, custom_css, description (all IsString + IsOptional), is_published (IsBoolean + IsOptional), show_comment (IsBoolean + IsOptional), sort (IsInt + IsOptional)

    Create server/src/page/page.controller.ts:
    - @Controller('pages') — no @Public(), relies on global JwtAuthGuard + AdminGuard
    - Inject PageService
    - @Post() create(@Body() dto: CreatePageDto): call pageService.create(dto), return result (interceptor wraps)
    - @Get() list(@Query() query: any): parse page (default 1), page_size (default 10, per D-73 underscore format), search, is_published from query params. Call pageService.list({ page, pageSize: page_size, search, isPublished }). Return result
    - @Get(':id') get(@Param('id') id: string): parseInt(id) to get numeric ID per D-71 (no Sqids). Call pageService.getById(numericId). Return result
    - @Put(':id') update(@Param('id') id: string, @Body() dto: UpdatePageDto): parseInt(id). Call pageService.update(numericId, dto). Return result
    - @Delete(':id') delete(@Param('id') id: string): parseInt(id). Call pageService.delete(numericId). Return null with success message (interceptor wraps)
    - @Post('initialize') initializeDefaultPages(): call pageService.initializeDefaultPages(). Return null with success message

    Important route ordering: @Post('initialize') MUST be registered before @Get(':id') to avoid NestJS treating 'initialize' as an :id param. Define initializeDefaultPages method BEFORE get/update/delete methods in the class.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>
    - CreatePageDto has required fields (title, path, content) and optional fields per D-77
    - UpdatePageDto has all optional fields per D-78
    - PageController has 6 endpoints matching Go handler
    - Page IDs parsed as parseInt, no Sqids encoding per D-71
    - List uses page_size query parameter per D-73
    - Initialize route defined before :id route to avoid param capture
    - TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PublicPageController and wire PageModule</name>
  <files>server/src/page/public-page.controller.ts, server/src/page/page.module.ts</files>
  <read_first>
    server/src/page/page.service.ts
    server/src/article/public-article.controller.ts
    server/src/page/page.controller.ts
    server/src/page/page.module.ts
    pkg/handler/page/page.go
  </read_first>
  <action>
    Create server/src/page/public-page.controller.ts per D-74:
    - Apply @Public() at class level (skips global auth guard)
    - @Controller('public/pages')
    - Inject PageService
    - @Get('*path') getByPath(@Param('path') path: string): prepend '/' to path if not already present (NestJS strips leading slash from wildcard param). Call pageService.getByPath('/' + path without double slash). If returned page has isPublished === false, throw NotFoundException with '页面不存在' per D-75. Otherwise return the page (interceptor wraps as { code: 200, data: page, message: '获取页面成功' })

    Note on wildcard route: NestJS Express adapter @Get('*path') with @Controller('public/pages') captures everything after /api/public/pages/ in the path param. For /api/public/pages/privacy, @Param('path') returns "privacy". For /api/public/pages/docs/guide, @Param('path') returns "docs/guide". Prepend '/' to normalize.

    Update server/src/page/page.module.ts:
    - Import Module from @nestjs/common
    - Import PageController, PublicPageController from current directory
    - Import PageService from current directory
    - Import PageRepository from current directory
    - Import DatabaseModule from ../database/database.module
    - @Module({ imports: [DatabaseModule], controllers: [PageController, PublicPageController], providers: [PageService, PageRepository], exports: [PageService] })
    - Export PageModule
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>
    - PublicPageController has @Public() at class level
    - @Get('*path') captures multi-level paths per D-74
    - Unpublished pages return 404 with no existence hint per D-75
    - Path param gets '/' prepended for normalization
    - PageModule wires all controllers, providers, and DatabaseModule import
    - PageModule exports PageService for potential cross-module use
    - TypeScript compilation passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin client -> /api/pages/* | Authenticated admin input, JWT + AdminGuard enforced |
| Public client -> /api/public/pages/*path | Untrusted path input, no auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Page path input | medium | mitigate | validatePath in PageService rejects special chars per D-79 |
| T-04-02 | Information Disclosure | Unpublished page enumeration | low | mitigate | Public endpoint returns generic 404, no existence hint per D-75 |
| T-04-05 | Tampering | Page content XSS via public access | low | accept | Content rendered by frontend; admin intentionally controls content and custom_js |
| T-04-06 | Spoofing | Page ID parameter injection | low | mitigate | parseInt on :id param ensures numeric-only; invalid parse returns NaN causing repo miss |
</threat_model>

<verification>
- TypeScript compilation: cd server && npx tsc --noEmit
- PageController exists: grep -c 'PageController' server/src/page/page.controller.ts
- PublicPageController has @Public: grep -c '@Public' server/src/page/public-page.controller.ts
- Wildcard route: grep -c "'\*path'" server/src/page/public-page.controller.ts
- PageModule wires both controllers: grep -c 'PublicPageController' server/src/page/page.module.ts
</verification>

<success_criteria>
- All 7 page endpoints implemented (6 admin + 1 public) matching Go backend routes
- Admin endpoints protected by global JWT + Admin guard
- Public endpoint uses @Public() to skip auth
- Page IDs are numeric, no Sqids per D-71
- List response format { pages, total, page, size } per D-73
- Public endpoint only returns published pages per D-75
- Wildcard path route captures multi-level paths per D-74
- PageModule properly wires all components
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/04-page-public-api/04-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `PageController` class (server/src/page/page.controller.ts) — create, list, get, update, delete, initializeDefaultPages
- `PublicPageController` class (server/src/page/public-page.controller.ts) — getByPath with @Public() and wildcard route
- `CreatePageDto` class (server/src/page/dto/create-page.dto.ts) — title/path/content required, rest optional
- `UpdatePageDto` class (server/src/page/dto/update-page.dto.ts) — all fields optional
- `PageModule` class (server/src/page/page.module.ts) — wires controllers, services, repository, DatabaseModule
