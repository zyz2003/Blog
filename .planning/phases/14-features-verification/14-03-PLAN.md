---
phase: 14-features-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - server/test/phase14-verification/doc-series-verification.spec.ts
  - server/test/phase14-verification/statistics-verification.spec.ts
autonomous: true
requirements:
  - DOCSERIES-01
  - STATS-01
  - STATS-02

must_haves:
  truths:
    - "Doc-series uses Sqids encoding matching Go, with correct field structure"
    - "Doc-series dates are non-null ISO strings (CCP-1 resolved in Phase 13)"
    - "Statistics summary structure matches Go StatisticsSummary"
    - "Statistics trend data has correct date format and daily/weekly/monthly arrays"
    - "Statistics analytics has 6 sub-arrays matching Go VisitorAnalytics"
    - "Statistics top-pages has last_visited_at as string|null matching Go *time.Time"
  artifacts:
    - path: "server/test/phase14-verification/doc-series-verification.spec.ts"
      provides: "Field-by-field verification of 5 doc-series endpoints"
      contains: "describe.*DocSeries.*verification"
    - path: "server/test/phase14-verification/statistics-verification.spec.ts"
      provides: "Field-by-field verification of 6 statistics endpoints"
      contains: "describe.*Statistics.*verification"
  key_links:
    - from: "server/src/doc-series/doc-series.repository.ts"
      to: "server/src/common/utils/sqids.util.ts"
      via: "generatePublicID with EntityType.DocSeries for Sqids encoding"
      pattern: "generatePublicID.*DocSeries"
    - from: "server/src/statistics/statistics.service.ts"
      to: "server/src/statistics/statistics.repository.ts"
      via: "Aggregation queries for summary/trend/analytics/top-pages"
      pattern: "getSummary|getTrend|getAnalytics|getTopPages"
---

<objective>
Verify Doc-series and Statistics modules field-by-field against Go backend.

Per D-309: Doc-series uses Sqids encoding (EntityType.DocSeries), Go also uses Sqids -- must verify encoding consistency.

Per D-308: Statistics key verification points are summary structure, trend date format, analytics nesting, and top-pages last_visited_at.

Purpose: Confirm Doc-series Sqids encoding matches Go and Statistics response structures match Go handler output.
Output: Comprehensive doc-series and statistics verification test suites
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-features-verification/14-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@server/test/helpers/api-compat-helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify Doc-series endpoints field-by-field</name>
  <files>server/test/phase14-verification/doc-series-verification.spec.ts</files>
  <behavior>
    - Test 1: GET /api/doc-series returns DocSeriesListResponse { list: DocSeriesResponse[], total, page, pageSize }
    - Test 2: Each DocSeriesResponse has id as Sqids string matching Go encoding
    - Test 3: Each DocSeriesResponse has created_at and updated_at as non-null ISO strings
    - Test 4: POST /api/doc-series creates series and returns DocSeriesResponse with Sqids id
    - Test 5: GET /api/public/doc-series/:id/articles returns DocSeriesWithArticles with articles array
    - Test 6: DocArticleItem has id (Sqids string), title, abbrlink, doc_sort (number), created_at (string)
  </behavior>
  <action>
    Create `server/test/phase14-verification/doc-series-verification.spec.ts` following Phase 13 pattern:

    **beforeAll:** createTestApp, seed doc-series test data.

    **Go DocSeriesResponse field-by-field assertion function:**
    - assertDocSeriesFields(data): verify 8 fields -- id(string, Sqids encoded), created_at(string, non-null per CCP-1), updated_at(string, non-null), name(string), description(string), cover_url(string), sort(number), doc_count(number)

    **Go DocSeriesWithArticles assertion:**
    - assertDocSeriesWithArticles(data): verify DocSeriesResponse fields + articles array
    - Each DocArticleItem: id(string, Sqids), title(string), abbrlink(string), doc_sort(number), created_at(string)

    **Test groups for 5 endpoints (#134-138):**
    - GET /api/doc-series: Verify DocSeriesListResponse { list, total, page, pageSize }. Each item has DocSeriesResponse fields. Verify id is Sqids string (not raw int). Verify created_at/updated_at are non-null strings.
    - POST /api/doc-series: Create a doc series, verify response has DocSeriesResponse fields with Sqids id.
    - PUT /api/doc-series/:id: Update a doc series, verify response structure.
    - DELETE /api/doc-series/:id: Verify void response.
    - GET /api/public/doc-series/:id/articles: Verify DocSeriesWithArticles with articles array. Each article has DocArticleItem fields.

    **Sqids encoding verification:**
    - Create a doc series, note the returned id (Sqids string)
    - Use the same id in PUT and GET requests, verify it resolves correctly
    - Compare Sqids encoding: the test seed is 'api-compat-test-seed', so the same DB id should produce the same Sqids string as Go would with the same seed
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/doc-series-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Doc-series Sqids encoding verified consistent with Go
    - All DocSeriesResponse fields match Go struct
    - DocSeriesWithArticles with articles array verified
    - Dates are non-null ISO strings per CCP-1
    - All 5 doc-series endpoint tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify Statistics endpoints field-by-field</name>
  <files>server/test/phase14-verification/statistics-verification.spec.ts</files>
  <behavior>
    - Test 1: GET /api/statistics/summary returns StatisticsSummary with basic_stats, top_pages, analytics, trend_data
    - Test 2: basic_stats has 6 fields: today_visitors, today_views, yesterday_visitors, yesterday_views, month_views, year_views (all numbers)
    - Test 3: analytics has 6 sub-arrays: top_countries, top_cities, top_browsers, top_os, top_devices, top_referers
    - Test 4: trend_data.daily has DateRangeStats items with date, visitors, views
    - Test 5: top_pages has URLStatistics items with last_visited_at as string|null
    - Test 6: GET /api/public/statistics/basic returns VisitorStatistics with 6 fields
    - Test 7: POST /api/public/statistics/visit records a visit (void response)
  </behavior>
  <action>
    Create `server/test/phase14-verification/statistics-verification.spec.ts` following Phase 13 pattern:

    **beforeAll:** createTestApp, seed visitor stats test data (insert visitor_logs and visitor_stats rows).

    **Go StatisticsSummary assertion function:**
    - assertStatisticsSummary(data): verify 4 top-level fields -- basic_stats(object), top_pages(array), analytics(object), trend_data(object)

    **Go VisitorStatistics assertion:**
    - assertVisitorStatistics(data): verify 6 fields -- today_visitors(number), today_views(number), yesterday_visitors(number), yesterday_views(number), month_views(number), year_views(number)

    **Go VisitorAnalytics assertion:**
    - assertVisitorAnalytics(data): verify 6 sub-arrays -- top_countries({country, count}[]), top_cities({city, count}[]), top_browsers({browser, count}[]), top_os({os, count}[]), top_devices({device, count}[]), top_referers({referer, count}[])

    **Go VisitorTrendData assertion:**
    - assertVisitorTrendData(data): verify 3 arrays -- daily(DateRangeStats[]), weekly(always empty per Go), monthly(always empty per Go)
    - DateRangeStats: { date(string), visitors(number), views(number) }
    - Note CCP-2: date format may differ (Go RFC3339 vs NestJS ISO 8601 with milliseconds) -- both are valid ISO strings, frontend handles both

    **Go URLStatistics assertion:**
    - assertURLStatistics(data): verify 7 fields -- url_path(string), page_title(string|null), total_views(number), unique_views(number), bounce_count(number), bounce_rate(number), avg_duration(number), last_visited_at(string|null)

    **Test groups for 6 endpoints (#159-164):**
    - GET /api/statistics/summary: Full StatisticsSummary with all nested structures
    - GET /api/public/statistics/basic: VisitorStatistics with 6 fields
    - POST /api/public/statistics/visit: Record a visit, verify void response
    - GET /api/statistics/trend: VisitorTrendData with daily array
    - GET /api/statistics/analytics: VisitorAnalytics with 6 sub-arrays
    - GET /api/statistics/top-pages: URLStatistics array with last_visited_at

    **Date format note:** Per CCP-2 (LOW risk), Go uses RFC3339 without milliseconds, NestJS uses ISO 8601 with milliseconds. Both are valid and frontend handles both. Do not assert exact date format, only assert it is a valid ISO date string.
  </action>
  <verify>
    <automated>cd server && npx vitest run test/phase14-verification/statistics-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Statistics summary structure matches Go StatisticsSummary
    - VisitorStatistics has all 6 fields with correct types
    - VisitorAnalytics has 6 sub-arrays with correct item structures
    - VisitorTrendData has daily/weekly/monthly arrays
    - URLStatistics has last_visited_at as string|null
    - All 6 statistics endpoint tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Statistics visit endpoint receives untrusted visitor data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | Statistics visit logging | low | accept | IP dedup and rate limiting prevent abuse |
</threat_model>

<verification>
- Doc-series tests pass: `cd server && npx vitest run test/phase14-verification/doc-series-verification.spec.ts`
- Statistics tests pass: `cd server && npx vitest run test/phase14-verification/statistics-verification.spec.ts`
</verification>

<success_criteria>
- Doc-series Sqids encoding verified consistent with Go
- All DocSeriesResponse fields match Go struct (8 fields)
- Statistics summary structure matches Go (4 top-level fields)
- VisitorAnalytics has 6 sub-arrays matching Go
- URLStatistics.last_visited_at is string|null matching Go *time.Time
- All 11 endpoint tests pass (5 doc-series + 6 statistics)
</success_criteria>

<output>
Create `.planning/phases/14-features-verification/14-03-SUMMARY.md` when done
</output>
