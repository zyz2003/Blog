---
phase: 18-streaming-chat-endpoint
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - server/src/ai/chat.service.spec.ts
  - server/src/ai/tools/tool-bridge.spec.ts
  - server/src/ai/ai-chat.controller.spec.ts
autonomous: true
requirements: [AI-05, AI-05F]
tags: [ai, testing, unit-test, chat, hardening]

must_haves:
  truths:
    - ChatService.chat() unit test: empty messages → throws/rejects
    - ChatService.chat() unit test: ModelResolver throws → DomainError propagates
    - ChatService.chat() unit test: user message persisted before streamText call
    - ChatService.chat() unit test: onFinish persists assistant message with parts
    - toAiSdkTools unit test: converts ToolDef[] to tool() map with correct names
    - toAiSdkTools unit test: execute delegates to original ToolDef.execute with ToolContext
    - AiChatController unit test: empty messages body → 400 JSON
    - AiChatController unit test: valid messages → pipeUIMessageStreamToResponse called
    - AiChatController unit test: DomainError → 500 JSON
  artifacts:
    - server/src/ai/chat.service.spec.ts
    - server/src/ai/tools/tool-bridge.spec.ts
    - server/src/ai/ai-chat.controller.spec.ts
  key_links:
    - chat.service.spec.ts → ChatService.chat() behavior verification
    - tool-bridge.spec.ts → toAiSdkTools conversion correctness
    - ai-chat.controller.spec.ts → pre-stream validation + stream piping
---

<objective>
Harden the streaming chat implementation with unit tests covering the critical paths
and edge cases identified during Plan 18-01/18-02 implementation.

Purpose: Verify the core behaviors that are hard to test end-to-end: pre-stream
validation, persistence ordering, tool-bridge conversion, and error propagation.

Output: Unit test files for ChatService, tool-bridge, and AiChatController.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-streaming-chat-endpoint/18-CONTEXT.md
@server/src/ai/chat.service.ts
@server/src/ai/tools/tool-bridge.ts
@server/src/ai/ai-chat.controller.ts
@server/src/ai/ai.module.ts
@server/src/ai/tools/tool-def.ts
@server/src/ai/tools/article-tools.ts
@server/src/ai/chat-history.service.ts
@server/src/ai/model/model-resolver.service.ts
@server/src/ai/domain-error.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for tool-bridge + ChatService + AiChatController</name>
  <files>
    server/src/ai/tools/tool-bridge.spec.ts,
    server/src/ai/chat.service.spec.ts,
    server/src/ai/ai-chat.controller.spec.ts
  </files>
  <read_first>
    - server/src/ai/tools/tool-bridge.ts (the code under test — toAiSdkTools function)
    - server/src/ai/tools/tool-def.ts (ToolDef + ToolContext interfaces — test fixtures need these types)
    - server/src/ai/tools/article-tools.ts (articleTools array — test fixture for toAiSdkTools)
    - server/src/ai/chat.service.ts (the code under test — ChatService.chat method)
    - server/src/ai/ai-chat.controller.ts (the code under test — AiChatController.chat handler)
    - server/src/ai/chat-history.service.ts (mock target — ChatHistoryService interface)
    - server/src/ai/model/model-resolver.service.ts (mock target — ModelResolver.resolve)
    - server/src/ai/domain-error.ts (DomainError class — used in error propagation tests)
  </read_first>
  <behavior>
    - toAiSdkTools: given a ToolDef[] with 2 tools, returns object with 2 keys matching tool names
    - toAiSdkTools: each converted tool has description matching original
    - toAiSdkTools: execute on converted tool delegates to original ToolDef.execute with (input, ctx)
    - ChatService.chat: when ModelResolver.resolve throws DomainError, the error propagates (controller handles 4xx)
    - ChatService.chat: user message persisted via ChatHistoryService.appendMessage before streamText
    - ChatService.chat: when conversationId not provided, createConversation is called
    - ChatService.chat: onFinish callback persists assistant message with role='assistant' and parts
    - AiChatController: POST /api/ai/chat with empty messages → res.status(400).json with code:400
    - AiChatController: POST /api/ai/chat with valid messages → pipeUIMessageStreamToResponse called
    - AiChatController: when ChatService.chat throws DomainError → res.status(500).json with code:500
  </behavior>
  <action>
    Create `server/src/ai/tools/tool-bridge.spec.ts` using vitest (the project's test runner per server/package.json devDependencies). Import `toAiSdkTools` from `./tool-bridge`, `ToolDef` and `ToolContext` from `./tool-def`, `z` from `zod`. Test cases: (1) `toAiSdkTools` with a single ToolDef returns an object with one key matching the tool name. (2) With articleTools array, returns object with keys 'search_articles' and 'get_article'. (3) The returned tool's `description` matches the original ToolDef's description. (4) Calling `execute` on the converted tool delegates to the original ToolDef's execute function with the correct input and context. For execute test, create a mock ToolDef with a jest.fn() execute and verify it was called with (input, ctx).

    Create `server/src/ai/chat.service.spec.ts` using vitest + NestJS Testing module. Mock dependencies: ModelResolver (resolve method), ChatHistoryService (createConversation, appendMessage, getMessages), SettingsService (get method), ModuleRef (resolve method), DRIZZLE injection. Test cases: (1) When ModelResolver.resolve throws DomainError, ChatService.chat should re-throw it. (2) When conversationId is not provided, createConversation should be called. (3) User message should be persisted via appendMessage before streamText is called — verify call order using mock call order tracking. (4) The onFinish callback should call appendMessage with role='assistant' and extracted parts. Note: streamText is from the `ai` package and hard to mock directly — consider testing the persistence logic by extracting it into a testable helper or by verifying the mock calls after invoking chat. If streamText mocking proves too complex, test the persistence helper method directly and test the streamText integration via a shallow integration test.

    Create `server/src/ai/ai-chat.controller.spec.ts` using vitest + NestJS Testing module. Mock ChatService (chat method). Create mock Express Response object with jest.fn() for status, json, and other methods. Test cases: (1) POST /api/ai/chat with body `{ messages: [] }` → res.status(400) called with 400, res.json called with `{ code: 400, data: null, message: '消息不能为空' }`. (2) POST /api/ai/chat with body `{ messages: undefined }` → same 400 response. (3) POST /api/ai/chat with valid messages → ChatService.chat called, pipeUIMessageStreamToResponse called with the stream. (4) When ChatService.chat throws DomainError → res.status(500) called, res.json called with `{ code: 500, data: null, message: <error.message> }`.

    Use the project's existing test patterns: check other spec files in server/src for the vitest + NestJS Testing setup pattern (e.g., import `Test` from `@nestjs/testing`, use `createTestingModule`).
  </action>
  <verify>
    <automated>cd server && npx vitest run src/ai/tools/tool-bridge.spec.ts src/ai/chat.service.spec.ts src/ai/ai-chat.controller.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - server/src/ai/tools/tool-bridge.spec.ts has 4+ test cases covering toAiSdkTools conversion
    - server/src/ai/chat.service.spec.ts has 4+ test cases covering ChatService.chat persistence + error handling
    - server/src/ai/ai-chat.controller.spec.ts has 4+ test cases covering pre-stream validation + stream piping
    - All tests pass: `cd server && npx vitest run` exits 0
    - Tests use vitest (not jest) — consistent with project test runner
    - Mocks properly isolate units (no real DB calls, no real LLM calls)
    - Test for persistence ordering: user message appendMessage called BEFORE streamText
    - Test for onFinish: assistant message appendMessage called with correct role + parts
  </acceptance_criteria>
  <done>
    Unit tests pass for tool-bridge (conversion correctness), ChatService (persistence + error propagation), and AiChatController (pre-stream validation + stream piping). All tests green.
  </done>
  <precondition>Plans 18-01 and 18-02 completed — chat.service.ts, tool-bridge.ts, and ai-chat.controller.ts must exist and compile</precondition>
  <reversibility rating="reversible">Test files only — removing them has no impact on production code.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Production code | Unit tests import production modules but mock all external dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Tampering | Test mocks hide real behavior | low | accept | Standard unit testing trade-off; integration tests cover real paths in future phases |
</threat_model>

<verification>
- `cd server && npx vitest run` passes all chat-related tests
- Test coverage includes: tool-bridge conversion, ChatService persistence ordering, controller validation
</verification>

<success_criteria>
- All unit tests pass
- tool-bridge conversion tested (ToolDef → AI SDK tool())
- ChatService persistence tested (user message before stream, assistant message in onFinish)
- AiChatController validation tested (empty messages → 400, DomainError → 500)
- No real DB or LLM calls in tests (proper mocking)
</success_criteria>

<output>
Create `.planning/phases/18-streaming-chat-endpoint/18-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Purpose | Consumed By |
|----------|---------|-------------|
| `server/src/ai/tools/tool-bridge.spec.ts` | Verifies ToolDef → AI SDK tool() conversion | CI+CI pipeline |
| `server/src/ai/chat.service.spec.ts` | Verifies ChatService persistence + error handling | CI pipeline |
| `server/src/ai/ai-chat.controller.spec.ts` | Verifies controller validation + stream piping | CI pipeline |
