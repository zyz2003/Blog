---
phase: 05-file-upload-media
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/storage-policy/storage-policy.repository.ts
  - server/src/storage-policy/storage-policy.service.ts
  - server/src/storage-policy/storage-policy.controller.ts
  - server/src/storage-policy/dto/create-policy.dto.ts
  - server/src/storage-policy/dto/update-policy.dto.ts
  - server/src/storage-policy/storage-policy.module.ts
  - server/src/common/constants/error-codes.ts
autonomous: true
requirements: [STORAGE-01]

must_haves:
  truths:
    - StoragePolicyRepository CRUD with soft-delete filtering (isNull deletedAt)
    - StoragePolicyService validates type='local' on create/update per D-99
    - StoragePolicyService enforces flag uniqueness among non-deleted policies per D-101
    - StoragePolicyService creates 3 default policies on module init per D-101/D-102
    - StoragePolicyService masks access_key/secret_key as '********' in responses per RESEARCH Section 4
    - StoragePolicyService rejects delete when policy has referencing files per RESEARCH Section 4
    - StoragePolicyController has 5 CRUD endpoints + 2 stubbed 501 endpoints
    - StoragePolicyModule exports StoragePolicyService for FileModule and ThumbnailModule
  artifacts:
    - server/src/storage-policy/storage-policy.repository.ts
    - server/src/storage-policy/storage-policy.service.ts
    - server/src/storage-policy/storage-policy.controller.ts
    - server/src/storage-policy/dto/create-policy.dto.ts
    - server/src/storage-policy/dto/update-policy.dto.ts
    - server/src/storage-policy/storage-policy.module.ts
  key_links:
    - StoragePolicyService.create validates type='local' and flag uniqueness before insert
    - StoragePolicyService.initializeDefaultPolicies uses findByFlag to check existence idempotently
    - StoragePolicyService.delete checks hasReferencingFiles before soft-delete
    - StoragePolicyService.toApiResponse masks non-empty access_key/secret_key
---

<objective>
Implement StoragePolicyService, StoragePolicyController, and DTOs with full CRUD for local storage policies, flag uniqueness validation, and automatic default policy creation on module init.

Purpose: StoragePolicyModule manages storage policies (local only in Phase 05). Default policies (article_image, comment_image, user_avatar) are created on startup. All cloud storage types return 400/501 per D-99. Secrets are masked in API responses.

Output: storage-policy.repository.ts, storage-policy.service.ts, storage-policy.controller.ts, DTOs, storage-policy.module.ts, updated error-codes.ts
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-file-upload-media/05-CONTEXT.md
@.planning/phases/05-file-upload-media/05-RESEARCH.md
@server/src/database/schemas/storage-policy.schema.ts
@server/src/database/schemas/file.schema.ts
@server/src/common/utils/sqids.util.ts
@server/src/common/constants/error-codes.ts
@server/src/storage-policy/storage-policy.module.ts
@pkg/handler/storage_policy/handler.go
@pkg/domain/model/storage_policy.go
@ent/schema/storagepolicy.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StoragePolicyRepository with CRUD and soft-delete queries</name>
  <files>server/src/storage-policy/storage-policy.repository.ts</files>
  <read_first>
    server/src/database/schemas/storage-policy.schema.ts
    server/src/database/schemas/file.schema.ts
    server/src/article/article.repository.ts
    pkg/handler/storage_policy/handler.go
  </read_first>
  <behavior>
    - findById returns policy when id matches and deletedAt is null
    - findById returns null when policy is soft-deleted
    - findByName returns policy matching name with deletedAt null
    - findByFlag returns policy matching flag with deletedAt null
    - create inserts policy and returns the created row
    - update sets updatedAt and returns updated row
    - softDelete sets deletedAt to current timestamp
    - list returns { list, total } with pagination (page, pageSize)
    - hasReferencingFiles returns true if any entity references this policy via files->entities join
  </behavior>
  <action>
    Create StoragePolicyRepository class in server/src/storage-policy/storage-policy.repository.ts following the ArticleRepository pattern from server/src/article/article.repository.ts.

    Inject DRIZZLE token from DatabaseModule. Import storagePolicies schema from server/src/database/schemas/storage-policy.schema.ts. Import files, entities schemas for hasReferencingFiles join. Import eq, isNull, and, desc, count, sql from drizzle-orm.

    Implement these methods (all queries must include isNull(storagePolicies.deletedAt) filter):
    - findById(id: number): single row select where id matches and deletedAt is null, return row or null
    - findByName(name: string): single row select where name matches and deletedAt is null, return row or null
    - findByFlag(flag: string): single row select where flag matches and deletedAt is null, return row or null
    - create(data): insert into storagePolicies with data, returning all columns
    - update(id: number, data): update storagePolicies set data + updatedAt=current timestamp, where id matches, returning all columns
    - softDelete(id: number): update storagePolicies set deletedAt=current timestamp where id matches
    - list(options: { page: number; pageSize: number }): select with isNull(deletedAt) filter, paginated with offset/limit. Count total. Return { list, total }
    - hasReferencingFiles(id: number): join files table to entities table where entities.policyId matches the given policy id. Return boolean (count > 0)

    Export StoragePolicyRepository as @Injectable(). Use @Inject(DRIZZLE) private readonly db pattern.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>
  <done>
    - StoragePolicyRepository class exists with all 8 methods
    - All queries include isNull(deletedAt) filter
    - hasReferencingFiles joins files->entities to check policy usage
    - TypeScript compilation passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StoragePolicyService with CRUD, flag validation, default policy init, and secret masking</name>
  <files>server/src/storage-policy/storage-policy.service.ts, server/src/common/constants/error-codes.ts</files>
  <read_first>
    server/src/storage-policy/storage-policy.repository.ts
    server/src/common/utils/sqids.util.ts
    server/src/common/constants/error-codes.ts
    pkg/handler/storage_policy/handler.go
    pkg/domain/model/storage_policy.go
  </read_first>
  <behavior>
    - create validates type='local' per D-99 (rejects others with 400)
    - create validates name uniqueness among non-deleted policies
    - create validates flag uniqueness among non-deleted policies per D-101
    - create accepts only 'article_image', 'comment_image', 'user_avatar' flag values
    - create masks access_key/secret_key in response as '********' per RESEARCH Section 4
    - getById decodes publicID with EntityType.StoragePolicy=5
    - getById throws NotFoundException with POLICY_NOT_FOUND if not found
    - list returns { list, total } with masked secrets
    - update keeps existing virtualPath if empty in request (Go behavior per RESEARCH Section 4)
    - update validates flag uniqueness if changed
    - delete rejects if hasReferencingFiles returns true
    - delete performs soft-delete via repository
    - initializeDefaultPolicies creates 3 default policies on module init per D-101/D-102
    - initializeDefaultPolicies is idempotent (uses findByFlag to skip existing)
    - toApiResponse converts DB row to frontend-compatible snake_case format
  </behavior>
  <action>
    Add STORAGE_POLICY error codes to server/src/common/constants/error-codes.ts:
    - STORAGE_POLICY_FLAG_CONFLICT: '存储策略标志冲突'
    - STORAGE_DEFAULT_POLICY_INIT_FAILED: '默认存储策略初始化失败'

    (Note: STORAGE_NOT_FOUND, POLICY_NOT_FOUND, INVALID_POLICY_TYPE, POLICY_NAME_CONFLICT, POLICY_USED_BY_FILES should already exist from prior phases; verify and add if missing.)

    Create StoragePolicyService class in server/src/storage-policy/storage-policy.service.ts. Inject StoragePolicyRepository and DRIZZLE.

    Implement methods:
    - create(data: CreatePolicyDto): validate type is 'local' (throw BadRequestException with INVALID_POLICY_TYPE if not, per D-99). Validate name uniqueness via findByName (throw ConflictException with POLICY_NAME_CONFLICT if exists). If flag provided, validate flag uniqueness via findByFlag (throw ConflictException with STORAGE_POLICY_FLAG_CONFLICT per D-101). Allowed flags: 'article_image', 'comment_image', 'user_avatar'. Insert via repository. Return toApiResponse with public ID (Sqids encode with EntityType.StoragePolicy=5).
    - getById(publicID: string): decode publicID with EntityType.StoragePolicy, find by id, throw NotFoundException with POLICY_NOT_FOUND if null. Return toApiResponse.
    - list(options): delegate to repository, return { list: masked list via toApiResponse, total }.
    - update(publicID: string, data: UpdatePolicyDto): decode publicID, find existing (throw NotFoundException if null). If data.type provided and not 'local', throw 400 per D-99. If name changed, check uniqueness. If flag changed, check uniqueness. If virtualPath is empty string in request, keep existing value (Go behavior per RESEARCH Section 4). Update via repository. Return toApiResponse.
    - delete(publicID: string): decode publicID, find existing. Check hasReferencingFiles (throw 400 with POLICY_USED_BY_FILES if files reference this policy per RESEARCH Section 4). Soft-delete via repository.
    - initializeDefaultPolicies(): OnModuleInit lifecycle. Create 3 default policies if they do not exist per D-101/D-102. Each default: { name: '内置-文章图片'/'内置-评论图片'/'内置-用户头像', type: 'local', flag: 'article_image'/'comment_image'/'user_avatar', basePath: 'data/uploads', isPrivate: false, maxSize: 0 }. Use findByFlag to check existence before creating. Wrap in try-catch; log error but don't throw on failure per D-101.
    - toApiResponse(policy): convert DB row to frontend-compatible response shape per RESEARCH Section 4: { id: Sqids-encoded, created_at, updated_at, name, type, flag, server, bucket_name, is_private, access_key (masked), secret_key (masked), max_size, base_path, virtual_path, settings, node_id }. All snake_case field names. Mask non-empty access_key/secret_key as '********'.

    Export StoragePolicyService as @Injectable() implementing OnModuleInit.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>
  <done>
    - StoragePolicyService class exists with all methods
    - create validates type='local' per D-99
    - Flag uniqueness enforced per D-101
    - Default policies created on init per D-101/D-102
    - Secrets masked as '********' in all responses per RESEARCH Section 4
    - Delete rejected when policy has referencing files
    - TypeScript compilation passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create StoragePolicyController, DTOs, and StoragePolicyModule wiring</name>
  <files>server/src/storage-policy/storage-policy.controller.ts, server/src/storage-policy/dto/create-policy.dto.ts, server/src/storage-policy/dto/update-policy.dto.ts, server/src/storage-policy/storage-policy.module.ts</files>
  <read_first>
    server/src/storage-policy/storage-policy.service.ts
    server/src/article/article.controller.ts
    pkg/handler/storage_policy/handler.go
    internal/infra/router/router.go
  </read_first>
  <behavior>
    - POST /api/policies creates storage policy via service
    - GET /api/policies lists policies with pagination
    - GET /api/policies/:id returns single policy by publicID
    - PUT /api/policies/:id updates policy
    - DELETE /api/policies/:id soft-deletes policy
    - GET /api/policies/connect/onedrive/:id returns 501
    - POST /api/policies/authorize/onedrive returns 501
    - All CRUD endpoints require JWT + Admin guards
    - All responses wrapped as { code, data, message } by global interceptor
  </behavior>
  <action>
    Create CreatePolicyDto in server/src/storage-policy/dto/create-policy.dto.ts using class-validator: name (string, required), type (string, required), flag (string, optional), server (string, optional), bucketName (string, optional), isPrivate (boolean, optional, default false), accessKey (string, optional), secretKey (string, optional), maxSize (number, optional, default 0), basePath (string, optional), virtualPath (string, optional), settings (any, optional, JSON object), nodeId (number, optional).

    Create UpdatePolicyDto in server/src/storage-policy/dto/update-policy.dto.ts: all fields optional (same as CreatePolicyDto but none required).

    Create StoragePolicyController in server/src/storage-policy/storage-policy.controller.ts. All endpoints require JWT + Admin guards. Decorate with @Controller('policies'). Add @UseGuards(AdminGuard) at controller level (JwtAuthGuard is global).

    Endpoints per RESEARCH Section 1:
    - POST /api/policies — create(@Body() dto). Call service.create, return response
    - GET /api/policies — list(@Query() query). Parse page (default 1) and pageSize (default 10, max 100). Call service.list
    - GET /api/policies/:id — getById(@Param('id') publicID). Call service.getById
    - PUT /api/policies/:id — update(@Param('id') publicID, @Body() dto). Call service.update
    - DELETE /api/policies/:id — delete(@Param('id') publicID). Call service.delete

    Add 501 stubs for deferred cloud storage endpoints per CONTEXT.md deferred items:
    - GET /api/policies/connect/onedrive/:id — throw HttpException('功能暂未实现', HttpStatus.NOT_IMPLEMENTED)
    - POST /api/policies/authorize/onedrive — throw HttpException('功能暂未实现', HttpStatus.NOT_IMPLEMENTED)

    Update StoragePolicyModule in server/src/storage-policy/storage-policy.module.ts (replace placeholder):
    - Import DatabaseModule
    - Register StoragePolicyController, StoragePolicyService, StoragePolicyRepository as providers
    - Export StoragePolicyService (needed by FileModule and ThumbnailModule later)
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>
  <done>
    - StoragePolicyController has 5 CRUD endpoints + 2 stubbed 501 endpoints
    - DTOs validate with class-validator
    - StoragePolicyModule imports DatabaseModule and exports StoragePolicyService
    - Default policies auto-created on module init
    - TypeScript compilation passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin client -> /api/policies | Authenticated admin input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | Policy type input | medium | mitigate | Validate type='local' on create/update; reject cloud types per D-99 |
| T-05-02 | Information Disclosure | Secret key exposure | medium | mitigate | Mask access_key/secret_key in all API responses per RESEARCH Section 4 |
| T-05-03 | Elevation of Privilege | Non-admin policy access | low | mitigate | AdminGuard on all policy endpoints |
</threat_model>

<verification>
- TypeScript compilation: cd server && npx tsc --noEmit
- Error codes added: grep -c 'STORAGE_POLICY_FLAG_CONFLICT' server/src/common/constants/error-codes.ts
- Controller exists: grep -c 'Controller' server/src/storage-policy/storage-policy.controller.ts
</verification>

<success_criteria>
- StoragePolicyRepository has all CRUD methods with soft-delete filtering
- StoragePolicyService validates type=local, flag uniqueness, masks secrets, initializes defaults
- StoragePolicyController has 5 CRUD routes at /api/policies, 2 stubbed 501 routes
- Default policies created on startup (article_image, comment_image, user_avatar)
- TypeScript compilation passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/05-file-upload-media/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `StoragePolicyRepository` class (server/src/storage-policy/storage-policy.repository.ts) — findById, findByName, findByFlag, create, update, softDelete, list, hasReferencingFiles
- `StoragePolicyService` class (server/src/storage-policy/storage-policy.service.ts) — create, getById, list, update, delete, initializeDefaultPolicies, toApiResponse
- `StoragePolicyController` class (server/src/storage-policy/storage-policy.controller.ts) — 5 CRUD + 2 501 stubs
- `CreatePolicyDto` (server/src/storage-policy/dto/create-policy.dto.ts)
- `UpdatePolicyDto` (server/src/storage-policy/dto/update-policy.dto.ts)
- `StoragePolicyModule` (server/src/storage-policy/storage-policy.module.ts)
- Error codes: STORAGE_POLICY_FLAG_CONFLICT, STORAGE_DEFAULT_POLICY_INIT_FAILED
