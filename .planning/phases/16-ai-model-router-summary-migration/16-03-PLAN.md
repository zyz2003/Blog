---
phase: 16-ai-model-router-summary-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
files_modified:
  - server/src/settings/public-setting-keys.ts
autonomous: false
requirements:
  - AI-02F
  - AI-01
  - AI-02
  - AI-02A

must_haves:
  truths:
    - Frontend ArticleLeadSummary typewriter still renders ai_summary_gpt_name + article.summaries[0] after migration per AI-02F
    - Admin can save ai_profiles JSON via AiModelsForm and the backend resolveProfiles reads it correctly
    - Legacy fallback works: admin who has not yet saved ai_profiles can still generate summaries using old ai_summary_* keys per D-330
    - POST /api/ai/generate-summary/:id returns { summary } through the full new stack (controller -> port -> adapter -> generateText)
    - ai_summary_gpt_name remains in PUBLIC_SETTING_KEYS so frontend can read it
  artifacts:
    - Updated public-setting-keys.ts (verify ai_summary_gpt_name is present)
  key_links:
    - Frontend ArticleLeadSummary reads public setting ai_summary_gpt_name -> backend PUBLIC_SETTING_KEYS must include it
    - Admin AiModelsForm saves ai_profiles JSON -> backend resolveProfiles reads it -> ModelResolver creates provider -> SummaryAdapter calls generateText
    - Legacy path: no ai_profiles in DB -> resolveProfiles reads ai_summary_* keys -> synthesizes legacy profile

user_setup: []
---

<objective>
Verify the complete end-to-end flow after the backend rebuild (Plan 01) and frontend settings upgrade (Plan 02). Confirm that: (1) the frontend typewriter summary display still works, (2) admin can configure and use AI profiles, (3) legacy fallback works for existing configurations, (4) the full stack from admin form through backend to frontend display is operational.

Purpose: This is the acceptance gate for Phase 16. All four requirement IDs (AI-01, AI-02, AI-02F, AI-02A) must be verified here.

Output: Confirmed working AI summary feature with new architecture, multi-profile admin UI, and backward compatibility.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-ai-model-router-summary-migration/16-CONTEXT.md
@.planning/phases/16-ai-model-router-summary-migration/16-01-SUMMARY.md
@.planning/phases/16-ai-model-router-summary-migration/16-02-SUMMARY.md
@frontend/src/components/post/ArticleLeadSummary.tsx
@server/src/settings/public-setting-keys.ts
</context>

<tasks>

<task type="auto">
  <name>Verify public-setting-keys and backend-frontend integration</name>
  <files>
    server/src/settings/public-setting-keys.ts
  </files>
  <read_first>
    server/src/settings/public-setting-keys.ts,
    frontend/src/components/post/ArticleLeadSummary.tsx,
    server/src/ai/model/ai-profile.ts,
    server/src/ai/adapters/summary.adapter.ts
  </read_first>
  <action>
    1. Verify `ai_summary_gpt_name` is in PUBLIC_SETTING_KEYS (it should already be at line 326). If missing, add it — the frontend ArticleLeadSummary reads this public key to display the AI name.

    2. Verify `ai_profiles` and `ai_default_profile_id` are NOT in PUBLIC_SETTING_KEYS — they contain API keys and must remain admin-only. If they are present, remove them.

    3. Verify `ai_summary_api_key` is NOT in PUBLIC_SETTING_KEYS — it is a private key. If present, remove it.

    4. Run the full server test suite to confirm no regressions: `cd server && npx vitest run`.

    5. Run the frontend type check: `cd frontend && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd server && npx vitest run 2>&1 | tail -5 && cd ../frontend && npx tsc --noEmit 2>&1 | tail -5; echo "DONE"</automated>
  </verify>
  <done>
    - ai_summary_gpt_name is in PUBLIC_SETTING_KEYS
    - ai_profiles, ai_default_profile_id, ai_summary_api_key are NOT in PUBLIC_SETTING_KEYS
    - Full server test suite passes
    - Frontend type check passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 16 complete AI infrastructure: backend ports/adapters/model architecture with AI SDK 7 generateText, frontend multi-profile AI settings management, legacy fallback compatibility.
  </what-built>
  <how-to-verify>
    **Prerequisite:** Server and frontend must be running (`cd server && npm run dev` + `cd frontend && npm run dev`).

    **Test 1 — Legacy fallback (AI-01, D-330):**
    1. Go to admin settings. If you have old ai_summary_* values in the DB, the "AI 模型" form should show a single "legacy" profile auto-generated from those keys.
    2. Go to any article in the editor, click "AI 生成" button in the sidebar.
    3. Verify the summary generates successfully (returns text in the summary field).

    **Test 2 — Multi-profile management (AI-02A, D-335):**
    1. Go to admin settings > "AI 功能" > "AI 模型".
    2. Click "添加模型" to add a new profile.
    3. Fill in: name="测试模型", provider="deepseek", api_url, api_key, model.
    4. Toggle enabled on, check "summary" in purposes.
    5. Click "设为默认".
    6. Save settings.
    7. Go to "AI 摘要" — verify the profile selector shows the new profile.
    8. Verify system_prompt and gpt_name fields are present and editable.

    **Test 3 — Frontend typewriter (AI-02F):**
    1. Go to any public article page that has a summary (summaries[0] is non-empty).
    2. Verify the ArticleLeadSummary component renders with the AI name from ai_summary_gpt_name.
    3. Verify the typewriter animation works (text appears character by character).

    **Test 4 — Placeholder cards (D-335):**
    1. Go to admin settings > "AI 功能" > "AI 对话".
    2. Verify "敬请期待" placeholder is shown.
    3. Go to "AI 写作" — same placeholder.

    **Test 5 — Nav structure (D-334):**
    1. Verify "AI 功能" appears as a top-level nav group in admin settings.
    2. Verify it has 4 sub-items: AI 模型, AI 摘要, AI 对话, AI 写作.
    3. Verify the old "高级功能 > AI 摘要" entry no longer exists.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which test failed and what you observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public site-config API -> Frontend | ai_summary_gpt_name exposed to all visitors (public key) |
| Admin settings API -> Admin browser | ai_profiles JSON with API keys transmitted over HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Information Disclosure | public-setting-keys.ts | high | mitigate | Verify ai_profiles and API keys are NOT in PUBLIC_SETTING_KEYS; only ai_summary_gpt_name is public |
</threat_model>

<verification>
- `cd server && npx vitest run` — full test suite passes
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
- Human verification: all 5 manual tests pass
- ai_summary_gpt_name in PUBLIC_SETTING_KEYS
- ai_profiles NOT in PUBLIC_SETTING_KEYS
</verification>

<success_criteria>
- All 4 requirement IDs verified: AI-01 (ModelResolver + profiles), AI-02 (generateText migration), AI-02F (typewriter works), AI-02A (admin UI)
- Legacy fallback works for existing ai_summary_* configurations per D-330
- Multi-profile management works in admin UI per D-335
- Frontend typewriter display works after migration per AI-02F
- No API keys leaked to non-admin users
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/16-ai-model-router-summary-migration/16-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Verified Behaviors
- POST /api/ai/generate-summary/:id works through new ports/adapters/model architecture
- resolveProfiles legacy fallback operational
- AiModelsForm multi-profile management operational
- AiSummaryForm profile selector operational
- ArticleLeadSummary typewriter display operational
- PUBLIC_SETTING_KEYS correctly configured (ai_summary_gpt_name public, API keys private)
