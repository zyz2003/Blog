---
phase: 13-content-verification
plan: 05
type: execute
wave: 3
depends_on: ['13-01', '13-03']
files_modified:
  - server/test/phase13-verification/search-verification.spec.ts
autonomous: true
requirements:
  - VERIFY-03
  - API-COMPAT-07
  - API-COMPAT-08

must_haves:
  truths:
    - "Search endpoint returns SearchResult with all fields matching Go search.go"
    - "Search hits have correct field structure (id, title, content, highlight, score)"
    - "Search pagination matches Go SearchPagination struct"
  artifacts:
    - path: "server/test/phase13-verification/search-verification.spec.ts"
      provides: "Search field-by-field verification tests (1 endpoint)"
      exports: ["describe Search Field Verification"]
  key_links:
    - from: "server/test/phase13-verification/search-verification.spec.ts"
      to: "_go-backend-archive/pkg/domain/model/search.go"
      via: "Go SearchResult and SearchHit structs define the authoritative format"
      pattern: "SearchResult|SearchHit"
---

<objective>
Search endpoint field-by-field verification + drizzle-kit push (if needed)

Purpose: Verify the search endpoint matches Go backend behavior per D-293/D-294. Also run drizzle-kit push if any schema changes were made in Plan 03 (file.service.ts changes do not modify schema, but this is the designated push point per schema_push_requirement).
Output: search-verification.spec.ts + confirmed schema state
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-content-verification/13-CONTEXT.md
@.planning/phases/13-content-verification/13-RESEARCH.md
@server/test/helpers/api-compat-helpers.ts
@_go-backend-archive/pkg/domain/model/search.go
@server/src/search/search.controller.ts
@server/src/search/search.service.ts
@server/src/search/dto/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema push confirmation + Search verification</name>
  <files>server/test/phase13-verification/search-verification.spec.ts</files>
  <read_first>
    _go-backend-archive/pkg/domain/model/search.go
    server/src/search/search.controller.ts
    server/src/search/search.service.ts
    server/src/search/dto/search-response.dto.ts
    server/test/helpers/api-compat-helpers.ts
    server/test/api-compat/search-api-compat.spec.ts
  </read_first>
  <acceptance_criteria>
    - npx drizzle-kit push exits 0 (confirms schema is in sync, no changes needed since Plan 03 only modified service code, not schema)
    - search-verification.spec.ts exists with test for GET /api/search
    - Search response verified to have hits array with id, title, content, highlight, score fields
    - Search response verified to have pagination with page, page_size, total fields
    - npx vitest run server/test/phase13-verification/search-verification.spec.ts passes
  </acceptance_criteria>
  <action>
    Run npx drizzle-kit push to confirm schema is in sync. Per schema_push_requirement, this is mandatory even if no schema files were modified in this phase (Plan 03 only changed file.service.ts, not schema files). The push confirms the live database matches the schema definitions. If the push reports no changes needed, that confirms CCP-1 constraints are already enforced.

    Create search-verification.spec.ts per D-298. Import helpers from ../helpers/api-compat-helpers per D-297.

    Search endpoint verification:
    - GET /api/search: Read Go SearchResult, SearchHit, and SearchPagination structs from _go-backend-archive/pkg/domain/model/search.go. Verify search response has: hits (array of SearchHit), pagination (object with page, page_size, total). Each SearchHit should have: id (string), title (string), content (string), highlight (string), score (number). Verify typeof for each field.

    Note: The search endpoint is not in the frontend API inventory (12-API-INVENTORY.md) because the frontend uses a different search mechanism. However, per D-293, ALL endpoints get field-by-field verification. The search endpoint is accessed via GET /api/search with query parameter q.

    Seed test data: Create an article before testing search, so the FTS5 index has content to search.
  </action>
  <verify>
    <automated>npx vitest run server/test/phase13-verification/search-verification.spec.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - drizzle-kit push confirms schema is in sync (no changes needed)
    - search-verification.spec.ts exists with field-by-field test for GET /api/search
    - Search hits verified with id, title, content, highlight, score fields
    - Search pagination verified with page, page_size, total
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → NestJS app | Test context runs NestJS in-memory |
| Search query → FTS5 index | User input goes through parameterized FTS5 query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Tampering | Search query injection | medium | mitigate | Drizzle parameterized queries prevent SQL injection in FTS5 |
| T-13-SC | Tampering | npm installs | high | mitigate | No new packages installed |
</threat_model>

<verification>
- Schema push: npx drizzle-kit push exits 0
- Search tests: npx vitest run server/test/phase13-verification/search-verification.spec.ts passes
- Full suite: npx vitest run server/test/ --reporter=verbose passes (no regressions)
</verification>

<success_criteria>
- drizzle-kit push confirms schema in sync
- Search verification test covers GET /api/search with field-by-field assertions
- No regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/13-content-verification/13-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `server/test/phase13-verification/search-verification.spec.ts` — describe("Search Field Verification"), test cases for 1 search endpoint
