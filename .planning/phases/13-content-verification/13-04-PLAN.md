---
phase: 13-content-verification
plan: 04
type: execute
wave: 2
depends_on: ['13-01']
files_modified:
  - server/test/phase13-verification/comment-verification.spec.ts
autonomous: true
requirements:
  - VERIFY-03
  - API-COMPAT-07
  - API-COMPAT-08

must_haves:
  truths:
    - "Comment ListResponse includes total_with_children and has_more fields matching Go dto.go"
    - "Comment Response has all fields matching Go comment DTO struct"
    - "Comment import ImportResult has correct field names matching Go"
    - "Public and admin comment endpoints return consistent field structure"
  artifacts:
    - path: "server/test/phase13-verification/comment-verification.spec.ts"
      provides: "Comment field-by-field verification tests (16 endpoints)"
      exports: ["describe Comment Field Verification"]
  key_links:
    - from: "server/test/phase13-verification/comment-verification.spec.ts"
      to: "_go-backend-archive/pkg/handler/comment/dto/dto.go"
      via: "Go comment DTO structs define the authoritative field list"
      pattern: "ListResponse|Response|ImportResult"
---

<objective>
Comment module field-by-field verification

Purpose: Verify all 16 comment endpoints (8 public + 8 admin) match Go backend behavior per D-293/D-294/D-295. Key MEDIUM risks: total_with_children and has_more fields in ListResponse, ImportResult field names.
Output: comment-verification.spec.ts
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-content-verification/13-CONTEXT.md
@.planning/phases/13-content-verification/13-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@server/test/helpers/api-compat-helpers.ts
@_go-backend-archive/pkg/handler/comment/dto/dto.go
@_go-backend-archive/pkg/domain/model/comment.go
@frontend/src/types/comment-management.ts
@frontend/src/lib/api/comment.ts
@server/src/comment/comment.controller.ts
@server/src/comment/comment-admin.controller.ts
@server/src/comment/comment.service.ts
@server/src/comment/dto/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Comment field-by-field verification (16 endpoints)</name>
  <files>server/test/phase13-verification/comment-verification.spec.ts</files>
  <read_first>
    _go-backend-archive/pkg/handler/comment/dto/dto.go
    _go-backend-archive/pkg/domain/model/comment.go
    frontend/src/types/comment-management.ts
    frontend/src/lib/api/comment.ts
    server/src/comment/comment.controller.ts
    server/src/comment/comment-admin.controller.ts
    server/src/comment/comment.service.ts
    server/src/comment/dto/comment-response.dto.ts
    server/test/helpers/api-compat-helpers.ts
    server/test/api-compat/comment-api-compat.spec.ts
  </read_first>
  <behavior>
    - Test 1: GET /api/public/comments/latest returns ListResponse with list, total, total_with_children, page, pageSize, has_more
    - Test 2: GET /api/public/comments returns ListResponse with all fields including total_with_children and has_more
    - Test 3: GET /api/public/comments/:id/children returns ListResponse with all fields
    - Test 4: POST /api/public/comments returns Comment Response with all fields (id, content, author, email, url, ip_location, agent, created_at, updated_at, like_count, is_liked, parent_id, post_id, path, status, pinned, children, avatar, qq_avatar, is_author)
    - Test 5: GET /api/public/comments/qq-info returns QQInfoResponse with avatar and name fields
    - Test 6: GET /api/comments returns admin ListResponse with total_with_children and has_more
    - Test 7: POST /api/comments/import returns ImportResult with total_count, success_count, skipped_count, failed_count, error_messages
    - Test 8: POST /api/public/comments/:id/like returns number
    - Test 9: POST /api/public/comments/:id/unlike returns number
    - Test 10: POST /api/public/comments/upload returns { id }
    - Test 11: DELETE /api/comments returns void
    - Test 12: PUT /api/comments/:id/status returns void
    - Test 13: PUT /api/comments/:id returns void
    - Test 14: PUT /api/comments/:id/info returns void
    - Test 15: PUT /api/comments/:id/pin returns void
    - Test 16: POST /api/comments/export returns blob
  </behavior>
  <action>
    Create comment-verification.spec.ts per D-298. Import helpers from ../helpers/api-compat-helpers per D-297.

    MEDIUM-risk comment endpoints (per D-295, verify first):
    - #78 GET /api/public/comments/latest: Read Go ListResponse from _go-backend-archive/pkg/handler/comment/dto/dto.go lines 126-133. Verify ListResponse has: list (array of Response), total (number), total_with_children (number), page (number), pageSize (number, camelCase in Go!), has_more (boolean). Per RESEARCH.md, NestJS has both total_with_children and has_more fields — confirm they are present and have correct types.
    - #79 GET /api/public/comments: Same ListResponse structure verification as #78.
    - #80 GET /api/public/comments/:id/children: Same ListResponse structure verification.
    - #81 POST /api/public/comments: Read Go Response struct from dto.go. Verify Comment Response has: id (string), content (string), author (string), email (string), url (string), ip_location (string), agent (string), created_at (string, NOT null per CCP-1), updated_at (string, NOT null), like_count (number), is_liked (boolean), parent_id (string|null), post_id (string), path (string), status (string), pinned (boolean), children (array), avatar (string), qq_avatar (string), is_author (boolean). Verify typeof for each field.
    - #86 GET /api/comments: Admin ListResponse with same total_with_children and has_more verification.
    - #93 POST /api/comments/import: Verify ImportResult has total_count (number), success_count (number), skipped_count (number), failed_count (number), error_messages (array). Per RESEARCH.md, NestJS ImportCommentsResultDto has both Go and frontend field names — confirm.

    LOW-risk comment endpoints:
    - #85 GET /api/public/comments/qq-info: Verify QQInfoResponse has avatar (string) and name (string) fields.

    NONE-risk comment endpoints:
    - #82 POST /api/public/comments/:id/like: Verify returns number.
    - #83 POST /api/public/comments/:id/unlike: Verify returns number.
    - #84 POST /api/public/comments/upload: Verify returns { id }.
    - #87 DELETE /api/comments: Verify void response.
    - #88 PUT /api/comments/:id/status: Verify void response.
    - #89 PUT /api/comments/:id: Verify void response.
    - #90 PUT /api/comments/:id/info: Verify void response.
    - #91 PUT /api/comments/:id/pin: Verify void response.
    - #92 POST /api/comments/export: Verify blob response.

    Per RESEARCH.md Pitfall 4, class-transformer @Expose() decorators in comment DTOs have no effect because ClassSerializerInterceptor is not registered globally. Tests should verify actual response fields, not DTO class definitions.
  </action>
  <verify>
    <automated>npx vitest run server/test/phase13-verification/comment-verification.spec.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - comment-verification.spec.ts exists with 16 endpoint tests
    - ListResponse verified to have total_with_children (number) and has_more (boolean)
    - Comment Response verified with all fields and correct types
    - ImportResult verified with total_count, success_count, skipped_count, failed_count, error_messages
    - Date fields confirmed as strings (not null) per CCP-1
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → NestJS app | Test context runs NestJS in-memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Spoofing | Test JWT token | low | accept | Test token uses known seed |
| T-13-SC | Tampering | npm installs | high | mitigate | No new packages installed |
</threat_model>

<verification>
- Comment tests: npx vitest run server/test/phase13-verification/comment-verification.spec.ts passes
- Full suite: npx vitest run server/test/ --reporter=verbose passes (no regressions)
</verification>

<success_criteria>
- Comment verification tests cover all 16 endpoints with field-by-field assertions
- ListResponse confirmed to have total_with_children and has_more
- ImportResult confirmed to have correct field names
- No regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/13-content-verification/13-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `server/test/phase13-verification/comment-verification.spec.ts` — describe("Comment Field Verification"), test cases for 16 comment endpoints
