---
phase: 13-content-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/test/phase13-verification/article-verification.spec.ts
  - server/test/phase13-verification/category-verification.spec.ts
  - server/test/phase13-verification/tag-verification.spec.ts
autonomous: true
requirements:
  - VERIFY-03
  - API-COMPAT-07
  - API-COMPAT-08

must_haves:
  truths:
    - "CCP-1 schema audit confirms all 28 tables with created_at/updated_at have .notNull() + .default() constraints"
    - "Article endpoints return all fields matching Go ArticleResponse struct with correct types"
    - "Category and Tag endpoints return all fields matching Go PostCategoryResponse/PostTagResponse structs"
    - "ArticleStatistics endpoint returns all 8 fields from Go ArticleStatistics struct"
    - "Article import/export/batch delete 501 status documented with frontend usage check"
  artifacts:
    - path: "server/test/phase13-verification/article-verification.spec.ts"
      provides: "Article field-by-field verification tests (25 endpoints)"
      exports: ["describe Article Field Verification"]
    - path: "server/test/phase13-verification/category-verification.spec.ts"
      provides: "Category field-by-field verification tests (4 endpoints)"
      exports: ["describe Category Field Verification"]
    - path: "server/test/phase13-verification/tag-verification.spec.ts"
      provides: "Tag field-by-field verification tests (4 endpoints)"
      exports: ["describe Tag Field Verification"]
  key_links:
    - from: "server/test/phase13-verification/article-verification.spec.ts"
      to: "_go-backend-archive/pkg/domain/model/article.go"
      via: "Go ArticleResponse struct defines the authoritative field list for assertions"
      pattern: "ArticleResponse"
    - from: "server/test/phase13-verification/article-verification.spec.ts"
      to: "frontend/src/types/article.ts"
      via: "Frontend Article type defines the consumer field expectations"
      pattern: "Article"
---

<objective>
CCP-1 schema audit + Article/Category/Tag field-by-field verification

Purpose: Resolve CCP-1 (date nullability) by confirming all schema constraints, then verify all article, category, and tag endpoints match Go backend behavior field-by-field per D-290/D-293/D-294/D-295.
Output: 3 verification test files + CCP-1 audit documentation
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-content-verification/13-CONTEXT.md
@.planning/phases/13-content-verification/13-RESEARCH.md
@.planning/phases/12-api-inventory-auth-verification/12-RISK-MARKING.md
@.planning/phases/12-api-inventory-auth-verification/12-API-INVENTORY.md
@server/test/helpers/api-compat-helpers.ts
@server/test/api-compat/article-api-compat.spec.ts
@_go-backend-archive/pkg/domain/model/article.go
@frontend/src/types/article.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CCP-1 schema audit + test infrastructure + Article verification</name>
  <files>server/test/phase13-verification/article-verification.spec.ts</files>
  <read_first>
    server/src/database/schemas/article.schema.ts
    server/src/database/schemas/article-history.schema.ts
    server/src/database/schemas/post-category.schema.ts
    server/src/database/schemas/post-tag.schema.ts
    server/src/database/schemas/comment.schema.ts
    server/src/database/schemas/page.schema.ts
    server/src/database/schemas/file.schema.ts
    server/src/database/schemas/direct-link.schema.ts
    server/src/database/schemas/doc-series.schema.ts
    server/src/database/schemas/entity.schema.ts
    server/src/database/schemas/file-entity.schema.ts
    server/src/database/schemas/link.schema.ts
    server/src/database/schemas/link-tag-pivot.schema.ts
    server/src/database/schemas/metadata.schema.ts
    server/src/database/schemas/notification-type.schema.ts
    server/src/database/schemas/notification.schema.ts
    server/src/database/schemas/setting.schema.ts
    server/src/database/schemas/storage-policy.schema.ts
    server/src/database/schemas/subscriber.schema.ts
    server/src/database/schemas/tag.schema.ts
    server/src/database/schemas/url-stat.schema.ts
    server/src/database/schemas/user-group.schema.ts
    server/src/database/schemas/user-installed-theme.schema.ts
    server/src/database/schemas/user-notification-config.schema.ts
    server/src/database/schemas/user.schema.ts
    server/src/database/schemas/visitor-stat.schema.ts
    server/src/database/schemas/visitor-log.schema.ts
    server/src/database/schemas/album.schema.ts
    server/test/helpers/api-compat-helpers.ts
    _go-backend-archive/pkg/domain/model/article.go
    frontend/src/types/article.ts
    frontend/src/types/article-statistics.ts
    server/src/article/article.controller.ts
    server/src/article/public-article.controller.ts
    server/src/article/article.service.ts
    server/src/article/dto/article-response.dto.ts
  </read_first>
  <behavior>
    - Test 1: CCP-1 audit — grep all schema files for created_at/updated_at, verify each has .notNull() + .default()
    - Test 2: GET /api/public/articles returns FeedListResponse with page_size (snake_case) in pagination, list items have all ArticleResponse fields with correct types
    - Test 3: GET /api/articles returns ArticleListResponse with pageSize (camelCase) in pagination, list items have all ArticleResponse fields
    - Test 4: GET /api/articles/:id returns ArticleResponse with all 30+ fields matching Go struct (id: string, created_at: string NOT null, updated_at: string NOT null, title: string, status: string, view_count: number, word_count: number, etc.)
    - Test 5: POST /api/articles returns ArticleResponse with all fields
    - Test 6: PUT /api/articles/:id returns ArticleResponse with all fields
    - Test 7: GET /api/public/articles/statistics returns all 8 ArticleStatistics fields (total_posts, total_words, avg_words, total_views, category_stats, tag_stats, top_viewed_posts, publish_trend)
    - Test 8: GET /api/public/articles/random returns ArticleResponse subset (id, is_doc, doc_series_id)
    - Test 9: GET /api/public/articles/archives returns { list: [{ year, month, count }] }
    - Test 10: POST /api/articles/import returns 501 (document functional gap)
    - Test 11: POST /api/articles/export returns blob (verify content-type)
    - Test 12: DELETE /api/articles/batch returns { success_count, failed_count, failed_ids }
    - Test 13: GET /api/articles/:id/history returns paginated history with date fields as strings
    - Test 14: GET /api/articles/:id/history/count returns { count: number }
  </behavior>
  <action>
    Create directory server/test/phase13-verification/ per D-296.

    CCP-1 Schema Audit (per D-290/D-291): Read all 28+ schema files listed in read_first. For each table that has created_at or updated_at fields, verify the field definition includes both .notNull() and .default() constraints. Document the audit result as a comment block at the top of the test file listing each table and its constraint status. Per D-292, if any table lacks constraints, fix the schema file and note that drizzle-kit push is needed (handled in Plan 05). Per RESEARCH.md CCP-1 audit, all 28 tables already have constraints — this task confirms and documents that finding.

    Create article-verification.spec.ts per D-298. Import createTestApp, closeTestApp, assertSuccessResponse, assertPaginatedResponse, TestContext from ../helpers/api-compat-helpers per D-297. Use beforeAll to initialize TestContext, afterAll to close.

    MEDIUM-risk article endpoints (per D-295, verify first):
    - #22 GET /api/public/articles: Verify FeedListResponse structure. Per RESEARCH.md Pitfall 6, Go public article list uses page_size (snake_case) in pagination. Read Go article_handler.ListPublic/ListHome to confirm. Assert data.list items have all ArticleResponse fields from Go model/article.go lines 159-211: id (string), created_at (string, NOT null per CCP-1), updated_at (string, NOT null), title (string), content_md (string), content_html (string), cover_url (string), status (string), view_count (number), word_count (number), reading_time (number), ip_location (string), primary_color (string), is_primary_color_manual (boolean), show_on_home (boolean), post_tags (array), post_categories (array), home_sort (number), pin_sort (number), top_img_url (string), summaries (array), abbrlink (string), copyright (boolean), is_reprint (boolean), copyright_author (string), copyright_author_href (string), copyright_url (string), keywords (string), comment_count (number), scheduled_at (string|null), review_status (string), owner_id (number), owner_nickname (string), owner_avatar (string), owner_email (string), is_takedown (boolean), takedown_reason (string), takedown_at (string|null), takedown_by (number|null), extra_config (object|null), is_doc (boolean), doc_series_id (string), doc_sort (number), doc_series (object|null). Verify pagination key naming: public list should use page_size, admin list should use pageSize.
    - #34 GET /api/articles: Verify ArticleListResponse with pageSize (camelCase) pagination. Same field assertions as #22.
    - #35 GET /api/articles/:id: Verify single ArticleResponse with all fields and correct types.
    - #38 POST /api/articles: Create article, verify response has all ArticleResponse fields.
    - #39 PUT /api/articles/:id: Update article, verify response has all ArticleResponse fields.
    - #31 GET /api/public/articles/statistics: Verify all 8 fields from Go ArticleStatistics: total_posts (number), total_words (number), avg_words (number), total_views (number), category_stats (array), tag_stats (array), top_viewed_posts (array), publish_trend (array). Per RESEARCH.md, NestJS ArticleStatisticsDto has all 8 fields.
    - #32 GET /api/public/articles/random: Verify response has id (string), is_doc (boolean), doc_series_id (string).
    - #42 POST /api/articles/import: Verify returns 501. Document as functional gap per RESEARCH.md Pitfall 5.

    LOW-risk article endpoints:
    - #43 GET /api/articles/:id/history: Verify paginated history with date fields as strings (not null per CCP-1).
    - #44 GET /api/articles/:id/history/:version: Verify history detail with date fields.
    - #45 POST /api/articles/:id/history/:version/restore: Verify restored article has date fields.

    NONE-risk article endpoints (confirm structure only):
    - #33 GET /api/public/articles/archives: Verify { list: [{ year, month, count }] }.
    - #36 DELETE /api/articles/:id: Verify void response.
    - #37 DELETE /api/articles/batch: Verify { success_count, failed_count, failed_ids }.
    - #40 POST /api/articles/upload: Verify { url, file_id }.
    - #41 POST /api/articles/export: Verify blob response with content-type application/zip.
    - #46 GET /api/articles/:id/history/count: Verify { count: number }.

    For each field assertion, verify both field existence (toHaveProperty) and type (typeof). For date fields, verify they are strings (not null, per CCP-1 resolution). For nullable fields (scheduled_at, takedown_at, takedown_by, extra_config, doc_series), verify they can be null or their expected type.
  </action>
  <verify>
    <automated>npx vitest run server/test/phase13-verification/article-verification.spec.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - server/test/phase13-verification/ directory created
    - article-verification.spec.ts exists with CCP-1 audit comment block confirming all 28 tables have .notNull() + .default() constraints
    - All 25 article endpoints have field-by-field verification tests
    - MEDIUM-risk endpoints verified: created_at/updated_at are strings (not null), ArticleStatistics has all 8 fields, pagination key naming verified (page_size for public, pageSize for admin)
    - Article import 501 documented as functional gap
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Category + Tag field-by-field verification</name>
  <files>server/test/phase13-verification/category-verification.spec.ts, server/test/phase13-verification/tag-verification.spec.ts</files>
  <read_first>
    _go-backend-archive/pkg/domain/model/post_category.go
    _go-backend-archive/pkg/domain/model/post_tag.go
    frontend/src/types/article.ts
    server/src/category/category.controller.ts
    server/src/category/category.service.ts
    server/src/tag/tag.controller.ts
    server/src/tag/tag.service.ts
    server/test/helpers/api-compat-helpers.ts
    server/test/api-compat/post-category-api-compat.spec.ts
    server/test/api-compat/post-tag-api-compat.spec.ts
  </read_first>
  <behavior>
    - Test 1: GET /api/post-categories returns PostCategory[] with all fields (id: string, name: string, slug: string, description: string, created_at: string NOT null, updated_at: string NOT null, is_series: boolean, sort_order: number)
    - Test 2: POST /api/post-categories returns PostCategory with all fields
    - Test 1 fields
    - Test 3: PUT /api/post-categories/:id returns PostCategory with all fields
    - Test 4: DELETE /api/post-categories/:id returns void
    - Test 5: GET /api/post-tags returns PostTag[] with all fields (id: string, name: string, slug: string, created_at: string NOT null, updated_at: string NOT null)
    - Test 6: POST /api/post-tags returns PostTag with all Test 5 fields
    - Test 7: PUT /api/post-tags/:id returns PostTag with all fields
    - Test 8: DELETE /api/post-tags/:id returns void
  </behavior>
  <action>
    Create category-verification.spec.ts per D-298. Import helpers from ../helpers/api-compat-helpers per D-297.

    MEDIUM-risk category/tag endpoints (per D-295):
    - #23 GET /api/post-categories: Read Go PostCategoryResponse struct from _go-backend-archive/pkg/domain/model/post_category.go. Verify each PostCategory item has: id (string, Sqids), name (string), slug (string), description (string), created_at (string, NOT null per CCP-1), updated_at (string, NOT null), is_series (boolean), sort_order (number). Verify typeof for each field.
    - #25 POST /api/post-categories: Create category with { name, slug, description, is_series, sort_order }, verify response has all PostCategory fields with correct types.
    - #27 PUT /api/post-categories/:id: Update category, verify response has all PostCategory fields.

    NONE-risk category endpoints:
    - #29 DELETE /api/post-categories/:id: Verify void response.

    Create tag-verification.spec.ts per D-298.

    MEDIUM-risk tag endpoints:
    - #24 GET /api/post-tags: Read Go PostTagResponse struct from _go-backend-archive/pkg/domain/model/post_tag.go. Verify each PostTag item has: id (string, Sqids), name (string), slug (string), created_at (string, NOT null per CCP-1), updated_at (string, NOT null). Verify typeof for each field.
    - #26 POST /api/post-tags: Create tag with { name, slug }, verify response has all PostTag fields.
    - #28 PUT /api/post-tags/:id: Update tag, verify response has all PostTag fields.

    NONE-risk tag endpoints:
    - #30 DELETE /api/post-tags/:id: Verify void response.

    For all date fields, assert they are strings (not null) per CCP-1 resolution. For id fields, assert typeof is string (Sqids encoded).
  </action>
  <verify>
    <automated>npx vitest run server/test/phase13-verification/category-verification.spec.ts server/test/phase13-verification/tag-verification.spec.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - category-verification.spec.ts exists with 4 endpoint tests (GET/POST/PUT/DELETE)
    - tag-verification.spec.ts exists with 4 endpoint tests (GET/POST/PUT/DELETE)
    - All PostCategory fields verified: id, name, slug, description, created_at, updated_at, is_series, sort_order
    - All PostTag fields verified: id, name, slug, created_at, updated_at
    - Date fields confirmed as strings (not null) per CCP-1
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → NestJS app | Test context runs NestJS in-memory, no network boundary |
| Test code → SQLite file DB | Test uses file-based SQLite, no auth boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | Schema audit grep | low | accept | Schema files are source of truth, grep confirms constraints |
| T-13-02 | Spoofing | Test JWT token | low | accept | Test token uses known seed, not production secret |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this phase |
</threat_model>

<verification>
- CCP-1 audit: grep -r "createdAt" server/src/database/schemas/ | grep -c "notNull" returns count matching tables with created_at
- Article tests: npx vitest run server/test/phase13-verification/article-verification.spec.ts passes
- Category tests: npx vitest run server/test/phase13-verification/category-verification.spec.ts passes
- Tag tests: npx vitest run server/test/phase13-verification/tag-verification.spec.ts passes
- Full suite: npx vitest run server/test/ --reporter=verbose passes (no regressions)
</verification>

<success_criteria>
- CCP-1 schema audit documented: all 28 tables with created_at/updated_at have .notNull() + .default()
- Article verification tests cover all 25 endpoints with field-by-field assertions
- Category verification tests cover all 4 endpoints with field-by-field assertions
- Tag verification tests cover all 4 endpoints with field-by-field assertions
- All date fields confirmed as strings (not null) in responses
- ArticleStatistics confirmed to have all 8 fields
- Article import 501 documented as functional gap
- No regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/13-content-verification/13-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `server/test/phase13-verification/article-verification.spec.ts` — describe("Article Field Verification"), test cases for 25 article endpoints
- `server/test/phase13-verification/category-verification.spec.ts` — describe("Category Field Verification"), test cases for 4 category endpoints
- `server/test/phase13-verification/tag-verification.spec.ts` — describe("Tag Field Verification"), test cases for 4 tag endpoints
