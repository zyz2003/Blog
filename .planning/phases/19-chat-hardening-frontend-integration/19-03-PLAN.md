---
phase: 19-chat-hardening-frontend-integration
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - frontend/src/app/admin/ai-chat/page.tsx
  - frontend/src/app/admin/ai-chat/_components/ConversationList.tsx
  - frontend/src/app/admin/ai-chat/_components/ConversationDetail.tsx
  - frontend/src/app/admin/ai-chat/_hooks/use-ai-chat-page.ts
  - frontend/src/components/admin/sidebar.tsx
autonomous: false
requirements: [AI-07]

must_haves:
  truths:
    - Admin can view all chat conversations in a paginated list at /admin/ai-chat (per D-387)
    - Admin can click a conversation to view its full message history
    - Admin can delete a conversation from the list or detail view
    - Phase 16 Wave 3 verification passes: typewriter summary display, AiModelsForm round-trip, legacy fallback, public key security (per D-393)
  artifacts:
    - frontend/src/app/admin/ai-chat/page.tsx
    - frontend/src/app/admin/ai-chat/_components/ConversationList.tsx
    - frontend/src/app/admin/ai-chat/_components/ConversationDetail.tsx
    - frontend/src/app/admin/ai-chat/_hooks/use-ai-chat-page.ts
    - Admin sidebar updated with AI Chat link
  key_links:
    - /admin/ai-chat page → fetchConversations API → GET /api/ai/conversations
    - ConversationDetail → fetchConversationMessages API → GET /api/ai/conversations/:id/messages
    - Delete button → deleteConversation API → DELETE /api/ai/conversations/:id
---

<objective>
Admin conversation management page and Phase 16 Wave 3 legacy verification.

Purpose: Give admins visibility into chat conversations (list, view, delete) and close the Phase 16 Wave 3 verification gap. All changes per D-387, D-393.

Output: /admin/ai-chat management page, Phase 16 Wave 3 verification confirmed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-model-router-summary-migration/16-CONTEXT.md
@frontend/src/app/admin/comments/page.tsx
@frontend/src/lib/api/ai.ts
</context>

<tasks>

<task type="tracer">
  <name>Admin conversation management page at /admin/ai-chat</name>
  <files>frontend/src/app/admin/ai-chat/page.tsx, frontend/src/app/admin/ai-chat/_components/ConversationList.tsx, frontend/src/app/admin/ai-chat/_components/ConversationDetail.tsx, frontend/src/app/admin/ai-chat/_hooks/use-ai-chat-page.ts, frontend/src/components/admin/sidebar.tsx</files>
  <action>
Build the admin conversation management page following the existing admin page pattern (reference comments page at frontend/src/app/admin/comments/):

1. **use-ai-chat-page.ts — custom hook for page state:**
   - State: `conversations`, `selectedConversationId`, `messages`, `isLoading`, `pagination` (page, pageSize, total)
   - On mount: call `fetchConversations(1, 20)` from the ai API client
   - `selectConversation(id)`: call `fetchConversationMessages(id)`, set messages state
   - `deleteConversation(id)`: call `deleteConversation(id)`, then refresh list
   - `loadPage(page)`: call `fetchConversations(page, 20)`, update pagination
   - Return all state and action functions

2. **ConversationList.tsx — table component:**
   - Columns: ID (Sqids publicId, truncated), Title (or "无标题"), Message Count (from API if available, or "—"), Created At (formatted date), Actions (View + Delete buttons)
   - Pagination controls at bottom (Previous/Next with page number)
   - Click "View" to select conversation and show detail panel
   - Click "Delete" to confirm and delete
   - Style: match existing admin table pattern (reference CommentTable)

3. **ConversationDetail.tsx — message detail panel:**
   - Shows when a conversation is selected
   - Header: conversation title + close button
   - Message list: each message shows role badge (user/assistant/system/tool), content, timestamp, token usage if available
   - Delete button at bottom
   - Style: scrollable panel, matching admin detail panels

4. **page.tsx — main page component:**
   - Uses the use-ai-chat-page hook
   - Layout: two-column on desktop (list left, detail right), single column on mobile (list or detail)
   - Loading skeleton while fetching
   - Follow the pattern from comments page: motion.div with adminContainerVariants, bg-card rounded-xl container

5. **Admin sidebar — add AI Chat link:**
   Find the admin sidebar component (likely in `frontend/src/components/admin/sidebar.tsx` or similar) and add a navigation item for "AI 对话" pointing to `/admin/ai-chat`. Place it in the AI/Plugins section if one exists, or near the existing admin nav items. Use the MessageCircle icon (already imported in settings-nav.ts).

Key constraints:
- All API calls go through the authenticated admin API client (JWT token in headers)
- The GET /api/ai/conversations endpoint requires admin auth (set up in Plan 19-01)
- Follow existing admin page patterns for consistency
- No new npm packages needed
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -10; echo "Build check complete"</automated>
  </verify>
  <done>
    - /admin/ai-chat page renders conversation list with pagination
    - Clicking a conversation shows its messages in detail panel
    - Delete button removes conversation and refreshes list
    - Admin sidebar has AI Chat navigation link
    - Frontend builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 16 Wave 3 verification + Phase 19 end-to-end acceptance (per D-393).

This checkpoint verifies:
1. **Phase 16 Wave 3 items:**
   - Frontend AI summary typewriter display still works after migration
   - Admin AiModelsForm round-trip: add/edit/remove profiles, set default, save, reload — values persist
   - Legacy fallback: old ai_summary_* single-config keys still work when ai_profiles is empty
   - Public key security: api_key is NOT in PUBLIC_SETTING_KEYS (not exposed to unauthenticated users)

2. **Phase 19 end-to-end:**
   - Chat widget opens, shows welcome message + suggestions
   - Send a message, get streaming response with tool calls (article search)
   - Long conversation (>20 messages) triggers compression
   - Disconnect browser mid-stream, verify assistant message still persisted
   - Refresh page, conversation recovers from localStorage
   - New conversation button works
   - Session switching works
   - Admin /admin/ai-chat page shows conversations, can view and delete
   - Admin AiChatForm saves chat profile + welcome + suggestions + system prompt
  </what-built>
  <how-to-verify>
1. Start both servers: `cd server && npm run dev` + `cd frontend && npm run dev`
2. **Phase 16 verification:**
   a. Open any public article page — verify AI summary typewriter animation works
   b. Go to Admin > Settings > AI 模型 — add a new profile, set purposes, save, reload page — verify profile persists
   c. In browser DevTools, check that api_key values are NOT in the public site-config response (GET /api/public/site-config)
   d. Test legacy fallback: temporarily clear ai_profiles setting, verify old ai_summary_* keys still produce summaries
3. **Phase 19 verification:**
   a. Click chat widget (bottom-right floating button) — verify welcome message + 3 suggestion buttons appear
   b. Click a suggestion — verify message sends and streaming response arrives
   c. Ask "搜索博客文章" — verify tool call returns article results
   d. Send 20+ messages to trigger compression — verify no error, response still coherent
   e. During a streaming response, close the browser tab — reopen and check /admin/ai-chat — verify the assistant message was persisted
   f. Refresh the page with an active conversation — verify messages reload from localStorage recovery
   g. Click "+" new conversation button — verify chat clears and new conversation starts
   h. Click conversation title to open session switcher — verify history list appears, can switch conversations
   i. Go to Admin > AI 对话 settings — verify AiChatForm shows profile dropdown, welcome message, suggestions, system prompt fields
   j. Save AiChatForm values, reload — verify persistence
   k. Go to /admin/ai-chat — verify conversation list with pagination
   l. Click a conversation — verify message detail shows
   m. Delete a conversation — verify it's removed from list
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → /admin/ai-chat page | Admin-only, requires JWT auth |
| admin → DELETE /api/ai/conversations/:id | Destructive, admin-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-19-07 | Elevation of Privilege | /admin/ai-chat page access | high | mitigate | Page protected by admin auth check in layout; API endpoints guarded by AdminGuard |
| T-19-08 | Tampering | Conversation deletion | medium | mitigate | Admin-only + confirmation dialog before delete |
</threat_model>

<verification>
- /admin/ai-chat page renders and loads conversations
- Conversation detail view shows messages
- Delete conversation works
- Admin sidebar has AI Chat link
- Phase 16 Wave 3 items verified (typewriter, AiModelsForm round-trip, legacy fallback, public key security)
- Phase 19 end-to-end features verified (welcome, suggestions, disconnect, session switching, conversation recovery, AiChatForm)
</verification>

<success_criteria>
- Admin conversation management page fully functional at /admin/ai-chat
- Phase 16 Wave 3 verification passes (all 4 items)
- Phase 19 end-to-end acceptance passes (all chat features working)
- Human verification checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/19-chat-hardening-frontend-integration/19-03-SUMMARY.md` when done
</output>
